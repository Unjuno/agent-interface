# Retired after duplicate consumption; original workflow is retained at 81047978.
# Never re-enable this allocation ID. A successor needs a new preregistration.
name: MAP01 measurement integration live 02 (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retained-allocation-only:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'live-02 is consumed and protocol-invalid; no execution authorized'
