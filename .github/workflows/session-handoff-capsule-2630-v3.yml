name: session-handoff-capsule-2630-v3
on:
  pull_request:
    paths:
      - "research/session_handoff/capsule_contract_successor_v3/**"
      - ".github/workflows/session-handoff-capsule-2630-v3.yml"
jobs:
  audit:
    runs-on: ubuntu-latest
    container: python:3.12-slim-bookworm
    steps:
      - uses: actions/checkout@v4
      - run: python -m unittest research.session_handoff.capsule_contract_successor_v3.test_validator
