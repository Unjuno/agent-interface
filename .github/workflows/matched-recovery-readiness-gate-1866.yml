name: Matched recovery readiness gate v1
on:
  pull_request:
    paths:
      - 'research/analysis/matched_recovery_readiness_gate_1866_v1/**'
  push:
    branches: [research/matched-recovery-readiness-gate-1866-v1]
    paths:
      - 'research/analysis/matched_recovery_readiness_gate_1866_v1/**'
permissions:
  contents: read
jobs:
  run:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with: {fetch-depth: 1, persist-credentials: false}
      - uses: actions/setup-python@v5
        with: {python-version: '3.13'}
      - name: Run one frozen construction
        run: |
          python -m py_compile research/analysis/matched_recovery_readiness_gate_1866_v1/experiment.py
          python research/analysis/matched_recovery_readiness_gate_1866_v1/experiment.py | tee result.json
      - uses: actions/upload-artifact@v4
        with:
          name: matched-recovery-readiness-gate-v1
          path: result.json
          retention-days: 30
