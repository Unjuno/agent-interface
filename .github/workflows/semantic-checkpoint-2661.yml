name: semantic-checkpoint-2661
on:
  pull_request:
    paths:
      - "research/semantic_checkpoint/checkpoint_contract_successor_v1/**"
      - ".github/workflows/semantic-checkpoint-2661.yml"
jobs:
  audit:
    runs-on: ubuntu-latest
    container: python:3.12-slim-bookworm
    steps:
      - uses: actions/checkout@v4
      - run: python -m unittest research.semantic_checkpoint.checkpoint_contract_successor_v1.test_validator
