# MotorState v1 public package

This package exposes only the pure validation adapter from the verified #2505
contract. It transforms no input, changes no lease, opens no backend, and does
not infer task success. Backend integration remains a separate successor.
