# Primary-assistant live GUI use through MCP composition

The primary assistant called native_observe through an official SDK stdio client,
viewed its returned image block, chose the explicit rectangle edge and key tail,
then called native_submit through MCP. No script chose the action. One private
WSL Inkscape allocation seed991116; one final action,43 emissions, verified release.
Independent SVG inspection confirms x86/y50/40×30/no transform. The public task
requires x>50.5, not exact x86. Feedback matched, tracked cleanup completed and
owner57149 exited0. Recorded process termination is not all-descendant proof.

Both original MCP responses, initialize/list_tools records, caller requests,
server stderr, native run, saved SVG and pre-run PLAN are retained. `audit.py`
checks46 manifest files, both image blocks against capture artifacts, raw receipt
digests, exact submitted decision, one action/program and independent geometry.

Scope: real model-chosen GUI action crossing the MCP adapter, using a SDK bridge
through shell tools. This Codex conversation did not register new native MCP
tools. Each call starts a new server connection, so persistent host integration,
shell elimination, latency, token/cost reduction and usability generalization
remain open. No helper model, sensor work, replay or Docker restart.

CI failure retained: first dedicated stdio CI failed before tests because sparse
checkout omitted runtime/selector_v1, imported through runtime.cli_v1.__init__.
Add that directory and its workflow trigger. A fresh local directory containing
only the corrected CI file list passed the real stdio protocol test. The live
run used unchanged e89b4011d adapter code; this repair affects CI checkout only.

Do not elevate the older inert-fixture test into live-use evidence: this archive
is the separate live successor. It does not satisfy host-registered tool usage.
