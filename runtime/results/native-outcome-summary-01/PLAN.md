# Native outcome presentation

Problem observed in native-calc-final-action-01: a terminal reply has successful
saved-file scoring while the last frame retains dialog pixels and feedback is
needs_review/BadWindow. Top-level finished is session termination, not evidence
that all of these layers succeeded.
H: a small direct-field summary makes these distinctions available before the
full receipt, without reinterpreting image content or granting authority.
T: expose recorded status, strict-boolean evaluation success (otherwise unknown),
current action input status, current action feedback status and cleanup status.
Do not infer success from finished/completed, prior actions, image pixels or title.
Retain complete raw report and source digest, and keep existing image validation.
Test differing/unknown/malformed fields, no mutation, compact/full agreement and
damaged images. Re-present the exact retained Calc final reply read-only and
verify its original image and report hashes are unchanged. No GUI allocation,
sensor work, model helper or input replay; this is not a new task completion run.
D: tests, source snapshots, exact input hashes and summary output; metadata size
overhead measured with/without the new field, not model token/cost claims.
C: PASS_SCOPED only for faithful presentation and unchanged evidence. Wrong
success inference or lost error is FAIL. Agent accuracy, decision speed, token
utility, readiness and painting coherence remain unmeasured/HOLD.
