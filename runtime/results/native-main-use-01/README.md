# Primary use of integrated main in WSL

Pinned source is in RESULT.json and the preregistered PLAN.json inside evidence.tar.gz. One new private Inkscape allocation, seed 991201, max three stages. No helper model or sensor experiment.

The primary assistant viewed the initial exact image, chose an edge click at [604,391], then inspected the returned selection handles and X=50/Y=50/W=40/H=30. Only then it sent six Right chords, explicit wait_update 50 ms, Ctrl+S and finish_after against returned source sequence 7. The final linked image showed X=62 and Document saved; the retained SVG independently parsed to x=62/y=50/w=40/h=30 without transform. Two requests were published once, with verified source/reply hashes. No action replay or extra allocation occurred.

The harness tool session exited 0. Raw cleanup says completed and tracked_processes_terminal, with owner_exit_verified=false and descendants_verified=false; those flags are preserved. A subsequent WSL process listing showed no remaining experiment GUI processes, but this is not a complete process-tree identity audit. No Docker restart or container experiment was performed.

RESULT.json includes exchange-call durations from the existing monotonic timestamps. They exclude primary reasoning, shell/tool overhead, image viewing and setup; they are not model-visible latency or a comparison to another interface/human baseline. Token use and cost were not measured.

The compressed bundle contains the plan, caller helper, decisions, returned envelopes, raw run artifacts, images, geometry, cleanup and SHA256 manifest. This is retained integration evidence, not a formal research-adoption PASS. Source code was not changed during the allocation.
