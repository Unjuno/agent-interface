# Timed primary MCP run, before allocation
Use the existing managed server and one persistent SDK connection. Inkscape seed 991118, max stages 2; choose input only after seeing returned source and public goal. Keep the existing wait/nudge/save recipe if the observed state supports it. Record SDK monotonic call entry/return before file serialization. Preserve failures and same-allocation pending resume; never replay a submit. Compare nested exchange and SDK spans descriptively, not as a causal performance trial. Host/model visibility and actual model token usage remain unmeasured. No sensors or Docker operations.
