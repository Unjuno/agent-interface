# Direct stdin integration plan

Plan text was issued before allocation, but its relative-path Set-Content failed
because host cwd was the parent workspace. Allocation89423 did start. This copy
is retained after setup and before the first action; do not claim a pre-start
on-disk freeze. Keep that deviation and do not restart the allocation.

Primary assistant, one WSL private calc-inkscape allocation, seed991111/991112,
max6 stages, text gap2ms, source a29a167a1. Same viewed actions as the preceding
native-combined-batch-01, supplied on stdin with no caller request files.
Immutable native request-N.json still persists. View every returned frame.
Compare requests, saved files, releases, cleanup and exchange/outer timings.
Single ordered same-seed pair; practice/order/host latency confounded. No causal
speed/token/first-useful-feedback claim. No sensors/helper model/Docker restart.
Failures retained, no ambiguous-input replay. Read-only exact final resume of
the prior run passed before this allocation.
