# Two-application primary integration

One WSL allocation on source 9a4f94417, app calc-inkscape, seed 991126,
max_stages 8 and text_gap_ms 2. This is primary-assistant integration use,
not an independent evaluator session, Docker adoption or a matched speed trial.

The primary viewed Inkscape, selected the rectangle edge and moved/saved it.
A separately authored Alt+Tab switched to Calc. The response reported
needs_review with the new Calc image; the primary inspected it before entering
the public task values 244 and 185. Save opened a partly painted format dialog.
An explicit observation returned the rendered choices. The primary selected
Excel format, received a save-progress image, then explicitly observed again.
The returned sheet showed both cells and no dialog; only then was finish sent.

All seven delivered images were viewed by the primary. There were nine MCP
calls: start, four input programs, two observations, finish and status. No
allocation restart, speculative follow-on input, automatic retry, sensor or
model delegation. Scope changes at app/dialog transitions remain needs_review,
not silently relabeled success. Existing read-only window review provided the
next decision source. No old-target-probe mode was enabled in this live run.

Saved-file evaluation passed for both apps: SVG x62,y50,width40,height30 with
no transform; XLSX A1=244,A2=185. Tracked cleanup completed and the same owner
exited 0; relay EOF exited 0. Cleanup does not claim verification of all untracked
descendants. The displayed Inkscape toolbar and saved SVG agreed. This trial
extends coverage to two applications but does not prove universal GUI support,
human speed, model-token savings or a causal pacing benefit.

The audit validates full returned image bytes, source/request linkage, binding
handoff, both saved files and terminal receipts. It does not independently
classify screenshot contents. The manifest excludes itself only.
