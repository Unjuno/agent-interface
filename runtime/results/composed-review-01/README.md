# Receipt and image in one read-only response

The assistant used `agent_review.py` on the retained seed 991072 submit report.
One WSL command returned the receipt view and an image content block. The host
forwarded the block directly to its image channel; the assistant saw the saved
XTerm text `t991072` together with independent evaluation and terminal release.
No separate `view_image` call or Windows path conversion was used.

`review.json` retains the response without duplicating base64 pixel data, plus
explicitly reported host-use metadata. The file was regenerated by the same
read-only helper after viewing; host rendering is an assistant observation,
not a machine-verified renderer receipt. Referenced raw evidence is already
retained in [composed-self-use-01](../composed-self-use-01/README.md):

- `submit/report.json`: SHA256 `63b341add03dc8c0f557e013a97b6a1efbcdf9faeb2bf28f5dcc22fc34c64587`.
- `runtime/005.png`: SHA256 `3e0cca89ed6717d6141c0ecb6e8b07ddc7f40373a26ee48471dea4e6a4169c31`, 19,853 bytes.

This is a review of a previous GUI execution, not a new benchmark or live input
result. It demonstrates a usable single-response display path. No model-token,
latency, human-speed or task-success improvement is inferred. Base64 increases
transport size; callers must provide sufficient output capacity and use the
image channel. Original report and images were not modified.

Five focused tests passed on Windows and WSL: exact encoded image bytes and
retained result, saved identity mismatch, conflicting latest observations,
missing newest image without fallback, and no observation without substitution
of a pre-action source image. PNG signature/hash checks are not pixel decoding.
