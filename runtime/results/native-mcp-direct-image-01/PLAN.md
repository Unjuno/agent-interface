# Direct image delivery trial, before allocation
Same managed MCP server and primary Inkscape action recipe, seed 991119, max stages 2. The SDK driver prints the complete CallToolResult as one JSON line; orchestration forwards its text and image content directly instead of decoding images to a file and calling view_image. Raw responses and SDK timings remain archived. Primary chooses the decision after the returned image. Do not restart on observation timeout or replay submit. No sensors, no runtime changes. This is a shell/SDK composition and not host registration. Measure the explicit image-view tool boundary removed; do not infer model token or causal latency gains from an ordered single trial.
