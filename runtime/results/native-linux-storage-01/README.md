# Linux-storage native integration stop

One primary-assistant WSL allocation, seed 991202, pinned source and pre-action plan inside evidence.tar.gz. Active artifacts were written under /tmp/agent-interface-native-linux-991202, following the storage profiling in #3503. This is a fresh integration check, not a matched benchmark against #3501.

The initial image matched the prior initial PNG hash. The primary selected an observed rectangle-edge click at [604,391]. Four visual guard checks were valid, admission accepted, and three pointer events were recorded. The final physical release check returned buttons_down=[left], verified=false. The harness returned needs_review after release_unverified and exited 1. No keyboard move/save, replay, second allocation or successful evaluation occurred. No fresh final image was claimed.

Raw cleanup reports completed and tracked_processes_terminal, while owner_exit_verified and descendants_verified remain false. The owned harness tool session was terminal with exit 1; the later process listing showed no owned experiment GUI processes. This is not a full independent process-tree audit. A separate host MCP stdio server was observed later and was not stopped or used to start another allocation.

The release path already calls X11 sync before querying physical buttons. This record alone cannot distinguish delayed event/state processing, platform behavior or another cause. The failure is not evidence that Linux storage caused an input defect, nor a successful end-to-end speed comparison. No release check was weakened and no corrective input was automatically emitted.

The archive contains the original source, action request/reply, guard/action/release records, cleanup, caller files, plan and file-hash manifest. Earlier successful use and storage-read profiling remain unchanged. See #2437 and #2942 for related release-uncertainty work. No sensor development or Docker restart.
