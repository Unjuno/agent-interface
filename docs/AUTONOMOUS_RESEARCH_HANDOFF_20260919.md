# Autonomous research handoff — 2026-09-19

## Frozen baseline

- Latest observed main: `6ee6ee4de375d89b266b57070de96e516af7d398`.
- Canonical direction: integrate one usable desktop path from setup through observation, guarded action, result, and bounded recovery (Issue #57 / `docs/CURRENT_GOAL.md`).
- Release decision remains `HOLD_RESEARCH_PREVIEW_NOT_READY` (Issue #58; `release/RESEARCH_PREVIEW_READINESS_20260919.json`).

## Roadmap state

1. **Actual interaction loop:** partial. Persistent asynchronous delivery, bounded held inputs, stale-state handling, model-in-loop measurements, and human-tempo comparison remain open.
2. **Observation gating:** O0/O1/O2 scoped evidence exists; O3 relevant-region gating and O4 local VERIFY remain open.
3. **Guard/speculation:** guard-cost policy, automatic guard selection, and longer focus/modal/replacement sessions remain open.
4. **DOOM milestone:** basic integration exists, but ordinary-speed threat control, synchronized video, matched comparison, and reproducible distribution remain open.
5. **Runtime preview:** packaging/checksum/quickstart exist as tooling, but supported-host acceptance, fresh golden run, smoke, and audit-live evidence are not all closed.
6. **Production stabilization:** frozen executable semantics, broader backends, and model-in-loop cross-app measurements remain open.

## H/T/D/C/U ledger

- **H:** Preserve rich-model intent; local mechanisms may refine, verify, invalidate, or yield only on fresh bounded evidence.
- **T:** Prefer one additive successor at a time; use container-first deterministic construction before any live/model allocation; freeze source, seed, arm order, and stop rule before formal work.
- **D:** Retain raw result, failure/STOP, source and blob identities, container/image digest, audit output, and exact invocation counters. Current retained boundaries include MAP01 task-effect admissibility, golden lifecycle mapping, bounded critical-event delivery STOP, and release readiness.
- **C:** PASS is scoped to its exact contract. No component result promotes to runtime, GUI, model, latency, human-tempo, or product claims without a separate source-backed gate and independent audit.
- **U:** Live watcher-to-model event delivery, ACK/expiry/recovery, source-backed golden result provenance, CLI adapter implementation, supported backend, packaged smoke, DOOM video, O3/O4 transfer, and cross-domain task value.

## Collision and integration map

The following open PRs already own the golden/CLI lifecycle area; do not duplicate their paths or branches: #2260, #2270, #2273, and #2277. The bounded critical-event successor is integrated as PR #2257 and remains a STOP because no live watcher/model was available. Closed synthetic adapter results (#2209/#2213) remain historical controls and are not revived.

Verified work should land through a non-draft PR from a fresh branch based on current main. Retained failures and STOPs remain immutable; any revisit receives a new successor Issue/path.

## Next bounded action

Wait for the lifecycle successor set to produce source-backed contract evidence, then update the readiness matrix from evidence only. The next live experiment must connect a real watcher and model-facing delivery path before claiming event usefulness or recovery.
