# Predicate specialist versioned switch v1 — Issue #4284

Allocation: `predicate-specialist-switch-4284-20260923-01`

## H
A tiny predicate specialist can replace GENERAL only after a frozen shadow-validation window and exact version/support/producer binding. Novelty, support or producer generation changes, stale activation receipts, required UNKNOWN, and directed disagreement must fail closed to GENERAL/UNKNOWN before graph consumption. A separately validated v2 may later activate only under a distinct immutable activation receipt.

## T
Authority-neutral deterministic lifecycle fixture. Three policies over one frozen 24-row timeline: ALWAYS_GENERAL, UNGUARDED_SPECIALIST_SWITCH diagnostic comparator, SHADOW_VALIDATED_VERSIONED_SWITCH candidate. General truth is authored independently from specialist tables. v1 and v2 artifacts are immutable. Minimum promotion window=3 consecutive eligible shadow agreements. Active specialist rows avoid GENERAL calls except explicit validation/invalidation boundaries. No GUI/model/provider/input.

## D
PASS_VERSIONED_PREDICATE_SPECIALIST_SWITCH_SCOPED only if: candidate never activates before 3 eligible shadow agreements; active candidate preserves oracle predicate+graph semantics; novelty/support-version/producer-generation/stale-receipt controls fall back before incompatible specialist consumption; required UNKNOWN stays UNKNOWN; directed v1 regression blocks/invalidates activation; v2 recovery creates a new activation identity; candidate uses fewer GENERAL calls than ALWAYS_GENERAL and at least 4 stable-active rows are specialist-served; authority=false everywhere; raw-only audit errors=[]; >=10 coherent mutations reject; formal1/reruns0/replacements0/tuning0.

## C
Deterministic authored lookup specialists and a synthetic lifecycle can make invalidation clean. This tests lifecycle/switch semantics, not learned-model quality or natural drift frequency.

## U
No live task authority, production deployment, model/token benefit, cross-app/platform generality, or automatic promotion claim.
