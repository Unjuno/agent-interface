# Excluded construction record

- construction-01: all 5 cases stopped before formal evidence because Python-Xlib attempted to read a missing default `~/.Xauthority`. No formal row was produced. Construction was changed only to provide an allocation-owned empty Xauthority for the private `-nolisten tcp` Xvfb.
- construction-02: fresh/guard/refuse paths passed; both stale recovery cases exposed that Tk/X11 `Control-a` moved the insertion cursor rather than selecting all text, so BackSpace left the wrong-field `7`. No formal row was produced. Recovery was narrowed to click the right edge of the one-character wrong Entry then BackSpace, followed by current-A click and `7`.

Neither construction outcome contributes timing/cost rows. Scientific population, state schedule, policies, cost endpoints, censor horizon and decision gates are unchanged.
