# Deterministic GPU photometric grounding — successor #4470

## H / T / D / C / U

**H.** A separable pair of fixed pooling matrices reproduces the forward values of the previous `adaptive_avg_pool2d((8, 10))` layer on the CNN's 16x40x25 feature map, while its backward is accepted by deterministic CUDA and yields bit-identical repeated gradients. If the construction gate passes, the unchanged two-arm brightness comparison can complete on this PC's RTX 3080 without relaxing deterministic mode.

**T.** Source intake is `9e3ee7ea02b27e935a50130a0be45e9938a4dfdb`. Verify `research/analysis/local_model_2912_image_manifest.json` and all six PNG byte hashes before formal execution. Train on complete source images task-1, task-2, task-4 and task-5; hold out task-3 and task-6. For a source dimension `n` and pooled dimension `m`, each output row `j` averages indices `[floor(j*n/m), ceil((j+1)*n/m))`. The height and width matrices are applied by `torch.einsum("oh,nchw,pw->ncop", Ah, x, Aw)`. This is the only model-operator change from #4463. CNN layers, initialization, optimizer, schedule, augmentation, evaluation transforms, confidence route and validator stay fixed.

**Construction gate, excluded from the one formal allocation.** (1) Candidate output shape is 16x16x8x10 and maximum absolute difference from CPU `adaptive_avg_pool2d((8,10))` is <=1e-6. (2) CUDA forward/backward completes with deterministic algorithms enabled and `CUBLAS_WORKSPACE_CONFIG=:4096:8` set before interpreter startup. (3) Two repeated CUDA probes produce bit-identical outputs and input gradients. (4) All six source image hashes validate; strict compiled-form candidates for both layouts pass, and an out-of-bounds mutation is rejected. (5) The raw-only auditor accepts a clean synthetic fixture, rejects >=10 effective corruptions, refuses to call aggregate improvement a pass when any perturbed case regresses, and reports accepted wrong coordinates as FAIL. Any failed gate is recorded before formal; no model training follows a failed construction gate.

**Formal allocation.** One local host-GPU process, fixed order: no augmentation then brightness augmentation. Each arm uses the same CPU-created initialization (seed 3926), 4 source rows, balanced batch-size-16 schedule, Adam at 0.001 and 300 optimizer steps. The augmented arm uses per-example multiplicative factors sampled uniformly from [0.70,1.30] during training only. Evaluate original, 0.85x and 1.15x brightness for each of the two untouched held-out images (six cases per arm). Persist logits, predicted layout, fixed route (accept >=0.75; yield [0.25,0.75); reject <0.25), compiled candidate, strict validator output and exact-coordinate comparison. Retain the source, model-code, environment, schedule, optimizer-step, wall-time and peak-VRAM identities. Run the raw-only auditor once against the one formal result. Exactly one formal invocation; no retry, replacement, operator change, nondeterministic fallback, threshold change or tuning.

**D.** `PASS_PHOTOMETRIC_AUGMENTATION_SCOPED` requires all frozen source/audit gates, all 12 candidates accepted by the strict schema/bounds validator, zero accepted wrong coordinates in either arm, augmentation no worse in each of the four perturbed held-out cases, and a strict improvement in at least one. An integrity-valid result without that benefit is `HOLD_NO_SAFE_BENEFIT`; any accepted wrong coordinate is `FAIL_ACCEPTED_FALSE_GROUNDING`. Missing source, deterministic execution, result, audit or process evidence is STOP/HOLD.

**C.** Windows host Python 3.11 / PyTorch 2.5.1+cu121 / CUDA 12.1 / RTX 3080 Laptop 16 GiB. Deterministic algorithms on, cuDNN deterministic, benchmark off, TF32 off, CUBLAS workspace `:4096:8`. Use the already-installed host GPU stack. Do not download/build a multi-gigabyte CUDA image for this small run; the result is host-local and makes no container claim. Check at least 1 GiB free on C: and 2 GiB free VRAM before the one formal invocation. No provider/network, GUI, input, task submission, authority grant or runtime promotion.

**U.** Two held-out original source images, one per known layout; four brightness-shifted cases are not independent source samples. No unseen-layout generalization, calibration, live GUI robustness, useful-control or product claim.

## Exact acceptance and reporting

The independent auditor verifies the canonical source manifest and image bytes, input transforms, source/runner/freeze identities, all routes and candidate coordinates, and the *per-case* no-regression gate. At least 10 effective structured raw-result mutations must reject. Results are additive under `research/analysis/gpu_photometric_2912_v3/`; #4463's CUDA STOP and all #2912 predecessors remain unchanged.
