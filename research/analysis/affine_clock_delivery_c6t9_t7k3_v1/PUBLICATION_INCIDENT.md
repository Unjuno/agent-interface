# Retained publication transcription failure

Before any result-tree commit or branch update, the first assembled study tree
`2384fe2b116ccd938bcc693668c2c9b8c1358b1f` failed the locally computed tree-ID
check. The mismatch was solely the submitted CAPSULE.json:12 per-part SHA-256
values were incorrectly transcribed, and the ZIP compatibility note was omitted.
This was an assistant transcription failure, not GitHub corruption, a safety
block or a change to any scientific observation/source/gate.

The rejected3237-byte metadata blob is retained ONLY as
`publication_incident/REJECTED_CAPSULE.json`, Git blob
`17dc77c99eea2ea13c6bc1caf4d748aa356e40eb`. Do not use it for restoration.
No original source or binary part differed: their independently computed Git
objects matched. No bad study tree was committed or attached as the branch's
scientific evidence.

The active3330-byte CAPSULE.json is the original locally verified metadata,
Git blob `2fbba33f3c4dc80e50e6ca180e2f662e825c766b`. Registration was corrected
from its actual bytes, then checked again. Scientific corpus reruns, source
changes, result replacements and new GUI/model calls remain zero.
