# Successor #2018: read-only semantic selection identity

## H/T/D/C/U

- **H**: A public read-only semantic receipt can distinguish a stable selection from a pixel-identical replacement while remaining non-authoritative.
- **T**: Deterministic controlled fixture comparing `VISUAL_ONLY`, `SEMANTIC_ONLY`, and `VISUAL_PLUS_SEMANTIC_CONSISTENCY` across stable, replacement, decoy, focus, unavailable, stale, mismatched, conflicting, and malformed records.
- **D**: `run.py` uses no GUI, click, selection, refresh, or input API; identity, application, and generation are checked; unavailable/contradictory data fails closed.
- **C**: Positive result is limited to this fixture and protocol. It does not establish OS accessibility reliability, latency, or effect completion.
- **U**: Real application semantic-source availability and behavior remain unverified.

## Result

Run exactly once with the standard library. The fixture must report zero authority and task-input events. The old screenshot-only rejection in #341 is not altered.
