# Independent audit

The audit checks that every required negative control is fail-closed and that every row has zero authority/task-input events. No real app, screenshot threshold, ROI tuning, or privileged API is used.
