# #1900 retained source-transport hold

The research-side bridge, formal harness, auditor and exact caller blob identity were frozen before any scientific execution.

Formal execution did **not** begin. The available local container has no GitHub DNS/checkout; the external containerized job runner returned HTTP 402 before execution; the platform raw downloader could not materialize the branch source; and a fallback manual base64 transport failed a byte-length integrity check before source reconstruction completed.

Because the preregistration requires the unchanged caller blob `7faf042304728ce91a3e4f89d465b251ea0bf70d`, continuing with hand-transcribed or non-identical caller bytes would invalidate the experiment. The correct disposition is therefore `HOLD_SOURCE_TRANSPORT`, with scientific formal invocations = 0.

No caller/runtime file was modified. No PASS/FAIL conclusion about the bridge is claimed.

A successor may change only exact-source materialization/execution plumbing. It should reuse the frozen H/T/D/C/U, bridge semantics and caller blob, and execute from an environment that can materialize exact Git objects without manual transcription.
