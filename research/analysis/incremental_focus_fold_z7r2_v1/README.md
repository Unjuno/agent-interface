# Incremental focus-state fold — retained offline contract and CPU study

**PASS_INCREMENTAL_FOCUS_FOLD_CONTRACT**, with a separate **PASS_LOCAL_REDUCER_CPU_GATE** on the frozen synthetic growing-history workload. Neither is a native-acquisition, GUI-task or production result. See REPORT.md, PLAN.md (H/T/D/C/U, variable table, conditional proof), FIRST_OUTCOME.json and PUBLICATION_STATUS.json.

2428 case histories /24972 decision boundaries, including1400 unchanged original decisions from40 previously captured streams. New X11/model runs0. Thirty fresh paired timing workers, five per length/batch cell, followed one source-first local freeze. New public registration/PR/merge are unavailable in this session; draft files are not posted Issues or PRs.

The constructor retains fixed context and a constant number of projection fields. `poisoned` means malformed prior history; this is distinct from temporarily unknown focus state, which a subsequent valid focus/keymap may resolve. All output authority fields remain neutral. Inherited TYPE is an observation label, not permission to emit input.

Read-only reproduction:

```sh
python -S -B verify.py
```

This verifies the member manifest, reproduces the raw-only audit and14 effective corruption-control outputs, and runs16 pure unit methods. It does not call execute.py, evaluate.py, run_stage.py, any native library or any GUI. Do not rerun consumed formal stages. A new live/adoption test needs a distinct allocation, scope and ownership check.

Complete old n6k4 delivery belongs to #4405. This study includes only the40 original block files and exact pure reference/source identity needed for independent replay; it does not claim to replace that publication. The stopped q8v6 piggyback construction is in a separate archive/path; synchronization work belongs to #4389. No blocked foreign publication was imported.