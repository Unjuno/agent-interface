# Delivery note — 2026-09-26

The restored original study contains historically true `STOP_GITHUB_WRITE_ACTION_NOT_EXPOSED` text from the conversation in which the experiment ran. That historical publication status is preserved byte-for-byte inside the archive.

This later continuation has GitHub write capability and created Issue #4420 plus branch `research/incremental-focus-fold-z7r2-20260926` solely to deliver the same retained scientific bytes. It does not retroactively turn the local freeze into public preregistration, does not change any scientific row or gate, and does not rerun the consumed allocation.

The governing current publication state is the Git history/PR around this delivery; the old `PUBLICATION_STATUS.json` remains provenance, not a current capability claim.