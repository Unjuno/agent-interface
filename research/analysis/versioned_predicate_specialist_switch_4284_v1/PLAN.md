# Versioned predicate specialist switch — #4284

Allocation: `versioned-predicate-specialist-switch-4284-20260923-01`.
Parent result: #4218 / main `14cfdf1a...`; exact v1 specialist table is reused, not retrained.

## H
A specialist may replace GENERAL only after a frozen four-row zero-disagreement shadow window bound to specialist/support/producer/schema identity. Identity change, novelty, stale activation, or regression must fall back to GENERAL/UNKNOWN before the guarded graph consumes an incompatible value. A separately validated v2 may activate only under a distinct immutable activation identity.

## T
Authority-neutral deterministic lifecycle fixture; stdlib Python only. Policies: ALWAYS_GENERAL, UNGUARDED_SPECIALIST_SWITCH diagnostic, SHADOW_VALIDATED_VERSIONED_SWITCH candidate. Ten required Issue schedules, two formal repetitions each = 60 formal cases in one invocation. Construction uses one separate repetition and is excluded. Same exact target-pair oracle/guarded graph semantics as the #4218 predicate boundary; no #4218 formal row is reused as outcome data.

Shadow minimum = 4 consecutive same-identity exact agreements. Stable-active sequence has 16 rows total, so candidate GENERAL-call ratio must be <=0.30 versus ALWAYS_GENERAL (expected 4/16=0.25). Formal source/gates are immutable after GitHub readback.

## D
PASS_VERSIONED_PREDICATE_SPECIALIST_SWITCH_SCOPED requires candidate semantic+graph equality on all rows; no premature/regression activation; novelty fallback; support/generation/stale-receipt invalidation before specialist consumption; UNKNOWN preserved; distinct v1/v2 activation identities; stable GENERAL-call ratio <=0.30; authority=false throughout; raw-only audit errors=[]; >=10 coherent corruptions rejected; formal1/reruns0/replacements0/tuning0.

## C
Lookup specialist is trivial and lifecycle overhead may exceed its real value. The fixture has clean authored novelty/version changes. Unguarded comparator is intentionally unsafe.

## U
No OS input, live model, natural drift, provider cost, task effect, token/product or production deployment claim.
