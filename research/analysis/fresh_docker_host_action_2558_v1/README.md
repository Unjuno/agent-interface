# Fresh Docker host-model action (#2558)

This is the first fresh model-in-loop action for the local Docker route. The
Docker X11 fixture produced the observation, the PC-local `codex.exe` boundary
returned compiled grounding, and an independent geometry gate admitted the
field point. The native action then produced the independently scored exact
effect `saved=true, text=docker2558`.

The post-task focus-change probe attempted reuse of the old field handle and
was refused with `SCOPE_MISMATCH` and zero additional emissions. Model output
remained non-authoritative throughout.
