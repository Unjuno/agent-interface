# Independent audit

The runner must report 16 rows, zero coordinate-map failures, zero raw-fallback violations, zero model invocations, and zero GUI mutations. The hold disposition is required when no multimodal evaluator is present.
