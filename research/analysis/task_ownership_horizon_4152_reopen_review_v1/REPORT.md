# #4152 reopened-issue review — bounded model-facing discriminator

Date: 2026-09-26  
Base: `46809003dd5e28c2277b62103fe24d3cd1e67ca6`

## H / T / D / C / U

**H.** The reopened #4152 question has no distinct model-facing discriminator beyond existing planner-gap/policy-cache/resident-program semantics unless a concrete live caller shows an observable difference.

**T.** Read the current #4152 issue and comments plus #1024, #1155 and #732; inspect retained #4152 experiment outputs, current main files and repository state. No new execution or model call was authorized by this review; it reuses public issue text and committed predecessor evidence only.

**D.** `HOLD_NO_NEW_DISCRIMINATOR`: #4152's retained 12-case private-X11/Tk result already reports `PASS_TASK_OWNERSHIP_HORIZON_SCOPED` (stable: 3 vs 1 effects in each of 3 reps; invalidation: one effect and no second click in every explicit-horizon case). Its own limits say it used cooperative fixture truth and an artificial planner delay, and did not establish model comprehension, tokens/latency or production integration. The issue's later administrative reconciliation states that #1024, #1155 and #732 already encode the ownership/authority split and requires a concrete unexpressible behavior before another experiment. Current public issue bodies confirm these abstractions. No new branch/path collision was found for this review; this branch and path are additive.

**C.** A genuinely model-facing task may reveal semantics that an authored Tk token cannot represent; conversely, another scripted delay or duplicate policy lifetime would not answer the reopened question.

**U.** This review does not establish that the abstractions are equivalent in a live caller, quantify model cost, or close #4152 / roadmap work. The next valid successor requires a named production caller/fixture where the frozen same task, current evidence and admission boundary can run both existing-envelope/cache behavior and proposed ownership behavior, with actual model receipt/tokens and independently scored effects. If no such caller exists on main, first integrate/select that caller; do not inflate this into a large synthetic benchmark.

## Provenance and scope

- Main was fetched from GitHub and is `46809003dd5e28c2277b62103fe24d3cd1e67ca6`.
- Existing experiment is in `research/integration/task_ownership_horizon_4152_v1/`; its raw result and report were read locally. No predecessor file was changed.
- Current issue text was fetched via GitHub MCP for #4152, #1024, #1155 and #732. The #4152 comment history includes the original 0/12 freeze record; the current issue body retains its subsequent 12-case PASS summary and administrative reconciliation.
- Worktree was reused only after verifying clean status and switching it to current main. Other worktrees and their untracked files were left untouched.
- No container/GPU/model call was needed for this read-only discriminator review.
