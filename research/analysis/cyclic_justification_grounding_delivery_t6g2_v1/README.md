# Cyclic justification grounding delivery (retrospective)

This additive directory publishes the already executed conversation-local `cyclic_justification_grounding_t6g2_v1` study. It is **retrospective evidence delivery**, not a new GitHub preregistration or a rerun.

Local result: `PASS_LOCAL_CYCLIC_GROUNDING_CONTRACT`. 27,237 finite graph/retraction cases; candidate `CLEAR_AND_REDERIVE` matched a separate all-closed-model enumerator on every case. `LOCAL_SUPPORT` produced 2,645 false-retain cases; `BLIND_INVALIDATE` produced 1,306 false-invalidate cases. Frozen audit: 700,837 checks, errors=[], 10/10 effective evidence mutations rejected.

## Scope

Positive finite OR-of-AND justification rules, fixed complete dependency declarations, evidence deletion only. Cycles are allowed. No negation/defaults, probabilistic support, concurrent mutation, source authenticity, model/GUI/task effect, runtime promotion or performance claim.

## Exact retention

The readable source/plan/proof/report/result files in this directory are copied byte-for-byte from the local study. `capsule_parts/` reconstructs a deterministic XZ tar containing all 38 direct study files except one CPython bytecode cache (`source/__pycache__/audit.cpython-313.pyc`), which is non-source derived interpreter output and remains in the original conversation ZIP. No scientific result file is rewritten.

Capsule SHA-256: `c83b722a8049907ffb7d372c692bd2a1b1c4ba3e7b8d6d3db5c7f8dbd2a21c42`; bytes: 1169468.
Original handoff ZIP SHA-256: `48d5422067e212ebc67aaf3febc9af65fa09a2b61cd1413ad246df5e9f4b22d0`; bytes: 1380978.

Run `python -B verify_publication.py` to reconstruct the retained files into a fresh temporary directory and rerun only the frozen raw auditor/controls; it must not invoke the scientific matrix.
