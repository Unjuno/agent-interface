# Action-conditioned routing repair successor #2095 r2

Current-main reconstruction of #2095 after the predecessor PR became non-mergeable. The predecessor remains unchanged. This additive fixture repairs malformed-intent coverage and verifies raw evidence retention from the router's returned object rather than appending the field in the harness.

The fixture is deterministic and standard-library-only. Container execution was unavailable in the originating run; the retained result is explicitly local-only. No model, GUI, network, runtime, latency, token, or user-input claim is made.