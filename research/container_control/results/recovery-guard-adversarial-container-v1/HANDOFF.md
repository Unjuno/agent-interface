# Handoff

This branch is construction/evidence only and does not own a live MAP01 allocation.

Do not merge or reinterpret it as a change to the frozen recovery matched experiment. Its only architectural finding is that a source-bound directional/displacement guard can remain fresh while continued actuation is locally counterproductive in a synthetic model.

Next non-overlapping worker task: transfer a task-relative stop/deadband condition into the existing rendered container/X11 bounded-recovery fixture, without changing the formal MAP01 allocation. Require at least one naturally exposed stop event, balanced app-observed key transitions, terminal empty input, zero stale re-press before planner return, and the existing visual-decoder error gate. Retain failure.

Do not tune the deadband on live MAP01 outcomes. Any MAP01 version must preregister its domain-specific semantic postcondition independently.
