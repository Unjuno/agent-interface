# Actual Mindustry construction using the shared cause-servo runtime

The assistant used unchanged mindustry_bend_interactive_v2, the same cause_servo_session_v1/executor_v8 as the desktop candidate, in a fresh private Linux/Xvfb process. The existing pinned save, empty-route mod, eight direction-specific targets and three-phase scorer were retained. No controller oracle projections were read before finish. This extends the preceding scripted cancellation probe to actual screenshot-guided construction.

Four accepted programs selected the basic conveyor, planned the five adjoining directional drags, resumed construction, then paused and reviewed. The assistant viewed image sequences1,4,11,14,18; final frame showed the bent route carrying copper and HUD294. The GUI prediction and final image hash were recorded in the runtime command stream before finish. Subsequent independent evaluation verified all eight placements/directions, the112-tile surrounding guard, source/core preservation and an unchanged post-control layout. Copper200 initially became294 after control and343 after a separate600.024-tick no-input window: **49 delivered copper**, excluding construction costs and delivery during control.

The independent audit verifies pinned sources,18 exact transported/reconstructed frames, four completed admission/terminal pairs, release verification, historical input-state samples and final empty owned-button/key samples, pre-finish GUI commitment, no admissions after finish, eight score controls and cleanup. Runtime process exit0 was observed; all owned processes exited and the canonical save was unchanged. No recovery or rejection occurred in this episode. Results: results/mindustry-bend-v2-self-use-01; replay: audit_mindustry_bend_build_v2.py.

| Program | Acceptance to first image ready (ms) | Acceptance to terminal (ms) |
|---|---:|---:|
| Select |324.054|807.443|
| Plan bends |573.914|3697.462|
| Build |235.242|835.362|
| Pause/review |301.852|1018.021|

Initial capture to final input terminal was84.967 seconds; to independent evaluation117.358 seconds. Between-program gaps were20.274,18.027,22.882 seconds. Image-ready is a runtime artifact timestamp, not model receipt or verified useful-feedback time. Model tokens/cost/receipt are unmeasured. Raw PTY output is verbose and sometimes truncated; authoritative events and referenced images were read from the same running session without restarting or resubmitting. The previous roughly300-second old-runtime episode had a large context/host gap, so the difference cannot be attributed to this backend. No controlled speed comparison or human-tempo claim is supported.

This was a known route with task-supplied coordinates and earlier calibration/self-use experience. It does not establish held-out real-time planning. Basic newer-runtime gameplay compatibility is now supported; stop repeating the same successful route. Next connect Mindustry to the existing private socket/pending-event interface and exercise a controlled focus loss, with same-handle recovery and explicit model-boundary timing. Reduce the18–23-second interaction gaps through evidence-backed delivery/decision changes, not by relabeling local image-ready times as end-to-end performance. Keep DOOM, desktop and other domain gaps in the coverage matrix.
