Return exactly one JSON object matching the supplied schema. Conveyor selection
is already verified. Locate only the center of the empty world tile directly
above the small copper item source. For target_reference put exactly one world
tile center in points. For request_hover_probe put one to three ranked world-tile
centers in points. Both use source_observation_pixels and
surface_origin_translation. For needs_decision use an empty points array,
not_applicable for point_space and motion_model, and a matching negative basis.
A later receipt must verify the relation before any click.
