# Assistant-driven recovery after actual Mindustry focus loss

The assistant used the existing private socket v1 and focus fixture v1, which does not restore focus during control. The fixture injects one actual X11 focus transfer after drag button-down; it restores its original window only during cleanup. The new mindustry_exchange_v1 utility performs one explicitly supplied request, persists the full request/reply and local exchange times, and prints selected fields to keep terminal output manageable. That console summary is intentionally incomplete; it is not the lossless composition format and is not evidence of token savings. The original full reply remains in each stage directory.

The assistant viewed initial image1, submitted a bounded drag with a trailing click, received input_stopped, then read the same action through terminal without resubmission. Images2/3 showed an inactive game title bar; observation state had sink focus and null pointer binding. Based on image2 and the state evidence, the assistant requested one Alt+Tab chord with a fresh lease tied to sequence3.

Alt+Tab returned focus to the original game window. The common executor reported needs_decision/focus_changed for this intentional focus transition and suppressed the trailing observe step. Its two passive observations provided sequences5/6 with the original game focus/surface/geometry. The assistant viewed image5, then sent a new pointer program based on sequence6 to select the conveyor menu. Image9 showed the selected Conveyor label and copper200; the program completed. No test-fixture focus restoration occurred before those observations/actions. Thus program needs_decision can coexist with successful intended window switching; outcome must be assessed from new evidence before granting a fresh action.

A pre-finish GUI judgment recorded recovered focus, successful menu selection and expected empty-route failure. Independent scoring subsequently returned CONTRADICTED with zero delivery, as expected; no construction was attempted. Bridge and finish-client exited0, all owned processes exited and canonical save remained unchanged. Audit verifies45 events, nine explicit exchanges, nine exact frame reconstructions, both suppressed tails, original window binding restored before the fresh pointer action, independent score and cleanup. Runtime sources were pinned at launch; helper/fixture hashes are also retained in a clearly postrun inventory, not a preregistered experiment.

Fault terminal to refocus terminal was45.789 seconds; fault terminal to successful pointer terminal72.282 seconds, including host reasoning, image inspection and tool orchestration. These figures demonstrate substantial recovery overhead. Local exchange timing files exclude model receipt, reasoning and token cost. This single known injected fault does not establish broad focus recovery or live human tempo. Results: results/mindustry-focus-self-use-01; auditor: audit_mindustry_focus_self_use_v1.py.

Next improve planner-facing recovery handling and measure actual model input/output boundaries rather than repeat this known focus-switch case. A useful next comparison should preserve the observed distinction between action termination, intended focus effect, historical observation and fresh input authority while reducing avoidable clock/read/tool round trips. Do not automatically replay the old drag or classify intentional focus change as ordinary completed input.
