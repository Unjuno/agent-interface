# Mindustry integration with cause-servo runtime

mindustry_bend_interactive_v2 retains the pinned save, empty-route build mod and three-phase direction/delivery scorer, and replaces the old session9/executor3 combination with the same cause_servo_session_v1/executor_v8 used in desktop experiments. The stale six-conveyor prose is corrected in this new version only. Launch manifests conservatively hash all live_control Python sources, including unused modules; this is an overinclusive source inventory, not a minimal dependency graph.

probe_mindustry_runtime_cancel_v1 ran an actual private Linux/Xvfb Mindustry process. This scripted integration probe scheduled a five-second drag with a trailing conveyor-menu click, requested cancellation after step start plus150ms, then submitted a fresh observe-only program. Actual evidence confirms pointer movement to the first point and acknowledged button-down before cancellation. Only those two pointer admissions exist: no drag endpoint or trailing click was admitted. The five-second wait was interrupted. This is not assistant gameplay or a focus-loss experiment.

All22 runtime events match the driver's raw stdout. Both exact transported images reconstruct; recovery reuses the identical original PNG with a fresh sequence and input-state samples. The assistant subsequently viewed that screenshot, which shows the paused empty fixture and copper200. Recovery samples report no owned keys/buttons. Cancel-to-owner-verified-release was2.539578ms and cancel-to-terminal14.198562ms. Recovery acceptance to image-ready was68.471991ms. These are single local runtime measurements, excluding model receipt, reasoning, token cost and transport into a model; no comparative speed claim is supported.

The independent game score correctly remains CONTRADICTED: eight missing targets and zero copper delivery across600.185 ticks. Integration success does not imply task success. The canonical save remained unchanged, all owned processes exited and the driver/runtime ended with exit0. Source pins, exact frames, admissions, cancelled terminal with per-lease cause, absent tail, observe-only recovery, independent score and cleanup are checked by audit_mindustry_runtime_cancel_v1.py. Results: results/mindustry-runtime-cancel-01 and results/mindustry-runtime-cancel-driver-01.

This closes basic game backend compatibility and explicit-cancel/re-observation evidence. It does not yet validate the input_stopped/two-passive-observation path, which is specific to focus/surface interruption, or the socket pending/read/resume path. Next use this version for actual bounded construction and a controlled focus interruption through private socket transport, preserving the same action handle after partial observations. Do not promote the runtime based on one scripted cancellation.
