# Bent-route construction through actual shared pointer input

The assistant constructed the calibrated eight-tile route in a fresh Linux/Xvfb Mindustry GUI using screenshots and the shared session_v9/executor_v3 pointer interface. The unchanged player-ready build mod authored the source and empty construction area, not the conveyors. Required directions were E,N,E,E,S,E,E,E in the existing pinned bend plan. Four accepted programs selected the basic conveyor, planned five adjoining directional drags, resumed construction, and paused for review. No input program was rejected or replayed.

The assistant viewed sequences 1,4,11,13,18. The final screenshot showed a paused completed route with copper along both bends and HUD copper256. A GUI judgment and image hash were committed into the runtime event stream before finish. Only after closing control did the separate engine measurement become available. Independent scoring verified all eight targets and directions, the 112-tile surrounding guard, source/core preservation, and unchanged layout across the delivery window. Copper200 initially became256 after control and302 after the no-input window: **46 delivered copper over600.674 ticks**. Construction costs and delivery during control are excluded from this delivery delta.

The entry and combined scorer were written before this episode. The new combined score uses validated three-phase delivery measurement plus explicit direction checks; prior measured entries remain unchanged. A copied manifest prose field still says six eastward conveyors: this is a retained metadata defect, not the executed task. The ready event, pinned eight-target plan, commands and resulting score establish the actual contract. Correct that field only in a future version; do not rewrite measured source hashes.

Results are in results/mindustry-bend-self-use-02. audit_mindustry_bend_build_v1.py verifies pinned sources, all18 exact reconstructed image frames, four admission/terminal pairs with released input, pre-finish GUI commitment, no further admissions after finish, cleanup and eight score controls. A wrong direction remains contradicted even with positive delivery; invalid NaN measurement and incomplete construction remain UNKNOWN. The known episode and local final-state guard do not establish an adversarial or global causal verifier.

The first launch, results/mindustry-bend-self-use-01, reached its initial screenshot and exited when non-PTY stdin closed; it accepted no actions and cleaned up. A fresh PTY launch was made after authoritative process exit. During the successful launch a long/truncated response was recovered from the same process/event log; the program was not resubmitted. Both owned GUI/X sessions exited and the canonical save remained unchanged. Final live process exit was0.

Initial capture to final input terminal was299.530 seconds; capture to evaluation354.894 seconds. Between-program gaps were33.292,219.728,15.152 seconds, including host reasoning/context/tool handling. This is not a low-latency result or a valid speed comparison with the earlier straight-route episode. Model receipt, tokens and cost remain unmeasured. The case was known from engine calibration and coordinates were supplied; no held-out planning claim is made.

This adds changed geometry and directional placement to Mindustry coverage; it is not broad multi-object planning or benchmark adoption. The main cross-domain gap remains that these game pilots use an older pointer runtime, while desktop interruption/evidence work uses the newer stopped-input runtime. Next prioritize transporting one bounded game action through that runtime with observation-only recovery, then a controlled interruption or changed-route case. Avoid repeating this successful known route merely to accumulate passes. DOOM remains one orthogonal stress domain.
