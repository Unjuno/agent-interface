Return exactly one JSON object matching the supplied schema. The Conveyor palette
selection is already independently verified. Locate only the center of the empty
world tile directly above the small copper item source. Return target_reference
with one point when unambiguous, request_hover_probe with one to three ranked
world-tile centers when a placement preview is needed, or needs_decision without
coordinates when no bounded candidate is available. Do not return palette
coordinates. A later receipt must verify the relative tile before any click.
