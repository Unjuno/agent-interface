# A bounded menu intent survives model delay through fresh local validation

The preceding live boundary probe repeatedly observed instead of input once a1000ms age budget expired. This candidate demonstrates one path to progress: retain a bounded intent, acquire a new observation after the model response, compare the expected target locally, and submit one newly admitted action. It does not replay an interrupted action or renew the old lease.

A fresh private Linux/Xvfb Mindustry process ran unchanged interactive v2/socket v1. The model was explicitly asked to return the fixed select_basic_conveyor/execute_once proposal; no autonomous target selection, planning or image interpretation is demonstrated. Luna/low returned that proposal in one real CLI invocation, with12033 input,9984 cached input and34 output tokens. The model invocation is real but essentially schema compliance. The old observation was6969.839ms old after the answer.

live_menu_intent_v1 then acquired a new observation. menu_intent_guard_v1 checked increasing sequence, matching non-null window binding, empty owned-input samples, matching image dimensions and byte-identical known menu patch at985,551–1032,603. The patch/point are fixture-specific constants. Fresh age was checked against1000ms, and the submitted runtime deadline was fresh_capture_ns+1000000000, so the existing owner enforces expiry on admission. This is a new program/lease against the new sequence; the original observation remains evidence for the intended patch, not current input authority.

The guard allowed one click at1008,578. Button-down acknowledgment occurred358.056ms after fresh capture; terminal completion927.254ms after capture. All recorded move/button-down admissions were before the deadline and release was verified. The assistant subsequently viewed image005, which showed Conveyor selected and copper200 while paused. Independent route scoring correctly remained false: selection was the requested effect, not construction. Both bridge/orchestrator ended0; all owned processes exited and the canonical save remained unchanged.

The audit covers35 events,9 socket exchanges,5 exact transported frames, source/prompt hashes, one no-tool model reply, matching local guard evidence, one input program and cleanup. Five offline controls reject stale age, changed binding, reused observation, changed patch and unsupported intent. These are not live refusal tests. Results: results/live-menu-intent-01; audit: audit_live_menu_intent_v1.py.

This is not a general semantic target verifier. Matching pixels/window identifiers can persist while hidden meaning changes; only the local sampled patch is checked. The sample/input gap is not atomic. The Python intent dictionary equality also does not strictly distinguish boolean true from numeric1, and malformed trusted-observation fields can raise rather than return a structured refusal. These limitations matter before general integration. The driver is a bounded research helper, not a production input contract.

This successful execution differs from the prior observe-only probe in task, model prompt and timing; do not claim a controlled latency improvement or human tempo. The subsecond values describe only the post-model local segment. Next test actual target/focus change between proposal and revalidation, preserving the model proposal and proving zero admissions on refusal. Also validate intent schema types before accepting untrusted outputs. Prefer demonstrating refusal plus successful progress under changed conditions over repeating this known unchanged menu.
