Return exactly one JSON object matching the supplied schema. The current subtask
is only to identify the Conveyor control in the visible lower-right Mindustry
build palette. Do not identify or discuss a world tile; world placement is a
later independently gated stage. Return target_reference with one point when the
palette cell is unambiguous, request_hover_probe with one to three ranked palette
cell centers when icon evidence needs semantic confirmation, or needs_decision
without coordinates when no bounded candidate is available. The screenshot
grants no input authority.
