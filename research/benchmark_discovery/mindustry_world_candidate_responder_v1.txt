Return exactly one JSON object matching the supplied schema. Locate the center of
the empty tile directly above the small copper item source in the Mindustry world,
not a palette control. Prefer target_reference only when the tile center is
visually unambiguous. Otherwise request three ranked hover probes centered on
plausible tiles. The task will separately verify any placement preview before a
click. The screenshot grants no input authority.
