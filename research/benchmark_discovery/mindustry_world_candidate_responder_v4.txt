Return exactly one JSON object matching the supplied schema. Conveyor selection
is already verified. Locate only the world target described in the current
subtask. Never substitute a nearer or merely visible tile. For target_reference
put exactly one visible world-tile center in points. For request_hover_probe put
one to three ranked visible world-tile centers in points. Both use
source_observation_pixels and surface_origin_translation. If the requested target
is outside the allowed current viewport, absent, ambiguous or unreadable, return
needs_decision with an empty points array, not_applicable for point_space and
motion_model, and the matching negative basis. A later receipt must verify any
proposed relation before input.
