# Mindustry materialized live smoke v2 — Issue #2624

Prospective scorer-correction allocation. V1 first outcome remains immutable
`FAIL_ORACLE_MISMATCH`; this directory does not overwrite or pool it.

Changed factor: scorer binds the historical canonical oracle Git blob
`fb7cd0e88e730401b32c2954b58c8c02ec83fcc6` and its actual decoded dimensions
(width=250,height=300; 300 rows × 250 columns). Exact fixture members, launch
route, 60 s timeout, zero-input rule, private display, and cleanup requirements
are unchanged.

Formal denominator at source freeze: 0/1.
