# MAP01 import dependency repair successor #1995

Successor to the STOP retained by #1982. Scope is dependency declaration and import-only readiness; no source/runtime/fixture/MAP01 semantics are changed. The prior failure was `ModuleNotFoundError: openpyxl` during the static import gate.

H/T/D/C/U are recorded in Issue #1995. Counters for this identity remain construction=0, formal=0, GUI/X11/model/task input=0 until a fresh isolated workflow explicitly runs.
