# runtime_outcome_fullsession_v1

Additive research evidence only. Exact measured source/fixture hashes and IDs were frozen at GitHub commit `c5b996b4e0f02471f97d744e32916251e37a3036` before six live cases.

`RESULT.json` separates the live mechanism result from the failed prefrozen audit. `REPORT.md` explains the checker defect and post-hoc independent audit. Historical timeout/running evidence is reused, not rerun.

`source_bundle.tar.gz` contains the exact published research source and tiny PWAD fixtures. Full raw screenshots/controller logs are conversation artifacts only and are referenced by digest in `VALIDATION.json`.
