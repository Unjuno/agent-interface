# Visual recovery followed by real task completion

2026-09-16. Scoped development evidence; no runtime promotion, model benchmark or full MAP01 clear.

## Results

| Test | Baseline | Candidate | Independent endpoint |
|---|---:|---:|---|
| Four fresh MAP01 disturbance pairs | Single correction 0/4 | Re-observe + bounded search 4/4 | Destination beyond first door, alive, no timeout |
| Four Inkscape pan triples | Stale coordinates 0/4, safely declined | View restoration 4/4; target reprojection 4/4 | Saved SVG loses only task-target |
| Two target-removal negatives | Not applicable | Reprojection 2/2 safe refusals | Zero target pointer/Delete/Save; unchanged file bytes |

The original 22-case block is **ABORTED, NOT PASS**. Eight MAP01 cases completed, then the first GUI setup safely refused before key admission because GTK transferred focus from client 10485768 to descendant 10485769. Thirteen original cases were not run. A new GUI-only v2 plan changed only startup readiness (stable observed descendant focus before input); it completed fourteen cases without rerunning MAP01. Original failure, initial failed audit, and successor plan are all retained.

## MAP01 boundary

Four seeds 993421–993424, signed turns +0.20/-0.28/+0.36/-0.36 s, alternating arm order. The controller uses X11 screenshots and an explicit reference, never position/heading/map/engine image buffers. Both arms use the same DEVELOPMENT-KNOWN fixed post-recovery program: sixteen 250 ms forward pulses, one 80 ms Use, eight 250 ms forward pulses. This is not autonomous door discovery, combat, unknown route navigation or level clear.

The independent harness checks forward displacement >=1100 map units, absolute cross-track <=96, final Z<=-120, alive and no timeout. All candidates achieved forward 1391.796–1391.871, cross-track 0, Z=-128, health 100. Two baseline programs completed without reaching that endpoint; two safely yielded on weak matches. Program completion is not semantic success.

Candidate process wall median 12.513 s [11.909–13.596]. Failed baseline timings are not successful-latency controls. Measured game rate during the controller: 35.000–35.055 tics/s. No model call, god, save/load, automap or direct action vector.

## Cheaper GUI recovery

Rather than restore the whole viewport, the candidate geometrically maps the explicit reference target into the current image and then takes a separate fresh screenshot to check an 11x11 patch before InputOwner pointer admission. Patch max RGB-code error <=8; target observation age <=500 ms. An earlier preparation probe failed at 522 ms age; the candidate adds a later observation instead of relaxing freshness.

Both methods completed 4/4 exact delete-and-save tasks on a custom SVG, preserving every other renderable ID/attribute and canvas attribute.

| Process-boundary metric | Restore view | Reproject target |
|---|---:|---:|
| Wall median [range], seconds | 8.102 [6.265–9.230] | 4.358 [4.257–4.736] |
| Observations median [range] | 8 [6–10] | 3 [3–3] |
| Keyboard actions median [range] | 6 [4–8] | 2 [2–2] |
| Clicks | 1/case | 1/case |

Paired time differences are -2.338/-1.997/-4.681/-4.974 s, median -3.510 s. Descriptive small-fixture results only, including process startup, matching, persistence and waits; no general speedup. Both removed-target negatives returned YIELD_TARGET_CHANGED with no target mutation. Uniform target patches can alias identical-looking objects; no general semantic-identity or atomic GUI-effect claim.

## Environment and audit

Intel Xeon Platinum 8370C (nominal 2.80 GHz), affinity CPUs 0–4, frequency not pinned/measured; Linux 6.18.44; Python 3.13.5; OpenCV 4.13.0, one thread; NumPy 2.3.5; ViZDoom 1.3.0; Inkscape 1.4. One case at a time in private Xvfb/Openbox. MAP01 640x480 ASYNC_SPECTATOR on a 1280x800 display; GUI crop 1140x539. Inputs use unchanged InputOwner v10. Process-level controller/scorer separation is not an OS-security proof; full Executor/model integration is not established.

Runtime dependency base: 9e6d5ecdbb5440fd5df1883161f2c63b2c3bb245. Publication base: dce1ced12ab167563cf502ec62e87c4fe803afac. Verified offline lab: 2592 source files / 12 wheels. No shared runtime or formal workflow changed.

Independent retained audit passes 22 completed cases, 96 controller PNGs, 204 keyboard actions, 8 clicks, 234 empty controller release/close records, source/image hashes, admission/release order and independent effects. Four regression methods pass. Re-extracted full-archive replay produces byte-identical audit output. Six published core source blobs were checked against executed local Git blob hashes.

## Retention and reproducibility

Full conversation attachment: `visual_followthrough_20260916_evidence.zip`, 15,090,686 bytes, 525 files, SHA-256 `5552bd0732897cfd008ce03936092bae9646208a3688e240b6f6233578e1e94a`.

It includes both frozen plans, all executed sources, preparation failures, original aborted block, GUI successor, every image, before/after SVG, raw release trace, manifest and independent audit. **This GitHub directory does not contain the PNG/raw-result tree and is not a standalone full-evidence checkout.** Obtain the matching conversation attachment to replay `python test_contract.py` and `python audit_v2.py`. Never overwrite/resume the aborted allocation. Runner commands require `--source` pointing to the frozen offline lab source plus a new `--out` directory.

## H/T/D/C/U and next boundary

H: local visual recovery can support a later known operation; repairing only a target may avoid unnecessary whole-view restoration. T: four MAP pairs, four GUI triples, two negative controls under pre-execution local source hashes. D: completed MAP sub-study and GUI successor gates pass; original combined block remains aborted. C: known reference/start scene, scripted follow-through, uniform patch aliasing and absent concurrent semantic interference can explain limited transfer. U: small sample size, OS/tic phase, feature matching, fixed calibration and scorer scope; no justified combined uncertainty u_c or coverage factor k is estimated.

Pixels and game-map units are not SI metres. Pixel error divided by pixel-per-second gain is a duration in seconds; timing uses perf_counter_ns. The full report contains the variable/unit contract. Next integration must test model-authored subgoal selection and post-action verification on unseen contexts; no full-level-clear or production claim follows from this record.
