# OS visual follow-through local development 01

Publication base: `dce1ced12ab167563cf502ec62e87c4fe803afac`.
Executed runtime dependency base: `9e6d5ecdbb5440fd5df1883161f2c63b2c3bb245` (verified offline lab, 2,592 sources / 12 wheels).
Prior recovered evidence ZIP SHA-256: `2fb567b27ec841b3049ad812c7066094bf2a6f6a7b19f1aa3052651af410f7d8`.

Frozen local plan SHA-256: `cd35766baa3a17ceb47e80ddc82bbb416aed563dfc876735c76d8dcba9a6d116`.

22 scheduled cases, one case at a time in private Xvfb sessions:
- Four fresh MAP01 pairs, seeds 993421–993424, signed turn disturbances +0.20/-0.28/+0.36/-0.36 seconds. One correction vs strict re-observation plus bounded search. Both use the identical development-known forward/use program afterwards. Independent destination: forward displacement >=1100 map units, cross-track <=96, final Z<=-120, alive and not timeout. This is initial-door traversal, NOT full MAP01 clear.
- Four Inkscape triples, canvas pans +2/-2/+4/-4 steps. Safe stale-coordinate refusal vs iterative viewport restoration vs current-image target reprojection. Exact saved SVG must lose only task-target; all other renderable IDs/attributes and canvas attributes unchanged.
- Two target-removal negatives for target reprojection. No target pointer/Delete/Save is allowed; post-mutation document bytes must remain unchanged.

Finite gates: MAP01 candidate >=3/4 and more destination successes than single; Inkscape reprojection 4/4 exact outcomes and negative refusal 2/2; all releases verified empty. Costs are descriptive only. Missing evidence is not success. No retry or overwritten first outcomes.

Inputs: unchanged InputOwner v10 over X11; controller reads OS screenshots and explicit reference/point only. No game variables, SVG oracle, automap, direct game-action vectors or model calls enter controller. Scoring remains harness-only, not an OS-security isolation proof. No shared runtime, existing formal allocation, workflow or launch claim is modified.

Preparation retained two too-early door-use probes, a GUI setup focus refusal, a stale-target-observation refusal, and an XML structural-metadata oracle mismatch. The corrected oracle accepts only non-rendering structural additions; target patch still requires a fresh exact observation and max RGB error <=8. Source hashes and full local plan are retained with the resulting evidence.
