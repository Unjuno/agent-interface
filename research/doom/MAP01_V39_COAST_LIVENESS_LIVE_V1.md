# MAP01 v39 unauthored-coast liveness first outcome

The frozen v39 same-fixture, six-decision Luna-low episode ran once without
retry while MAP01 continued to advance. The v39 change makes an unauthored,
empty coast carry no model-authored policy guard; exact frames still update
current state and any returned action must pass fresh immediate validity.
Three decisions used that coast mode. At least one completed after health
decreased during its model wait. Decision2 completed but failed fresh action
validity before input; decision3 completed and admitted a primary program.
This exposes the repaired answer-liveness path without bypassing the later
input gate.

| Final admission | Decisions | Plan admissions |
|---|---:|---:|
| `INPUT_ADMITTED` | 3 | 3 |
| `REJECTED_ACTION_NOT_CURRENT` | 2 | 0 |
| `REJECTED_POLICY_INVALIDATED` | 1 | 0 |

One model-authored action was revoked while it was running. Decision3 authored
`retreat_fire` followed by `strafe_left` with a six-point maximum health-loss
predicate from source health73. A typed current frame showed health65; the
running guard rejected `health_max_decrease_from_source_failed`, matched the
active program ID/intent token, requested cancel, and exposed a verified empty
physical-input release while terminal closure remained pending. Its final
terminal was cancelled and verified empty again. This is the first naturally
exposed active running-action revocation for the integrated path. It is one
event, not a latency or reliability distribution.

The independent scorer recorded one kill, zero deaths, no MAP01 exit and an
unfinished episode after50.371s control/43.318s model-wall. Nine programs
(six cover, three model plans) all had matching empty-release terminals.
There were218 exact observations and218 corresponding early typed health/ammo
events, with zero reconciliation failures. One exact unchanged frame used
AIT provenance plus a previous PNG rather than creating a new PNG, so there
were217 PNG files. The original hash-frozen audit failed only because it
equated observation count with PNG count. That failed audit remains in the
first outcome. A separately versioned retained audit v2 checks AIT presence,
the referenced PNG and decoded RGB hash for every observation, then passes
all original mechanics,
including unauthored-coast monitor modes, no input after rejection, exact
running-program binding, causal early release and independent score. File
manifest and v2 audit pass on Windows and WSL for464 files/63,880,177 bytes.

V38's one of six plan admissions and v39's three of six are descriptive
first outcomes, not a causal speed or success comparison: game trajectory
and model outputs differ. V39 also had zero cover renewals and one authored
policy invalidation in decision5, so sustained useful control is still open.
The next test should separate coast liveness from threat survival and measure
renewal gaps, useful-control intervals, damage, ammo and progress under a
versioned condition. Domain coverage beyond DOOM remains a requirement.
Interrupted-turn token usage is incomplete; do not infer a six-turn total.

See the [`frozen preregistration`](map01_v39_coast_liveness_live_v1_prereg.json),
[`first retained report`](results/map01-v39-coast-liveness-live-01/report.json),
[`original failed audit`](results/map01-v39-coast-liveness-live-01/audit.json),
and [`retained audit v2`](results/map01-v39-coast-liveness-live-01/audit-v2.json).
