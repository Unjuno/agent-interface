# Lifecycle source-generation v1

Research-only real-MAP01 experiment. It separates **receipt delivery freshness** from **underlying source-generation freshness**. Stalled source progress may refuse only new authority; it never cancels active input. A trusted terminal receipt retains the earlier explicit cancellation path. No shared runtime mutation or gameplay-policy claim.
