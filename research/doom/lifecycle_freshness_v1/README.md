# Lifecycle freshness v1

Research-only real MAP01/X11 experiment. It separates terminal revocation from
lifecycle-evidence freshness. Silence never means terminal and never cancels the
active input; it may only refuse a new submit after configured evidence age.
