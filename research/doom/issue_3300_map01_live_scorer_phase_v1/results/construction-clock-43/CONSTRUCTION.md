# Construction-clock-43 — in-memory VIZ_Tic clock sampling

Disposition: `PASS_CONSTRUCTION_ONLY_ENTRY_CLOCK_CORRELATION`.
Formal allocation remains **0/120**.

## H / T / D / C / U

- **H:** Replacing run41/42 per-tic file writes with a fixed in-memory ring buffer, while calling `clock_gettime(CLOCK_MONOTONIC)` directly as the first C statement in `VIZ_Tic`, preserves same-clock scorer phase observations and reduces per-tic I/O instrumentation.
- **T:** Fresh ViZDoom 1.3.0 source build at pinned upstream commit `c8e0a31182d98c6f40a65674283e736b851e8e59`, same official Freedoom 0.13.0 WAD, three fresh hidden `ASYNC_SPECTATOR` MAP01 sessions at 35 Hz. Each had a 1.5 s passive API polling window, exact scorer once, no advancing API calls, 150 ms post-scorer window and cleanup. The source patch takes the timestamp directly in `VIZ_Tic`, stores `{pid, monotonic_ns, gametic, vizTime}` in an 8192-record static buffer, and flushes once during process destruction. Docker runtime remained linux/arm64, network none, read-only root with tmpfs and read-only inputs.
- **D:** All 3 sessions initialized, scorer returned and cleanup completed; API tic stayed at 1 in all 3. The shutdown trace contained 64/64/65 entries and contiguous `vizTime`. The independent v2 audit bracketed all 24 getter starts, zero errors. Median entry intervals were 28.463/28.371/28.697 ms; observed min–max included scheduler outliers (18.688–40.229, 11.334–46.719, 10.714–39.630 ms). Getter-start phase ranges were 0.533–0.541, 0.256–0.269, and 0.0012–0.9998 by repetition; the last scorer sequence spans almost a complete tic interval. Getter spans were 1.292–33.083 µs, 1.333–23.791 µs and 1.292–18.291 µs. Corrected auditor tests pass 2/2.
- **C:** Run43 removes the per-tic `write()` syscall; the only file flush is at process exit. It still reads the clock and updates memory each tic, and the direct clock sample occurs after compiler-generated function prologue, so machine-entry-to-sample uncertainty remains unbounded. Scheduler outliers show that nominal 35 Hz cannot be used to impute an unobserved edge. This is still construction-only and not ±1 ns/µs formal phase evidence. Run41/42 cadence comparisons are separate allocations, not paired causal attribution of instrumentation overhead.
- **U:** Whether the remaining function-prologue/clock-sample uncertainty can be bounded sufficiently for the #3453 boundary-neighborhood schedule, and whether source instrumentation leaves the target distribution unchanged, remain unresolved. No formal collection is authorized.

The first post-hoc file named `audit-v1.json` was accidentally generated with the narrowed v2 auditor; it is preserved with that provenance. The original v1 label was separately regenerated from the pinned prior image as `audit-original-v1.json`; `audit-source-clock-corrected.json` is the scope-corrected v2 result. These records differ in terminology/limitations, not raw observations. Run43 is not pooled with runs41–42.
