# TEMPORAL-X11-DISPLACEMENT-OBSERVABILITY-CEILING-R1-20260918-013

Issue #1351. Offline representation observability test only; no X11/live authority.

Evidence is frozen from published #1344 facts: candidate accuracy .94/.93 at100/200ms; UNKNOWN phases100={94..99} and200={0..6}, two directions each; observed UNKNOWN pair patterns contain only same-sign7px/8px displacements. Nominal full motion7.3px with direct bound±0.75px accepts each such interval as full-compatible.

Hypothesis: every phase>0 boundary UNKNOWN admits an opposite-current blind-tail witness without changing the two sampled displacements. Phase0 has no blind tail and is the only timing-identifiable subset. Therefore safe displacement+phase ceilings stay below .95 at both100 and200ms.

Formal is one deterministic standard-library invocation; reruns/replacements/tuning0. Construction uses disjoint toy phases only.
