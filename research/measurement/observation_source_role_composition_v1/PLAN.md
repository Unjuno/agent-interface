# Observation source-role composition — Issue #1471

Goal: compose the retained no-authority observation-reuse semantics from #615/#629 and #721 without relabeling parent evidence or introducing new GUI/model/input work.

H: explicit non-rebindable source roles preserve valid planner-context and terminal-feedback reuse while rejecting historical terminal -> current-context upgrades and changed-current -> old-terminal-proof rebinding.

T: stdlib container; exact retained parent summary identifiers; independent candidate/oracle; excluded directed construction; source-first freeze; one 100,000-row deterministic formal corpus with >=20,000 cases for each primary role-confusion discriminator; independent audit/corruption controls.

D: candidate/oracle mismatch0; role-confusion/rebind/authority/old-action/replay accepts0; all exact valid role controls accepted; untyped discriminator exposes both primary faults; integrity passes.

C: production may use distinct receipt types instead of an enum. #721 full raw packet is not reanalyzed; only its retained Issue contract is used.

U: retained/synthetic composition only. No performance, model, physical-input, GUI or production claim.
