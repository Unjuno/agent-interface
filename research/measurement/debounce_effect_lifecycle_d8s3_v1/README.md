# Debounced effect lifecycle — local evaluated evidence

**PASS_LOCAL_DEBOUNCE_EFFECT_LIFECYCLE** / publication HOLD. Read REPORT_JA.md, PLAN.md and PROOF.md for scope.

12 fresh native X11/Tk sessions,18 requested snapshots,12 actual private-file writes. Current cumulative-value inference falsely certifies4 requests; normal-cancel-return inference falsely cancels2 completed writes. Exact lifecycle reporting:12 WRITTEN,2 SUPERSEDED_BEFORE_WRITE,2 CANCELED_BEFORE_WRITE,2 UNKNOWN; false written/canceled0. Three shadow reporting processes consume each shared saved trace; these are not36 independent GUI cases.

Same unchanged current-main backend/core imported in full. One cooperative sender acknowledges each same-key F8 before the next dispatch. Additional app-origin/timer/file receipts are required; this is not arbitrary native correlation, cryptographic trust, production runtime, GUI-global semantics, model utility or a timer algorithm discovery.

Local source/gates were frozen before12 evaluation cases; no public Issue preregistration. Formal directories name the measured phase only. Foreign branches and all historical evidence are untouched. GitHub registration/PR/main integration were not possible with the exposed read-only connector. ISSUE_DRAFT.md and PR_DRAFT.md are drafts, not created resources.

## Read-only reproduction

With Python3.13 standard library, from this directory:

```sh
python -S -B verify.py
```

This checks all retained bytes, re-audits original GUI traces, checks all12 effective copied-evidence controls and runs12 pure policy unit methods. It does not invoke app.py, run_case.py, supervise.py or execute_once.py. Do not rerun a consumed allocation to improve publication status.

Complete original construction/evaluation files and the first auditor construction failure are retained. Temporary mutated copies are not included; exact mutation code, changed-file before/after hashes and resulting errors reproduce them from original evidence. The manifest is an integrity record, not an independently authenticated signature. Review scripts before executing them.
