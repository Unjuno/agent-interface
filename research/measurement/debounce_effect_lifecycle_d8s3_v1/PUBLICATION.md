# Retrospective publication note

GitHub Issue: #4426.

This directory delivers the already-consumed local allocation `debounce-effect-d8s3-20260926-01`. It is not public preregistration and no GUI/scientific row is rerun for publication.

Original complete ZIP: SHA-256 `69bf4a8bf23c14a2451ce9407cc40989f994100c7e0fb3d789c93e695db555bb`, 314 ZIP members.

The canonical lossless publication archive is reconstructed by concatenating `evidence.part01.b64` through `evidence.part07.b64` in numeric order and strict-base64 decoding. The decoded `tar.xz` is 73,836 bytes with SHA-256 `ca08faeea1bcb181b9f10524eca2397d1f54052c47d6c9916997b7a55e54a32d`. Exact per-part byte counts, SHA-256 values and Git blob IDs are bound in `ARCHIVE_MANIFEST.json`; every remote part blob was read back against the locally computed Git object identity.

Use `restore_evidence.py NEW_DIRECTORY` for bounded safe extraction. The restored archive contains the complete original study directory, including source, construction failures, both formal batches, raw X11/application/reporter/process records, frozen hashes, original audit/controls and the retained read-only `verify.py`. After extraction, run the restored study's `python -S -B verify.py`; it does not restart the GUI experiment.

Readable publication files in this branch summarize exact retained results. Historical `HOLD_PUBLICATION_TOOL_UNAVAILABLE` statements inside the archived original remain immutable chronology; this continuation has GitHub write access and does not rewrite that past state.

Scientific result remains `PASS_LOCAL_DEBOUNCE_EFFECT_LIFECYCLE`; publication/review/main integration are separate gates.
