# Post-#1103 real-desktop operation/target oracle census

Decision: **PARTIAL_REALDESKTOP_ORACLE_PROGRESS_BLOCKED_DATA**.

The retained-data census reviewed 21 post-#1103 real-app decision opportunities under the frozen strict rule. Exactly **2** rows qualify as independently authored decision-level operation/target supervision: the OpenTTD evidence-authority v2 Company Finances positive and the paired Airport no-match condition. This is real progress from #1103's zero qualified rows, but it does not close the #1015 corpus gate.

## Frozen counts

- reviewed opportunities: 21
- oracle-qualified rows: 2
- qualified positives: 1
- qualified semantic negatives/abstentions: 1
- qualified operation families: \`CLICK\` only (1 / >=2 required)
- target-alternative rows: 2
- explicit semantic \`YIELD / NO_LOCAL_ACTION\`: 0
- independent qualified split/session units: 2 / >=12 required
- directly #1015-compatible qualified rows: 0 / 2
- lossless-adapter eligible rows: 2 / 2
- oracle/future leakage: 0

The Chromium memory ablation is not counted: its independent server scorer distinguishes correct/decoy/no-effect only after an unconstrained image-point decision and there is no finite caller-visible candidate packet with an independently authored acceptable set. Mindustry receipt revalidation is not counted because operation/target is selected upstream and the retained decision is a currentness/admission check. Compiled Chromium v5 is not counted because the caller-authored method scaffold already fixes the operation sequence; its POST scorer verifies effect, not operation choice.

OpenTTD v2 does qualify narrowly. The target noun is fixed before decision, exactly five verified receipt candidates are visible, the positive preregistration names Company Finances/receipt5 and the no-match preregistration states Airport construction is absent from all five receipts. The retained output vocabulary is \`TARGET_REFERENCE_ONLY / NO_TARGET_AUTHORITY\`, not literal #1015 \`CLICK / YIELD\`, so both rows are adapter-eligible rather than directly compatible.

Independent audit passes with errors \`[]\`. Six corruption controls reject inflated counts, Chromium final-score laundering, Mindustry currentness laundering, compiled-method laundering, coercing no-authority into direct YIELD, and source-blob substitution. Postformal scientific source rehash is 6/6 exact. Formal invocation1; reruns/replacements/tuning0; model/GUI/task-input actions0.

## Next discriminator

Do **not** allocate a learned backend. The smallest useful successor is prospective real-app decision-row collection using the already-frozen #1015 row contract. It should add only missing classes: at least a second executable operation family and explicit semantic \`YIELD / NO_LOCAL_ACTION\`, each with a finite caller-visible candidate packet, a pre-decision independent acceptable set, and independent episode/split units. Re-running current OpenTTD/Chromium/Mindustry tasks without that decision-level oracle would not close the data gate.
