# #1819 Temporal query-cost break-even

H: request specificity has exact prior-dependent break-even surfaces once symbolic query overhead is charged.
T: exact Fraction arithmetic over all denominator-40 4-class priors; independent audit; fixed corruption controls.
D: 12,341 priors, exact extrema/equality/threshold identities, audit mismatch0, corruption controls PASS, formal1/reruns0.
C: scalar image/query costs are accounting abstractions; no measured token/latency claim.
U: analytical only; empirical overhead calibration remains downstream.
