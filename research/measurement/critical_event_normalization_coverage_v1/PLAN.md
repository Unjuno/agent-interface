# CRITICAL-EVENT-NORMALIZATION-COVERAGE-20260918-001

H: #1021 queue semantics are valid, but current main lacks an executable provenance-preserving normalizer from current runtime MUST_PRESERVE_EDGE records into the six validated queue critical kinds.

T: Pin exact source identities and a frozen source-derived inventory. Count a mapping only if an executable source artifact on current main is identified; prose/name similarity is not mapping evidence. Run one deterministic source-closure after source-first Git publication. No live/model/GUI/input action.

D: PASS only if every MUST_PRESERVE_EDGE has an executable mapping. HOLD_CRITICAL_NORMALIZATION_MISSING if at least one required edge lacks one while reducer semantics remain intact. FAIL for an existing mapping that downgrades required edges or promotes ordinary state without evidence.

C: Several raw records may legitimately collapse to one queue kind. Aggregate owner_release proves terminal neutralization, not a fresh physical edge. Effect viewport change is not semantic EFFECT_VERIFIED.

U: Source-scope integration readiness only; absence from reachable current-main source/search is not proof about unpublished/private work.
