# Critical-event normalization coverage v1 — retained first outcome

Decision: **`HOLD_CRITICAL_NORMALIZATION_MISSING`**.

The exact #1021 freshness/backpressure reducer remains valid at its scoped contract. This source-closure asks the next integration question: can current runtime safety/currentness/effect records be converted into its six critical queue kinds by an executable provenance-preserving current-main adapter?

Frozen source inventory contains **9 MUST_PRESERVE_EDGE** rows and **0 executable mappings** in reachable current-main source/search scope. Missing source-bound edges include:

- input-owner verified release because of `focus_changed`, `surface_changed`, `expired`, `cancelled`, or `stop_requested`;
- `owner_failed` and `cleanup_failed` fail-closed safety records;
- observable-signal guard `HARD_INVALIDATED` and `UNKNOWN` outcomes that require a new decision.

The audit deliberately does **not** promote ordinary `input_admission`, guard `SOFT_CHANGED` with `keep_existing_policy=true`, or MAP01 viewport-only `visible_change` into critical/effect-verified semantics.

This is therefore an integration/normalization HOLD, not evidence that the #1021 queue reducer is wrong and not proof that no mapping exists outside reachable current-main source. The next valid rung is a separately frozen normalization adapter contract; a matched model-facing backpressure experiment should wait until that mapping is explicit.

Formal invocation: 1; reruns: 0. Independent audit PASS. Corruption controls reject 5/5. Model/provider/GUI/task-input/authority actions: 0.
