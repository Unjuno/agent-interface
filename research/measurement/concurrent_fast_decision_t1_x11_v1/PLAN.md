# #1384 T1 controlled X11 concurrent fast-decision effects

Parent #1376; direct child #1378 / PR #1381 retained `PASS_T0_DETERMINISTIC_CONCURRENCY_SHADOW_SCOPED`.
Issue #1384 is the frozen H/T/D/C/U contract and remains authoritative.

Scientific factor: transfer the exact #1378 deterministic selector from shadow timing into one controlled private-X11 task-effect fixture while keeping the simulated frontier boundary fixed at request=0 ms / return=40 ms in both arms. No classifier.

Controller evidence is one sentinel pixel only. Independent progress/harm scorer pixels are disjoint and read only after the frontier-open interval by a separate Xlib connection.

Construction: exactly one matched pair / two fresh sessions, ACTIVATE at 18 ms, only after explicit #60 live lease.
Formal: exactly six matched pairs / twelve fresh sessions under the six frozen #1384 scenarios, one formal invocation, reruns/replacements/tuning=0.
