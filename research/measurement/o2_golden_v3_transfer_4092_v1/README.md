# Golden-v3 O2 contiguous-transfer allocation (#4103)

Source-first formal transfer of the exact #4092 contiguous-materialization factor to eight predeclared adjacent retained golden-desktop-v3 observation frame pairs. No new GUI/model/input/task effect occurs in this experiment.

The one formal allocation is triggered only when the dedicated PR is **opened**. Synchronizing later evidence commits does not rerun it. The temporary workflow is branch-only execution machinery and must be removed before the final evidence PR is merged.

See `PLAN.md`, `schedule.json`, and `FREEZE.json`. A component PASS is not runtime promotion; a separate adoption/integration decision is required.
