# Issue #4103 — source-first frozen transfer plan

## H
The exact #4092 contiguous tile-materialization factor transfers to eight predeclared adjacent retained golden-desktop-v3 frame pairs without changing AIT1 bytes or reconstructed RGB. Per-pair median contiguous/vector update-wall ratio <=1.10, median of eight medians <=0.95, and at least three medians <=0.90.

## T
Retained evidence only; no new GUI/model/input/task effect. Golden frame Git blobs and eight adjacent pair identities are frozen in `schedule.json` and Issue #4103. Decode PNG to RGB with Pillow. Tile edge 64, zlib level 1. Canonical current-main O2 plus frozen vector/contiguous variants. Five excluded warmups +31 measured paired updates per pair, alternating order: 248 measured pairs. Initial encode outside timing. Exactly one GitHub Actions formal allocation using the pinned existing `runtime/integration_checks/Dockerfile`; formal container `--network none`, checkout read-only, result mount writable. No formal retry/replacement/tuning.

## D
PASS only with all identities, 248 pairs, exact wire/pixel reconstruction, exits, audit, >=8 corruption controls and all performance gates. Correctness mismatch = FAIL. Complete integrity but performance miss = HOLD. Missing source/image/process/denominator evidence = STOP/HOLD. No subset rescue.

## C
Retained Chromium pixels transfer workload shape only; no historical capture timing, current authority, model wait or task effect. Repeated encodes are technical repetitions. GitHub runner CPU/frequency noise differs from the earlier local environment. Paired comparison mitigates but does not eliminate that uncertainty.

## U
No runtime/default promotion, bandwidth/token saving, end-to-end task speedup, model quality, human tempo, measured peak RSS, cross-platform or product claim. PASS permits only a separate runtime-adoption/integration decision.

## Bounded roadmap
Construction on synthetic pixels only -> freeze all source/gates and exact retained-image Git blobs -> commit public freeze and branch-only one-shot workflow -> open draft PR (formal allocation once) -> retain artifact and independent raw audit -> remove temporary workflow from final diff -> evidence-only PR/main readback -> separate adoption decision only if PASS.
