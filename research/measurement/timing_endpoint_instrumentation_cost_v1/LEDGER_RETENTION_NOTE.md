# Raw timing ledger retention

The one frozen formal invocation produced `845496` bytes of gzip-compressed paired timing ledger with SHA-256 `fe8aac782c4e65b96b44bf2f4157644d4d0a52aca345b56687b5612e09bed9c3`. The frozen independent auditor consumed this full ledger and recomputed baseline/instrumented/delta summaries. A copied-ledger mutation is rejected by the frozen corruption verifier.

The GitHub connector used here is text-oriented; the binary gzip ledger is not uploaded through it. The exact commitment, row count (`100000` pairs), seed (`128020260918001`), source archive and audited summaries are retained. This publication transport boundary does not alter or rerun the scientific result.
