# O4 live application transfer #2769

Evidence-only research bundle. `REPORT.md` is the canonical disposition. The formal allocation is consumed; do not rerun it. `controls_v2.py` is postformal read-only audit diagnosis and is not frozen experiment source.

Offline checks: `python audit.py formal-01`; `python controls_v2.py formal-01`. The original frozen `controls.py formal-01` must remain 7/8.
