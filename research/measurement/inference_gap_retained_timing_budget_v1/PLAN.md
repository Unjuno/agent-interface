# Retained inference-gap timing budget v1

Task: `INFERENCE-GAP-RETAINED-TIMING-BUDGET-20260918-001` / Issue #1489 / parent #1482.

## H/T/D/C/U

H: On the ten exact retained #1451 requested-route intervals, the sum of the retained T1 maximum useful-effect latency and retained #1459 maximum receipt-drain latency is a small timing allowance relative to each measured frontier gap. Passing this rung means timing scale alone is not a blocker; it does not establish causal composition or useful control.

T: Standard-library retained-evidence analysis only. Freeze exact Git blob identities and parsed values from #1451 RESULT, #1459 AUDIT/REPORT, and #1467 publication-integrity report. Candidate uses exact rational arithmetic; an independently structured Decimal oracle recomputes the metrics. One deterministic formal invocation includes five source-integrity/schema corruption controls. No model, GUI, X11, network, task input, raw #1459 reconstruction, or missing-part synthesis.

D: PASS_RETAINED_INFERENCE_GAP_TIMING_SCALE_SCOPED iff candidate/oracle mismatch0, ten row identities are preserved, boundary allowance equals 2.692318 + 5.182661 = 7.874979 ms, every gap/allowance >=100, every effective descriptive window >0, every overhead fraction <1%, all controls pass, source/result audit passes, formal1/reruns0.

C: Cross-experiment magnitudes are not one causal timing trace. XTEST/application semantic tails may exceed subprocess receipt drain. Requested gpt-6-astra identity is not verified in #1451.

U: #1459 publication is byte-set incomplete (formal parts03..06 absent); only integrated summary/audit values are consumed. No task-quality, token, human-tempo, causal speedup or real-frontier claim.
