# P0 physical-useful causal trace source closure v1 — first outcome

Decision: **`PASS_P0_CAUSAL_TRACE_GAP_LOCALIZED_SCOPED`**.

The retained semantic chain is source-closed at BASE `159dd3684ffcd0ee78b9ff5939e0173cd77c7b4f`:

`press/release physical bracket contracts -> same-lineage dual-edge adapter -> dual-edge occupancy bounds -> censored-down temporal gate -> provenance/exactly-once useful-effect roles`.

All six retained semantic nodes match their frozen identities/decisions, and the typed semantic chain is connected. This is **synthetic-contract evidence only**; it does not establish real X11 physical truth or a live useful effect.

Exactly three runtime/live gates remain `UNPROVEN_AT_BASE`:
1. `OWNER_EDGE_PRODUCER` — no BASE runtime owner emits the required same-operation physical edge receipts (`input_owner_v12` absent at BASE; #998 intent is not evidence);
2. `STABLE_HOLD_IDENTITY` — no retained BASE proof that press/release receipts share one stable actuation generation (#999 claim activity is not evidence);
3. `LIVE_CAUSAL_SAMPLE` — no separately leased live sample carries the receipts through occupancy/provenance/temporal admission to an independently scored useful effect.

Therefore `live_useful_control_proven = false`. This is the desired non-overclaiming result: the semantic measurement contracts are coherent, while the runtime/live proof boundary remains explicit.

Formal invocation: 1; reruns: 0. Independent audit: PASS (9/9). Corruption controls: 5/5 rejected.

Scope: retained/source evidence audit only. #998/#999 are intentionally not promoted from active work to proof; later completion requires a fresh successor rather than mutating this BASE.
