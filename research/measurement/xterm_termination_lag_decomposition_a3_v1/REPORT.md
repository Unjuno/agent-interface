# #1584 XTerm termination-tail A3 retained execution stop

Decision: **STOPPED_OUTER_EXECUTION_TIMEOUT_NO_RESULT**. Scientific disposition: **NONE**.

The fresh A3 successor changed only allocation identity and orchestration interpreter binding from #1563. The exact scientific harness/contract/fixture remained unchanged; the explicit supervisor `/opt/pyvenv/bin/python3` imported Xlib successfully. Excluded construction4 was eligible under the inherited #1563 construction rule: correctness/marker/ROI/reconstruction clean, effect→child-ready p95 20.982622 ms, child-ready→native-exit p50 164.001617 ms.

The one formal supervisor invocation was then run with a 45 s outer execution envelope. The wrapper terminated the invocation after 46 case directories had been created. No `FORMAL_RESULT.json`, scientific row ledger, audit result, or corruption result was serialized. `formal_stdout.txt` and `formal_stderr.txt` are empty. Therefore no partial case is pooled or interpreted scientifically.

Post-stop checks: all 8 frozen scientific source files still match their frozen SHA-256 values; residual Xvfb/Openbox/XTerm/decompose processes for display `:862` are zero. Formal invocation1 / reruns0 / replacements0 / tuning0.

The legal successor changes only the external execution envelope under a fresh TASK/seed while preserving this scientific source. The partial progress may justify provisioning a longer wrapper, but it is not a scientific sample and cannot be reused as result rows.
