# Retained receive/hash contrasts under #2117

## Chronology and ownership

This is posthoc analysis and evidence delivery of an already completed conversation-local experiment, not a new live allocation, retrospective public preregistration, or a wrapper-only successor Issue. The combined result has already been inspected. Preserve every original byte and decision, including HOLD_CUE_INTEGRITY.

Intake main: e4c2e58122aa138e421048d8e86ec18259143b9e. README, CURRENT_GOAL, ROADMAP, relevant open/closed Issues, open PRs, and 125 returned branch names were read through GitHub MCP. The parallel receive-hash-factorial-2117-v3 branch and #4010 are not this work. Allocation IDs alone are not globally unique; identify this source by archive SHA-256, internal path, freeze, and environment together.

Owned publication path: research/measurement/receive_hash_retained_contrasts_a55fed37/. Branch: research/receive-hash-retained-contrasts-a55fed37. No shared runtime, workflow, global roadmap, historical result, or other branch mutation.

Source archive: agent_interface_receive_hash_20260922.zip, 5,886,413 bytes, SHA-256 a55fed37ce2d65e132577aa9c1f754f8229524faf3ccf5eb4413000f368afa74. The relevant original path is research/live_control/x11_receive_hash_handoff_allocation04/. Original allocation receive-hash-handoff-20260922-04 used Xeon Platinum 8573C, CPython 3.13.5, private Xvfb, fixed logical CPU placement, unpinned frequency/host load, and no Docker/OrbStack image identity. Original 03 STOP and all 01/02 history remain immutable and are not pooled.

## H - descriptive question

The combined batching-plus-chunked-hash contrast does not identify the contributions of its two changes. On the retained four-block factorial, report the batching contrast at each hash setting, the hashing contrast at each receiver setting, and their interaction. Determine whether the direction and magnitude are consistent across the four blocks without inventing a population effect or inferring pure GIL causality.

## T - bounded read-only analysis

Independently decode all 9,115 original captures and 8,675 received packets using standard-library code that does not import the experimental runner, validator, or original auditor. Reconcile packet identity, pixel digest, monotonic boundaries, send status, case identity, and all 384 actual cue widths. Derive a 32-row table containing acquisition denominator, delivered count, 20ms validation-completion count, exact doubled median age in ns, and cue-width violations. Compare with the unchanged retained audit, then compute all four within-block conditional contrasts separately for idle and busy. Use every case; no cue exclusion, new seed, live process/capture, threshold tuning, or synthetic replacement of missing observations.

Counts of frames are not independent experimental repetitions. The unit for contrast summaries is one matched block, four blocks per load stratum. Publish all four contrast values plus their median and range. Do not use p-values or a reliability estimate.

## D - decisions

POSTHOC_RECONSTRUCTION_VERIFIED requires exact source archive/member commitments, all 32 case identities, complete denominators, raw-to-table agreement, unchanged original HOLD, independently recomputed contrast arithmetic, and rejecting corruption tests. This is an integrity decision only. Source, data, or arithmetic mismatch is STOP/HOLD. The original empirical decision remains HOLD_CUE_INTEGRITY even when descriptive contrast directions agree.

The published compact table is sufficient to reproduce these posthoc arithmetic contrasts, not a substitute for the complete per-packet image/process archive. Explicitly label original raw archival availability and do not claim it is fully GitHub-hosted unless actually uploaded and read back. Provide a read-only extraction command for the exact source ZIP; never execute the consumed runner.

## C - competing explanations

Receiver batching, hash call granularity, Python/native crossings, queue backpressure, order, scheduler contention, and host noise can interact. A small deterministic four-block schedule is not random assignment to a population. The one out-of-window cue remains in the denominator and invalidates the original all-cue gate. New faster frame handling does not imply notification-currentness, model usefulness, or correct GUI action.

## U and roadmap

No new model, provider, GUI/input, game, package install, live timing, or authority. Current execution container identity will be recorded for analysis only; it cannot replace original measurement hardware. No calibrated combined uncertainty or coverage factor is invented. Scope excludes full #2117 model/task/held-out-route acceptance and the global roadmap.

Roadmap: verify original bytes -> independent raw reconstruction -> all-block conditional contrasts and tests -> readable additive report/data/source PR -> exact-head review and checks -> main readback only after qualified merge -> own branch cleanup only if supported and dependency-safe. Prior public-write limitations remain historical; this session has a write-capable MCP toolset. Local tooling incidents belong here, not in a new fleet work order.
