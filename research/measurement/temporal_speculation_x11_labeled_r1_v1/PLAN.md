# TEMPORAL-SPECULATION-X11-LABELED-COLLECTION-R1-20260918-001

One factor: retain frame/content/state/future labels on the existing private-X11 moving-rectangle temporal-ring mechanism.

Frozen constants: candidate universe {-2,-1,+1,+2}; K=2; CURRENT_ONLY={-1,+1}; temporal right={+1,+2}; temporal left={-1,-2}; ambiguous={-1,+1}; ROI [80,60,160,120]; 20 Hz; 500 ms ring role; history offsets -150/-50 ms; future horizon +100 ms; tick join tolerance <=12 ms. Two history frames must be distinct. Historical evidence ranks prepared no-authority branches only; coverage/hit is not authority.

Formal: one invocation, fresh sessions of 3.5 s each, displays 800..823. Stop collection after retained exact-current opposite-history/future-disagreement alias pairs reach 64, otherwise stop at 24 sessions. Reruns/replacements/tuning0.

PASS gate: >=32 alias pairs, >=16 future-disagreement pairs (alias pairs here already require disagreement), temporal coverage gain >=5 percentage points on paired rows, capture exceptions0, wrong/stale/authority admissions0, source/audit integrity pass. HOLD only for insufficient alias pairs under session cap. No XTEST/model/provider/shared runtime.
