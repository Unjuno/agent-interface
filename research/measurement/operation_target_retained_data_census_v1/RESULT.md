# Operation/target retained-data census v1 — first outcome

Decision: **`BLOCKED_DATA`**.

One source-first deterministic census invocation, reruns0; no model/provider/GUI/task input. Golden Desktop v3 contributes six exact, independently final-effect-scored tasks, but they all instantiate the same token-field + submit workflow.

Observed eligibility:
- positive rows: **6** / required32;
- semantic negative rows: **0** / required32;
- operation classes: **1** / required>=2;
- target alternatives per operation: **1** / required>=2;
- YIELD/NO_LOCAL_ACTION negative rows: **0**;
- independent operation/target semantic oracle: **false**;
- independent final-effect oracle: **true**;
- independent split units: **1**.

The retained task4 stale-layout repair is currentness/grounding evidence, not an operation-selection negative label. Treating it as one would mix authority/target validity with semantic action choice.

Therefore no classifier/local-model shadow allocation is justified from this corpus. The next valid step is to collect or identify a larger retained browser/desktop corpus with multiple permitted operation choices, compatible target alternatives, explicit NO_LOCAL_ACTION/YIELD negatives, and an independent semantic oracle.

Independent audit PASS. This does not weaken Golden Desktop correctness evidence; it only limits its reuse as #1015 training/evaluation data.
