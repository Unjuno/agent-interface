# Cross-domain retained operation/target corpus census — first outcome

Decision: **`BLOCKED_DATA`**.

One source-first deterministic formal invocation, reruns0; no model/provider/GUI/task input. Expanding from Golden Desktop v3 to retained Calc modal, Chromium combined, Inkscape correction and Inkscape drag evidence increases raw operation diversity but does not create decision-level operation/target supervision.

Frozen census:
- retained source program opportunities (upper bound): **21** across **6** episode units;
- raw operation families: **9** (`CELL_ENTRY`, `DRAG`, `FIELD_EDIT`, `FORM_SUBMIT`, `MODAL_CONFIRM`, `NAVIGATE`, `POINTER_SELECT`, `SAVE`, `WATCH`);
- episode-final independent oracles: present for all five source families;
- oracle-qualified decision-level positive rows: **0 / 32 required**;
- semantic operation-choice negative rows: **0 / 32 required**;
- oracle-qualified operation families: **0 / >=2 required**;
- target alternatives per oracle-qualified state: **0 / >=2 required**;
- explicit semantic `YIELD / NO_LOCAL_ACTION` rows: **0**;
- decision-level operation/target semantic oracle: **absent**;
- episode split units: **6**.

The census deliberately does not relabel malformed operation schema, focus/currentness interruption, failed drag effect, an invalid numeric entry later corrected, or stale binding rejection as semantic operation-choice negatives. Those are different evidence roles.

Independent audit PASS and corruption controls reject5/5. This does not weaken any retained desktop task result. It establishes that existing trajectories—even across several GUI domains—are not a valid #1015 shadow corpus. A new data-collection contract must score operation/target choices at each state, including explicit no-action/yield cases, rather than infer labels from final task success.
