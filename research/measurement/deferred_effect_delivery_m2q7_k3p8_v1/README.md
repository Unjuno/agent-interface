# #4391 — deferred application-effect origin, complete retained evidence

Retrospective publication of the consumed local study `deferred-effect-origin-20260925-m2q7`.
This is not a fresh experiment, a public preregistration, or a production promotion.
Parents #4134/#34 and all old results remain unchanged. Publication intake main:
`4c701cc51b06296268ad8d9ae3eff1dd6f2d379d`.

## Retained H/T/D/C/U

H: latest-operation context and FIFO order do not identify deferred effect origin under overlapping
or reverse completion. A received native event ID carried through the application callback can
bind the effect independently of the current operation. Missing origin stays UNKNOWN.

T: 12 private authenticated TCP-disabled Xvfb/Tk sessions (6 schedules x2 repeats), two native
F8/F9 programs per session, 24 actual increments. Three separate reporting policies consume the
same saved stream: these are paired reports, not36 independent GUI trials.26 notifications per
policy include two repeated notifications. Two construction sessions are excluded. Local source
freeze preceded two six-case batches, each once; no retry/replacement/exclusion/tuning.

D: `PASS_LOCAL_DEFERRED_EFFECT_ORIGIN_SCOPED`, preserved exactly.

| Reporting policy | Wrong origin | Duplicate notification accepted again | UNKNOWN | DUPLICATE suppressed | Unique correct of24 |
|---|---:|---:|---:|---:|---:|
| LATEST |8|2|4|0|12|
| FIFO |14|2|2|0|10|
| ORIGIN_BOUND |0|0|2|2|22|

Wrong-origin and duplicate-acceptance categories may overlap; never add them. Repeated reporting
is not a repeated GUI effect. Every real application final counter is2. Reporters grant no input
authority and assert no whole-task success. Raw-only audit1412 checks/errors0;12 effective
corruption controls;72 process exit records plus2 supervisors all0;25 source freezes unchanged.

C: cooperative app, distinct keys used once, exact initial request-to-received-event mapping,
one sender/session. Same-author separate algorithm/process audit is not independent human review.
This is not a defect allegation against the backend. Hashes do not authenticate receipts.

U: repeated identical keys, missing receiver events, restart, arbitrary GUI, natural failure rates,
model/task/token/latency benefits, MAP01 and production adoption remain untested.

Environment: supplied Linux x86_64 container, CPython3.13.5, Tk8.6.16, Python-Xlib0.15.
No Docker/OrbStack image attestation, model call, user desktop or experimental network.
Complete unchanged X11 backend/core admission were exercised, not the whole public CLI/API.
Upstream backend blob9cae101a219348077668c8fc086acf8e13154afe and core contract
blob87154518107e4231a6f8ec06e976d2856b375d1d remain exact inside the archive.

## Complete evidence, not only a review summary

Nine binary fragments reconstruct all270 original files /3957752 member bytes, including
all12 formal sessions,2 construction sessions, native app/process records, complete source,
original plans/freezes, independent audit/control code and first outputs, Apache2 license,
and historical publication HOLD statements. Those historical statements are not current
connector-capability claims. The archive is68036 bytes, SHA256:
`61df111f2c274b4520d810656bade1f6c437884d867e48de1ca2f01b13e34ab5`.
Original280442-byte ZIP SHA256:
`928690152325f6122af9764d2f72203dca25c86556dd09191bee348cb3d8b23a`.

`ARCHIVE.json` binds every fragment's SHA256/size/Git blob, the original manifest and freeze.
`readable/reporter.py` is the exact original reporter. All other original sources and the full
Japanese report are restored without modification. `ORIGINAL_REVALIDATION*.json` records this
continuation's read-only reconstruction, not a rerun of actors.

After reviewing the code, from this directory with Python3.13 standard library:

```sh
python -S -B verify_evidence.py
python -S -B -m unittest -v test_restore
```

The verifier restores into a new temporary directory, validates all original bytes, and runs
only the original saved-data audits/controls and unit tests. It does not start an app, Xvfb,
backend, provider, scientific runner or old allocation. It requires a trusted/quiescent checkout
and temporary parent; checksums are integrity rather than an independent trust root. Packaging
checks do not add scientific samples.

## Adoption boundary

Keep the current operation label separate from deferred effect origin. Missing origin is not
proof of no effect, and cannot authorize blind replay. The mapping from native input requests
to application-received events remains an explicit assumption. Evidence delivery/CI/review,
application semantics and product acceptance are separate. #34/#2789 and global ROADMAP remain open.
