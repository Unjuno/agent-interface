# Actual CLI usage endpoint feasibility

A local `codex exec --json` probe read the known unsaved-confirmation screenshot from browser-emission-live-01. The prompt prohibited tools and requested saved/evidence/next_action JSON. Flags were --ignore-user-config --ephemeral --sandbox read-only --skip-git-repo-check, with the result directory as working directory and the image attached explicitly. No model override was selected. Prompt, CLI version, JSONL, stderr, exit status and an independent audit are saved in results/model-boundary-probe-01. The shell invocation supplied the saved prompt through stdin; it did not inspect authentication files or private session histories.

The emitted sequence is thread.started, turn.started, one completed agent message, turn.completed. The answer correctly says saved=false, quotes the visible unsaved warning and proposes the confirmation checkbox then Save. The usage event reports input_tokens=17477, cached_input_tokens=0, cache_write_input_tokens=0, output_tokens=49 and reasoning_output_tokens=0. Exit status is 0. No command/tool execution items were emitted. Audit binds the attached image hash to the earlier live screenshot and checks the actual event sequence and usage value types.

This changes the next action: actual turn usage is available for a separately run CLI experiment, so a controlled comparison harness can consume provider-reported numbers instead of estimating tokens from bytes. These are totals for that CLI turn, not attribution to the screenshot, not parent conversation usage and not a compression win. The default model identity was absent from JSONL. Actual model receipt time and cost remain unknown. PowerShell redirected output is preserved as produced; no raw UTF-8 transport-byte claim is made for these files.

Unexpectedly, stderr contains PowerShell snapshot and Cloudflare MCP initialization warnings despite --ignore-user-config. The process still returned the requested answer and usage, but isolation is unproven. Do not silently remove the diagnostics or promote this as a matched model/task/environment test. Next establish explicit model selection and a reproducible tool/instruction environment, capture local event-arrival timestamps separately from model timestamps, and preregister paired presentation arms. No additional model runs are needed merely to reconfirm this endpoint.

Official documentation checked: [Non-interactive mode](https://learn.chatgpt.com/docs/non-interactive-mode), accessed 2026-09-13, documents JSONL lifecycle events and a turn.completed usage object. The installed CLI help independently confirms --json and the selected flags. The documented sample is not treated as experimental data; the numbers above come from the actual saved run.
