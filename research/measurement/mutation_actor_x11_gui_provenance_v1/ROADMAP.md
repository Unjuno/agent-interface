# ROADMAP — MUTATION-ACTOR-X11-GUI-PROVENANCE-20260918-001

## H
The fixed lineage-bound actor classifier remains correct when a separate process causes a real private-X11/Tk visible mutation. A pixel-only scorer confirms whether the GUI changed; actor attribution remains receipt-lineage-bound. A 500 ms temporal-nearest baseline will falsely self-credit non-self mutations.

## T
1. Private 320x240 Xvfb + isolated persistent Tk fixture.
2. Fresh injector child for every formal case; Unix socket request/response.
3. Nine frozen families: self, same-session other intent, external process, HUMAN injector, OS injector, no witness, self+HUMAN conflict, mismatched lineage, no mutation.
4. Independent pixel scorer receives only display+bbox and maps exact ROI pixels to visible state; no actor metadata.
5. Candidate lineage classifier fixed from prior rungs. Independent actor oracle uses frozen family schedule, not candidate logic.
6. Excluded construction on 18 cases; then source-first freeze/readback/ownership reread.
7. Exactly one formal 360-case invocation (40/family), reruns/replacements/tuning0.

## D
PASS iff actor candidate/oracle mismatch0; non-self SELF false credit0; exact self and explicit external actor classes exact; no-witness/conflict/mismatch UNATTRIBUTED; no-mutation exact; pixel scorer agrees with fixture visible state360/360; temporal-nearest false-self>0; capture/fixture exceptions0; child cleanup100%; authority/task-success promotions0; source/audit integrity passes.

## C
A cooperating Tk fixture can retain richer provenance than ordinary desktop applications. A PASS validates composition, not general actor discovery or authentication.

## U
Private Xvfb/Tk synthetic GUI only. HUMAN/OS are controlled injector labels. No model/provider/network/user-data/shared-runtime/real OS task input/production claim.

## STOP
One formal block only. No threshold, color-map, classifier, cadence, or lineage changes after formal starts.
