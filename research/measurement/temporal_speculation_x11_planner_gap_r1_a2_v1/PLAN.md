# TEMPORAL-SPECULATION-X11-PLANNER-GAP-R1-20260918-002

Fresh A2 successor to consumed #1191. Scientific H/T/D/C/U unchanged. Parent partial rows pooled0.

Exact mechanism: 12 matched scenarios x3 arms; current pixels x=160; histories left/right; magnitude1/magnitude2 balanced; K=2 CURRENT=(-1,+1), TEMPORAL direction-specific (±1,±2), WAIT none; future+100ms, planner return+130ms, validity+120ms. Fixture-local effect requires fresh matching future evidence, unexpired branch, unchanged current digest and external admission token. No OS task input/model/provider/shared runtime.

Only mechanism change: formal runner launched exactly once through `launch_detached.py`, which writes an invocation sentinel/PID and returns. Later calls only poll the same PID/result. Fresh displays960..995. Formal invocation1/reruns0. Gates unchanged: temporal future->effect median<5ms,p95<8ms; CURRENT−TEMPORAL mean>=10ms; WAIT−TEMPORAL mean>=20ms; hits WAIT0/CURRENT6/TEMPORAL12; exact matched current digest; wrong/stale/authority effects0; exceptions0.
