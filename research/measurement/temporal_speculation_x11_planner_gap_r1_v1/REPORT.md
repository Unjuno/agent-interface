# #1191 first allocation stop

Status: **STOPPED_OUTER_EXECUTION_TIMEOUT**. Scientific disposition: **NONE**.

The source-first formal supervisor was launched exactly once. The external 45 s execution ceiling terminated it before `RESULT.json` or `AUDIT.json` was written. Reruns: 0.

Post-stop read-only inspection found 27 case directories, 26 complete fixture EFFECT_APPLIED+STOPPED logs, 0 fixture ERROR logs, and 0 residual Xvfb/Tk/runner processes. These partial case logs are failure evidence only and are not pooled or scored. Frozen source rehash remained exact.

A fresh successor may preserve every scientific condition and change only the outer transport to one detached supervisor PID plus polling. #1191 is never rerun.
