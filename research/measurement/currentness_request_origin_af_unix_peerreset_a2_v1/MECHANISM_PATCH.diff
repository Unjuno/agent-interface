--- #1243/runtime_server.py
+++ #1250/runtime_server.py
@@ -86,7 +86,10 @@
 
     def handle(self):
         while True:
-            raw = self.rfile.readline(MAX_LINE + 2)
+            try:
+                raw = self.rfile.readline(MAX_LINE + 2)
+            except ConnectionResetError:
+                return
             if not raw:
                 return
             recv_ns = time.monotonic_ns()
