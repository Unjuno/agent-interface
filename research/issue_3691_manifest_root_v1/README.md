# Issue #3691 manifest trust-root reproduction

## H/T/D/C/U

- **H:** The open Issue #3691 auditor authenticates raw and predecessor-freeze bytes using digests loaded from a caller-controlled study manifest. If that manifest is replaceable, coordinated replacement of raw + predecessor freeze + study manifest can recover PASS despite all internal checks.
- **T:** Reproduce the exact current PR #3697 head finding (d55849d6b7e88dd48963c6aaad3f9ecc845d2959) and inspect the current Issue #3691 branch head (71ab9b6a0149695184282bc200cc544290fa42bb) without modifying either branch. Use exact source and evidence from GitHub blobs, record their Git blob SHA values and independently computed SHA-256 values, then construct replacement raw/freeze/manifest wholly in a temporary directory. Compare canonical invocation to the coordinated replacement invocation and preserve the original evidence byte-for-byte.
- **D:** PASS_REPRODUCTION only if the exact current source returns PASS for canonical artifacts and also returns PASS for a self-consistent coordinated replacement; FAIL if it rejects the coordinated replacement; STOP if exact inputs cannot be reconstructed. This result tests the trust-root boundary only, not Docker validation or XRes.
- **C:** No repository checkout mutation, no Docker/container invocation, no network in the harness, no X11/GUI/input/model execution, no formal allocation rerun, no modification of #3675/#3676/#3691 evidence or PR #3697 branch.
- **U:** A reproduction shows this specific artifact-authentication gap, not arbitrary auditor unsoundness or runtime/security claims.

## Frozen inputs (GitHub contents API, branch head 71ab9b6a0149695184282bc200cc544290fa42bb)

- research/issue_3691_audit_integrity_v1/audit.py Git blob: 67fdda7cdd141803351ff98825b68c08728413e7.
- research/issue_3691_audit_integrity_v1/test_integrity.py Git blob: 94a640767f0dcf7e74ab355c9270c2873bc8126a.
- research/issue_3691_audit_integrity_v1/evidence/raw.json Git blob: 5bea90956ecf5cf0ec130f72b18769bebb01f9b0.
- research/issue_3691_audit_integrity_v1/evidence/predecessor_FREEZE.json Git blob: 4831ebb5657789fd12e694d366844d1a2432ba36.
- The study manifest used by the PR is generated by test_integrity.py::study_manifest(); it has no external digest pin. The branch also retains FREEZE.json, but the CLI does not authenticate that file and accepts an arbitrary --study-freeze path.

## Reproduction recipe

1. Fetch the four exact files by the recorded GitHub refs/blobs and verify both Git blob identity and SHA-256 before use.
2. In a temporary directory, preserve the canonical bytes. For the challenge copy, set both fixture pixel_sha256 values to 1 repeated 64 times; append one space to predecessor freeze bytes; update raw freeze_sha256 to that replacement freeze's SHA-256; create a replacement study manifest pinning the replacement raw and freeze hashes and unchanged current source hashes.
3. Run the exact branch auditor against canonical artifacts and the coordinated replacement artifacts with independent output locations. Record full JSON outputs and all computed digests.
4. Do not convert a native reproduction into Docker PASS. Docker Desktop on this host remains unavailable; Issue #3691 requires local Docker and explicitly disallows Actions substitution.

## Current status

Not yet executed. Exact source/evidence retrieval is underway; no result is claimed by this preregistration.