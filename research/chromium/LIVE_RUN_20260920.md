# Chromium live receipt rerun — 2026-09-20

## H/T/D/C/U

- H: A receipt bound to the live Chromium main-window XID should admit only the matching session/resource/generation; a replacement XID must be rejected.
- T: Real Chromium `mixed-formal-2992-debian:20260920`, Docker `--network none`, Xvfb `:155`, two isolated profiles, local `file://` fixture.
- D: `xid1=4194307`, `xid2=12582915`; valid row admitted, session restart/resource replacement/duplicate rows denied; `model_calls=0`, `network_calls=0`. The valid effect was not observed. `BadMatch` occurred during X11 frame access (`resource_id=4194307`).
- C: FAIL for the live-effect gate. The binding-negative rows pass, but this run does not establish a Chromium receipt-to-effect transfer.
- U: The remaining blocker is safe observation/input on the Chromium X11 window, not XID replacement isolation. Do not promote this to a reusable Chromium success result.

## Formal decision

`FAIL_CHROMIUM_LIVE_RECEIPT`

This is a new execution record and does not alter the prior failure record. The distinct replacement XID is evidence that the earlier XID-reuse confounder was removed; it is not evidence of a successful live effect.
