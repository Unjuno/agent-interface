# Issue #3212 DOM-oracle rerun — 2026-09-20

This is an additive successor execution. The earlier FAIL and HOLD records are preserved unchanged.

## H/T/D/C/U

- H: A receipt bound to a real Chromium session/resource/generation can admit the matching action, produce an independently observed application effect, and reject session restart, target replacement, and duplicate replay.
- T: `mixed-formal-2992-debian:20260920`; Docker `--network none`; Xvfb `:155`; real Chromium `--app=file:///workspace/page.html`; isolated profiles; Xlib/XTEST for input; read-only Chromium DevTools Protocol DOM oracle on localhost; no model calls and no external network.
- D: `xid1=4194307`, `xid2=12582915`. The valid action entered `abc` and submitted. Independent DOM oracle returned `{"title":"saved:abc","saved":"true","value":"abc"}`. Session restart, replacement resource, and duplicate receipt were denied before effect. No X11 `BadMatch` occurred in this run.
- C: `PASS_CHROMIUM_LIVE_RECEIPT_SCOPED` for this frozen fixture and declared transitions. This is not a universal browser or production claim, and it does not establish a reuse-latency benefit.
- U: The reusable-vs-fresh benefit, broader lifecycle cases, and production/browser generalization remain unmeasured. Preserve this scoped PASS separately from the prior HOLD and FAIL records.

## Raw result

```json
{"decision":"PASS_CHROMIUM_LIVE_RECEIPT_SCOPED","xid1":"4194307","xid2":"12582915","valid":{"admitted":true,"effect":true,"title":"","dom":"{\"title\":\"saved:abc\",\"saved\":\"true\",\"value\":\"abc\"}"},"negative_cases":["session_mismatch","resource_mismatch","duplicate"],"model_calls":0,"network_calls":0}
```

The window-manager title accessor remained empty, so the DOM oracle—not the title accessor or a frame hash—is the effect authority for this result.
