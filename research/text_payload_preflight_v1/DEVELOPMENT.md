# Development retained before source freeze

1. Local GitHub HTTP read failed DNS resolution. Repository reads/writes use the authorized GitHub connector instead. The exact baseline text was materialized from connector content and its Git blob verified as b4f8e043ce4f8929d446e038418ea0fd3655bab0.
2. Proposed nonclipboard Unicode accessibility route was preflighted but not executed: no Python gi/pyatspi bindings, no at-spi bus/registry executables, and only LibreOffice libvclplug_genlo.so (no GTK VCL plugin) were present. libatspi alone does not establish a working route. No accessibility result is claimed.
3. development-01 ran four GUI payload probes plus four context negatives. Candidate normal payloads refused because harness used Tk Entry.winfo_id as X input focus. Tk's logical widget focus differed from the actual X focus window. Negative preflight gates passed. Preserved results: prepared 5/8, baseline 1/4, all side-state/release controls passed.
4. Harness was repaired by reading actual X input focus and verifying it is in the receiver widget's ancestry (excluding X root). development-02 then passed prepared 8/8 while baseline remained 1/4. No candidate text-lowering change was needed for this focus repair.
5. The initial source-reading suspicion was that underscore might turn into minus. Actual baseline development evidence instead shows `unmapped key _` after emitting `office`. The report must retain the observed error, not the earlier hypothesis.
6. Six development Writer sessions found baseline durable `sentinel office` for underscore/newline/exclamation inputs. Candidate saved exact underscore/exclamation strings and left `sentinel ` unchanged for newline. These development sessions are not pooled into formal counts.
7. Pure planner rejected malformed maps, oversized/invalid text, missing symbols and missing Shift. Final development hardening added explicit outer map-list type validation. Frozen formal tests remain 13 named tests.
