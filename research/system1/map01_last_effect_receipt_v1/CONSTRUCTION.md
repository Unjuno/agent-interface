# Construction record

A direct disposable-container `git clone` from `https://github.com/Unjuno/agent-interface.git`
was attempted before analysis and failed at DNS resolution (`Could not resolve host:
github.com`). No scientific output was produced by that attempt.

Construction therefore uses GitHub connector reads pinned to the declared base/source
identities, writes only the required retained text/JSON bytes into `/tmp`, and runs the
deterministic analysis there. This acquisition-path substitution does not change the
frozen representation factor or decision rule. Exact source identities are retained in
the audit; no live model/GUI/game/OS-input call is introduced.