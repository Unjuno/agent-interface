# Keep copied predecessor source and raw training output byte-identical across hosts.
intent_alignment_3442_pilot_01/** -text -whitespace
# Keep the independent audit bundle's frozen Python/manifest bytes stable across hosts.
intent_alignment_3442_audit_v1/** -text
