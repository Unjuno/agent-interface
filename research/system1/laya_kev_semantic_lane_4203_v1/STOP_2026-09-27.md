# Local checkpoint-fetch STOP — 2026-09-27

**Disposition:** STOP before construction/model inference. No model was loaded and no formal evaluation row was run. This is an acquisition failure, not model evidence and not a FAIL/HOLD result about either model.

## Machine and container path

- Windows host, NVIDIA GeForce RTX 3080 Laptop, 16,384 MiB VRAM; NVIDIA driver 581.57, CUDA driver capability 13.0.
- Docker Desktop 29.8.0, Linux x86_64 daemon, 20 CPUs, 16,633,602,048 bytes RAM; Docker is responsive.
- GPU was idle (11 MiB display allocation) at intake. No CUDA container/model smoke was run.
- The ordinary repository shallow clone failed with curl receive reset/early EOF. A managed worktree could not be created because the calling workspace root is not a Git checkout. Existing dirty checkouts were not modified.

## Exact acquisition evidence

- `hf 1.29.0`.
- `hf download --dry-run convaiinnovations/laya-typed-decisions --revision 1a793eb568e6718f15941d08f85432581df534e3` returned 7 files, 846.2 MB total; weight file is 842,609,220 bytes. Hub tree API confirmed all names/sizes at the exact pinned commit.
- Real `hf download` was unauthenticated. It repeatedly timed out resolving/downloading `tokenizer/tokenizer.json` through `https://huggingface.co/api/resolve-cache/...`, then failed with `FileNotFoundError` when its incomplete cache parent was absent. This did not produce a usable snapshot.
- Direct `curl.exe --location --retry 2 --connect-timeout 15 --max-time 120` against the same exact pinned tokenizer URL started (HTTP redirect succeeded), received only 176,128 bytes of the 3,583,228-byte file (~4.9%), slowed below 15 KiB/s and stalled for 45 seconds. It was manually interrupted. Partial `.part` data is not valid evidence or a usable model; do not treat it as a complete file.
- A HEAD request to Hugging Face API returned HTTP 200, which shows API metadata/control-plane reachability but not viable artifact throughput.
- Kev weights were not attempted because even the smallest Laya tokenizer transfer failed. No model install/build, image construction, model load, inference, training, or formal row was run.

## Resume gate

Resume only when the pinned artifact files can be fetched fully and verified against the Hub LFS SHA256 (for the Laya checkpoint: `4fa56de72383a9d3efa9cfa78955733c81b9fc8067a587ca4beb82c78107a24e`; metadata and small files must also match the pinned tree). Prefer a reliable authenticated HF transfer or a locally provided, independently hash-verified artifact cache. Do not switch revisions, mirrors, checkpoints, or evaluation rows without a new additive successor protocol and issue record. After staging both model packages, use separate local Docker environments, run only construction/dummy smoke first, freeze resource/time caps and input/oracle/audit hashes, and only then consider the preregistered one-shot formal evaluation.

## Preserved outcome

- Preserve the historical Linux STOP and this local fetch STOP separately; neither claims the comparison is scientifically negative.
- No retry loop against the same slow route, no remote inference fallback, no fine-tuning, and no expansion to a larger model.
- Resume checklist/protocol: `README.md`; pinned input notes above.
