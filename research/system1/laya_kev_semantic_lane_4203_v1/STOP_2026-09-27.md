# Local checkpoint-fetch STOP — 2026-09-27

**Disposition:** STOP before model inference/formal rows. No model was loaded and no formal evaluation row was run. This is an acquisition failure, not model evidence and not a FAIL/HOLD result about either model.

## Machine and container path

- Windows host, NVIDIA GeForce RTX 3080 Laptop, 16,384 MiB VRAM; NVIDIA driver 581.57, CUDA driver capability 13.0.
- Docker Desktop 29.8.0, Linux x86_64 daemon, 20 CPUs, 16,633,602,048 bytes RAM; Docker is responsive.
- GPU was idle (11 MiB display allocation) at intake.
- The ordinary repository shallow clone failed with curl receive reset/early EOF. A managed worktree could not be created because the calling workspace root is not a Git checkout. Existing dirty checkouts were not modified.

## Exact acquisition evidence

- `hf 1.29.0`.
- `hf download --dry-run convaiinnovations/laya-typed-decisions --revision 1a793eb568e6718f15941d08f85432581df534e3` returned 7 files, 846.2 MB total; weight file is 842,609,220 bytes. Hub tree API confirmed all names/sizes at the exact pinned commit.
- Initial `hf download` with custom `HF_HOME` lacked the normal CLI credential and timed out repeatedly on the 3.58 MB tokenizer via `api/resolve-cache`.
- Diagnostic correction: `hf auth whoami` reports user `Unjuno`. Passing the CLI token only through subprocess `HF_TOKEN` and using `--local-dir` successfully fetched the exact pinned `tokenizer/tokenizer.json` (3,583,228 bytes), tokenizer config, encoder config, and metadata. Token contents were not printed or committed.
- Under that authenticated `--local-dir` invocation, the 842,609,220-byte model payload remained absent/0 bytes for ~6 minutes while Hub/CDN connections became idle/intermittent. The dedicated `hf download` was interrupted; no usable checkpoint snapshot exists.
- A separate unauthenticated direct curl probe of the same tokenizer stopped after 318,966 bytes in 20 seconds (an earlier attempt stopped at 176,128 bytes). A 337-byte config file did transfer successfully. Partial `.part` files are not valid evidence.
- Follow-up: enabled the installed Hub client's documented `HF_XET_HIGH_PERFORMANCE=1` transfer mode and retried the same exact revision into a separate local directory. This time even the 3.58 MB tokenizer again timed out repeatedly at `api/resolve-cache`; the attempt was stopped after ~1 minute. The high-performance mode did not reach model transfer.
- A HEAD request to Hugging Face metadata returned HTTP 200; metadata/control-plane reachability does not establish artifact bandwidth.
- Kev weights were not attempted. No model install/build, model load, inference, training, or formal row was run.

## Duplicate construction smoke (not new study evidence)

For transparency, this session ran a bounded local Docker CUDA construction probe for #4470's separable pooling mechanism:
`docker run --rm --gpus all --network none --read-only --tmpfs /tmp:rw,noexec,nosuid,size=512m --memory=4g --cpus=4 -e CUBLAS_WORKSPACE_CONFIG=:4096:8 pytorch/pytorch:2.5.1-cuda12.1-cudnn9-runtime ...`
PyTorch 2.5.1+cu121 on RTX 3080, deterministic algorithms enabled: output shape [16,16,8,10], max error 1.7881393432617188e-07 vs adaptive-pool reference, two repeated outputs and gradients bit-identical. This duplicates already merged #4482 construction evidence and adds no scientific result; no optimizer step or model experiment occurred.

## Resume gate

Resume #4203 only when both candidates' exact pinned artifacts can be fully fetched and independently hashed (Laya checkpoint SHA256 `4fa56de72383a9d3efa9cfa78955733c81b9fc8067a587ca4beb82c78107a24e`; all metadata/small files must also match the pinned tree). Prefer a working authenticated transfer or a locally supplied, independently hash-verified artifact cache. Do not switch revisions, mirrors, checkpoints, or evaluation rows without a new additive successor protocol/issue record. Once both packages are staged, use separate local Docker environments, run construction/dummy smoke first, freeze resource/time caps and issue-matched inputs/oracle/audit hashes, and only then consider the formally authorized comparison.

## Preserved outcome

- Preserve the historical Linux STOP and this local fetch STOP separately; neither claims the comparison is scientifically negative.
- No remote inference fallback, fine-tuning, or larger model. No formal run is authorized by this record.
- The earlier proposed 64-row grid was withdrawn in the branch README; it was not derived from the Issue. Row count and gates remain TBD until the complete Issue contract is frozen.
- Resume checklist/protocol: `README.md`; pinned input notes above.
