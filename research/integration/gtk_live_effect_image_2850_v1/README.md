# GTK live-effect image successor for #2850

Additive image only. This preserves `agent-interface-gtk-fixture-v2:2748` and
adds the missing `python3-xlib` dependency identified by the local #2850 run.
The image is for a bounded local Docker/Xvfb allocation; it makes no formal
acceptance claim by itself.
