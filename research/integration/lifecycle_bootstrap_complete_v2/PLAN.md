# Allocation02 under Issue4039 — same science, corrected external budgeting

The original one-shot18-case allocation is immutable STOP_SUPERVISION_TIMEOUT / HOLD_FORMAL_INCOMPLETE. Its9 completed rows and one partial case are not reused. PR4048 retains that first evidence. No new scientific Issue is created; this is an engineering correction under the same question as directed by2789.

The excluded six-case construction's actual elapsed supervision was23.260977987s. The original30s budget for18 cases was inadequate planning. This observation is a diagnostic, not a performance benchmark. It cannot guarantee future durations.

## H
An existence-only bootstrap inventory can distinguish the ordinary exit of a preexisting unrelated window from unexplained loss, preventing unnecessary global invalidation of a correctly observed target. Existing windows receive no invented birth tokens. Target destruction, observer restart and explicit gaps stay UNKNOWN. This is the unchanged4039 hypothesis, not a new mechanism.

## T
Allocation lifecycle-bootstrap-4039-02. New local path /mnt/data/lifecycle_bootstrap_complete_v2, repository path research/integration/lifecycle_bootstrap_complete_v2. Same six cases in unchanged order, repeated three times,18 fresh Xvfb/actor lifetimes and36 paired policy outputs. Three immutable batches contain indices0-5,6-11,12-17. Every batch begins with all earlier raw hashes and actual outer exits checked. Exactly one attempt per batch; a missing or failed batch stops the allocation. No old-row pooling or formal retry.

The original case(), actor, policy, Tracker, predecessor,12 unit tests,12 semantic corruption challenges and raw auditor are byte-identical copies; only their containing path changes. The old run.py entrypoint is never invoked. New batch.py wraps case(), supervise.py bounds each actual batch to40s and records its actual exit/stdout/stderr, aggregate.py explicitly joins the three complete terminals, audit_batches.py independently verifies the envelope and calls the unchanged raw audit. The aggregated exit field is explicitly labeled aggregate_of_three_actual_batch_exits and must never be described as a single monolithic process exit.

Commands, from this directory, serially: python -B supervise.py 0; then1; then2, only after actual prior exit0. Each outer container tool call allows45s. After all three pass: python -B aggregate.py; python -B audit_batches.py. Sources and gates hash-committed on GitHub before batch0. Full source bytes are published after measurement.16 preformal unit methods (12 inherited plus4 wrapper guards) must pass. No new live construction is pooled.

Policy input remains exact allowlist: epoch,epoch_after,target,bootstrap,before,after,gap. Epochs opaque random hex; no scenario/path/oracle/witness data. Private authenticated Xvfb/TCP disabled, separate fixture/policy processes and witness X connection. No model/input/network experiment/install. Same provided Linux x86_64/CPython3.13.5 environment; not Docker/OrbStack attested.

## D
Unchanged18-case scientific gates: three preexisting-bystander exits yield legacyUNKNOWN/candidateMATCH; nine unchanged/known-bystander/repaint positives MATCH under both; three target destruction and three observer restart cases UNKNOWN under both. All bootstrap birth tokens remain null. Full raw/source/native event/pixel/process/cleanup/input-separation audit must have no errors;12 semantic corruptions reject. Additionally all three actual recorded batch exits are0, ranges/prefix hashes/source hashes exact, no retry, no overlap, and independent envelope controls reject. Only then PASS_BOOTSTRAP_LIFECYCLE_SCOPE_SCOPED. Any completed candidate gate miss FAIL; incomplete provenance, timeout or missing batch HOLD/STOP. Never use original9 rows to complete this denominator.

## C/U
Batch boundaries change idle scheduling; timings are diagnostic only. Candidate receives extra bootstrap information; no equal-information speedup/model-call savings. Quiescent bootstrap/direct-root windows only. No reparenting, XID reuse, automatic gap detection, authenticated ownership, check/input atomicity, model/GUI task quality or production promotion. Independent audit means separate implementation, not external human approval. The previous subscription-handoff HOLD remains unchanged and separate.

## Variables and units
| Symbol | Meaning | SI unit | Definition/domain | Type |
| i | zero-based fresh case index |1|integer0..17|scalar integer|
| j | batch index |1|integer0..2|scalar integer|
| t | monotonic diagnostic timestamp |s|same-container recorded ns converted to seconds|scalar real|
| deadline | process supervision bound |s|40s per six-case batch, not a latency guarantee|scalar real|
Counts/ranges are dimensionless; process limits and elapsed differences have units of time. No statistical uncertainty interval or coverage factor is claimed; scheduling and source coverage dominate unquantified external uncertainty.

## Roadmap
Keep oldSTOP -> source/budget freeze -> three first-outcome batches -> explicit aggregate/raw-envelope audits -> additive evidence PR -> qualified checks/main readback. This closes only the finite question if gates pass, not globalROADMAP; new failure remains under4039, not a wrapper successor.
