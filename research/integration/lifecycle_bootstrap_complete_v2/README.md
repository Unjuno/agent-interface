# Issue4039 — bootstrap lifecycle scope, complete allocation02

**PASS_BOOTSTRAP_LIFECYCLE_SCOPE_SCOPED**, for this18-case observation-only fixture. This is not a runtime adoption, general safety, model utility or product claim.

The first allocation remains STOP_SUPERVISION_TIMEOUT / HOLD_FORMAL_INCOMPLETE in `../lifecycle_bootstrap_scope_v1/`, published by PR4048 (merge6e5a6dd60cd93aee300cd4bd11712bb9d6bd8520). Its9 completed cases and partial tenth are not included here. Allocation02 is a new, publicly hash-frozen set of18 fresh cases under the SAME scientific Issue, not a wrapper-successor question. There were two allocations in total, not one secretly retried allocation.

## Scientific question and result

The exact archived #3950 Tracker globally invalidates when a DestroyNotify has no observed CreateNotify. That is conservative for unexplained loss, but an unrelated window already present at observer startup has a legitimately unobserved birth. Its later ordinary destruction can poison otherwise continuous target evidence.

The candidate consumes one extra, quiescent root QueryTree bootstrap inventory. Those objects are known to exist but have unknown births; no lifetime token is fabricated. Their first explained destruction removes them from that set without invalidating observed target births. Unexplained events, native-event provenance failure, declared gaps, target destruction and reconnect must not become MATCH.

| Condition | New cases | Exact legacy Tracker | Bootstrap-aware |
|---|---:|---|---|
| Unchanged |3|MATCH3|MATCH3|
| Observed later-created bystander destroyed |3|MATCH3|MATCH3|
| Preexisting bystander destroyed |3|UNKNOWN3|MATCH3|
| Same-content target repaint |3|MATCH3|MATCH3|
| Target destroyed |3|UNKNOWN3|UNKNOWN3|
| Observer restarted |3|UNKNOWN3|UNKNOWN3|

All18 raw cases /36 classifications independently reconstruct. Bootstrap birth tokens remain null. All36 actor/server exits are recorded0, root trees/keymaps/buttons are terminally empty and18 server sockets disappear.12 inherited semantic corruptions reject;6 postformal envelope corruptions reject.16 preformal test methods pass. `AUDIT.json` errors=[] and all3 actual batch receipts verify.

These are deliberately chosen finite cases, not failure rates, statistical generalization, avoided model calls or speed gains. The candidate has extra bootstrap information; equal-information superiority is not claimed. Both policies receive the same event stream. Current inventory alone does not reconstruct old identity.

## What changed after the STOP

The original six-case construction lasted23.260977987s. Assigning30s to18 cases was a local budgeting error. The scientific case function, actor, policies, baseline, raw auditor and inherited tests are byte-identical. Only containing paths and outer serialization/supervision changed.

Three serial, immutable batches own cases0-5,6-11,12-17. Each was invoked once with40s supervision and a45s container-call envelope. Before the next, actual prior exits and raw hashes had to match. No old rows, replacement cases or post-result tuning. All3 subprocess exits are0; exactly one allocation02 logical schedule. `formal.execution.json` is explicitly an aggregate of three actual exits, not a fictional monolithic exit.

The public FREEZE commit f53611b6611a4946f1654f27b7eded8d63b7d0e3 and Issue4039 comment5768492499 preceded batch0. SHA256890bb0221e01a9a00cec1cb3f53204fd76a773e796111650eba67b9d9b9193ec binds15 source/plan/environment files. Full source bytes were published after the measurements; public commitment was to their hashes. The6 copied-data envelope controls are explicitly postformal and never execute a case.

## Observation and input boundaries

Separate cooperative fixture process; observer and independent witness X connections; actual root trees, subscription masks, native event fields and48x32 exact BGRX pixels. Only the observer's projected events and bootstrap inventory enter the policy. Each policy runs in a separate `python -I -B` process, cwd=/tmp, sanitized environment. Exact stdin allowlist: epoch,epoch_after,target,bootstrap,before,after,gap. Epochs are opaque random hex. No scenario/path/expected answer/witness field is passed. This addresses the previous conversation's metadata-separation mistake without changing its HOLD.

Provided Linux6.18.44/x86_64 execution container, CPython3.13.5, installed Xvfb/Python-Xlib. No Docker/OrbStack image attestation. Private authenticated Xvfb with TCP disabled, no model/provider, keyboard/mouse/XTEST, host desktop, user documents, installation or network experiment. Runtime binaries and environment hashes are in ENVIRONMENT.json. Timings are diagnostic, not benchmarks.

Scope assumptions: quiescent commissioning, direct-root children, unchanged subscriptions, native events and trustworthy explicit gap state. No reparenting, same-XID reuse, arbitrary lost-event detection, authenticated XID ownership, action/check atomicity, crash persistence or cross-platform qualification. A model benefit remains unmeasured. Independent audit means separate implementation/process by the same worker, not external human review.

## Recheck retained data only

Run `python -I -S -B unpack.py /absolute/new/review-directory`. The unpacker validates bounded compressed/expanded hashes, per-member hashes, paths and fresh output; it never imports study code. From the restored directory:

```
python -B audit_batches.py > /tmp/4039-allocation02-audit.json
cmp AUDIT.json /tmp/4039-allocation02-audit.json
python -B mutation_checks.py formal-02
python -B -m unittest test_contract test_batches -v
```

Do not invoke consumed supervise.py/batch.py/run.py/aggregate.py entrypoints. All actual input wires, raw rows, original source, actual process receipts, source freeze, audits, negatives and construction-only tests are in the byte-exact capsule. The previous subscription-handoff24-case HOLD's full ZIP remains conversation-hosted; only its recheck/chronology record is published in the earlier v1 capsule.

## Integration decision

The finite #4039 boundary is now evaluated, while the original STOP remains unchanged. Compatibility decision: retain as observation-availability research only; do not install into the shared runtime. The next practical acceptance is a real integration-path comparison with explicit subscription coverage and actual rich-model task accounting, not more wrappers or a favorable-tail rerun. #2/#2789 and repository ROADMAP remain open.
