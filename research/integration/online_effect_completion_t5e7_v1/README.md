# Online application-effect completion — Issue #4321

Source-first freeze for allocation `online-effect-completion-4321-20260924-t5e7`.
Base main: `55d6c6cead19fc02824d620c174bf0fd03681cf1`.

Native completion and release are not task completion. Compare RETURN_ONLY with EFFECT_BOUND after ordinary public Python dispatch. The latter consumes a bound post-dispatch app snapshot and separately read file bytes. Eight directed schedules (SUCCESS, ENTRY_READONLY, FILE_CHANGED, EVIDENCE_MISSING, FOREIGN_EVIDENCE, STALE_EVIDENCE, QUEUE_CHANGED, LEASE_EXPIRED), two repetitions,16 sessions,32 paired decisions. Policies grant no input authority and never replay input.

The exact PLAN,32-file FREEZE, environment, runner, app, independent auditor,12 corruption controls and16 unit tests are retained losslessly in SOURCE_MANIFEST.json plus six parts. The15 vendor source blobs are unchanged current-main code. Source archive SHA256: `88f1afbcd09445c4eabd65bb054ae2658cc98f1f27d2b4fa1ad936d664e05c47`. FREEZE SHA256: `189be4b0369d9bb0155b970c2057661e8a137c56426c335a954cb9ae5f0092da`.

Excluded construction:2-case smoke plus8-case full schedule,16 unit tests and12/12 effective corruption controls passed. Formal has not started at this source commit. Two immutable8-session batches will run once only after public source readback. First failure/HOLD/STOP remains retained; no reruns or post-result tuning.

Read-only source restoration/verification (never starts GUI):
```sh
python -B verify_publication.py
```
The data-only unpacker rejects existing destinations, non-regular/unsafe archive members, hash mismatches and oversized expansion. Use a trusted destination parent. It is not a sandbox for untrusted filesystems.

Scope: private authenticated TCP-disabled Xvfb/Tk, supplied Linux/CPython, cooperative app and fixture-owned currentness. No Docker image attestation, authenticated source truth, atomic snapshot/check-use, crash/power-loss, CLI/MCP-host, model/token/latency, generic application or product claim. Online success is an observed point-in-time postcondition, not future persistence or causal authorship. Same-author independent implementation is not external human review.

The earlier c3a8 experiment remains local engineering evidence. Its report/audit/provenance will be retained in the result capsule; its full old ZIP remains a separate conversation artifact and is not falsely claimed as included here. The global ROADMAP remains open.
