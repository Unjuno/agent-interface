# GTK live matrix preflight for #2850

This additive record preserves one local Docker/Xvfb run of the existing
eight-case runner using the dependency-repaired GTK image lineage. It is a
preflight record, not formal #2606 acceptance.
