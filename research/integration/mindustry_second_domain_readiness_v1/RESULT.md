# Mindustry second-domain integrated readiness v1 — retained first outcome

Task: `MINDUSTRY-INTEGRATED-SECOND-DOMAIN-READINESS-20260917-001`  
Issue: #846  
Immutable evidence BASE: `824fe51dc35da56782a0a1817889bff22b9b8b64`

Disposition: **`HOLD_MINDUSTRY_COMPOSED_LIFECYCLE_MISSING`**.

The frozen container formal runner was invoked exactly once; reruns were zero. The independent auditor passed all eight checks. No model, GUI, game, provider, network or task input occurred in this allocation.

## What is source-closed

Cells 1–8 and 14 pass at their retained scope:

- Luna-low/low is already used through the retained target-handle model runner family;
- per-attempt input/cached/cache-write/output/reasoning usage is retained and reconciled;
- engine-side task scoring independently verifies placement/direction, source/core preservation and collateral guard tiles after control closes;
- no-match/unreadable branches retain coordinate-free safe stops with no placement button admission (the old no-match reason mismatch remains a formal diagnostic failure and is not relabelled);
- post-model current binding/pixel revalidation exists and changed pixels, unavailable binding/focus and resize fail closed;
- terminal release/cleanup evidence is retained;
- model/wall/local timing endpoints exist;
- an eight-target Mindustry construction workload exists without engine state being supplied to the controller;
- the Mindustry call ledger plus the retained #57 phase ledger provide the all-attempt/preflight/repair accounting shape for a fresh comparison.

## Exact remaining gap

Only cells **9, 11, 12 and 13** are missing:

1. no current-path same-session Mindustry demonstration of repeated equivalent work through a retained compiled target/method with **zero new model generation**;
2. no composed selective repair of only the invalidated persistent target/method;
3. no subsequent model-free reuse after that repair;
4. no matched plain/current-optimized/persistent three-arm economic comparison over one frozen Mindustry workload.

Cell 10 already passes independently: changed palette/world, unavailable binding and surface resize refuse old target authority before consequential input. Thus the remaining blocker is not failure to invalidate; it is the absent **composed persistent lifecycle and matched harness**.

## Consequence

Do not allocate a frontier model yet. The minimum successor is a source-first, model-free Mindustry integrated mechanics preflight that freezes one repeatable workload and demonstrates the control-flow skeleton:

`cold -> reuse -> reuse -> invalidation/refusal -> selective repair -> reuse -> reuse`

with the same authority/release/effect-accounting interfaces. Only after that mechanics rung passes should a separately preregistered live/model three-arm allocation be requested.

RESULT SHA-256: `0efafaf2325b10ddfa3690bce86df11aded19efad34e48104865312a36fd1c0d`.  
AUDIT SHA-256: `582879f3efc4b1282528ff7b3c085671456a0be06b5703e12828d8e0a1d92d50`.

This is evidence closure, not a second-domain performance result, reliability estimate, token-saving claim, or live Mindustry success claim.
