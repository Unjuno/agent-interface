# Typed predicate ingress: retained engineering evidence (#4398)

## Chronology and scope

This is retrospective publication of `predicate-ingress-i8k4-20260925-local01`, not a new scientific allocation or public preregistration. The original 110-input / 220-worker matrix was consumed once before this Issue existed. No measured worker is restarted for publication. Closed #4236 and merged #4244 retain `HOLD_AUDIT_GATE_SPEC_ERROR`; none of their sources, results or gates changes.

The new boundary is typed request/current-state ingress around the unchanged research predicate. Invalid current evidence is rejected before evaluation; an invalid stored cache is a miss only when current evidence is valid. This is not a production runtime adoption or a claim of an observed real-world incident.

Publication intake main: `4c701cc51b06296268ad8d9ae3eff1dd6f2d379d`. README, CURRENT_GOAL, ROADMAP, open/closed Issues, PRs, first 100 branches and targeted ingress searches were checked through GitHub MCP. Coverage is bounded/non-atomic and cannot rule out unpushed work. Only this additive directory is owned; no existing runtime, workflow, index or foreign allocation changes.

## Retained result

`PASS_LOCAL_TYPED_INGRESS_ENGINEERING` remains a local engineering result.

| Input group | Fixed inputs | Adapter disposition |
|---|---:|---|
| Valid | 12 | Exact predecessor-result parity |
| Invalid current state | 61 | Rejected before predecessor evaluation |
| Invalid or absent artifact | 17 | Valid-current recomputation, no reuse |
| Invalid/unsupported wire | 11 | Rejected before evaluation |
| Invalid/unsupported request | 9 | Rejected before evaluation |

Both policies received the same 110 wires, for 220 recorded worker processes. Baseline exceptions are recorded observations, not successful predicate results. The baseline returned values on 49 invalid-current cases and marked 8 as reused. All adapter outer responses retain authority=none, input_dispatched=false and task_success=null. Nested `executable` is a research diagnostic, not OS permission. The predecessor computes truth even on a cache hit; no computation/model-call elimination is measured.

The original raw-only auditor performs 2,661 checks with zero errors. Its 14 effective evidence mutations reject normally. Original 14 frozen files, all worker exits and the runner exit reconcile. Eight unit methods and a separate six-input construction sample are not pooled into the matrix. These are same-author separate implementations/processes, not external human review.

## H / T / D / C / U

H: preserve valid results, refuse invalid current evidence before evaluation, and distinguish recoverable cache invalidity.

T: exact source and directed finite input matrix, provided Linux/CPython 3.13.5 standard-library container; original local freeze before execution, not GitHub preregistration. The original environment is in the restored ENVIRONMENT.json. No Docker/OrbStack image-attested replication is claimed.

D: all 110 exact cases / 220 process receipts, valid parity, zero predecessor calls on invalid current/request/wire, no malformed-cache reuse, complete audit and 14 effective controls. Publication has separate object-identity, reconstruction, CI and review gates.

C: known Python/JSON behavior and a stronger opt-in input envelope. Complete declared dependencies and trusted current-state sources remain assumptions. Digest equality is not authentication. Typed acceptance does not establish current world truth.

U: no learned-model accuracy, GUI/action effect, latency/tokens, natural failure rate, concurrent mutation, crash/power-loss durability, all-JSON proof, cross-platform support or production promotion. Broader #57/#2789 and ROADMAP remain open.

## Complete evidence and historical notes

All 39 original files (704,155 member bytes) are retained in eight exact binary segments. Concatenation restores one ordinary TAR.XZ archive (59,268 bytes):

`b9d7e7699e81d620c207a0474e6a0c8341bb82470165e45c56082622ace6e4d2`

The original supplied ZIP identity is `9dba2e5870e7d6d6560479cfa82bc2d6a41355342e296c6f6f4163d7bd9785c0`. Repackaging changes archive metadata, not original member bytes. No predecessor capsule or unrelated blocked publication is included.

`PLAN_ORIGINAL.md`, `REPORT_ORIGINAL_JA.md`, `FREEZE_ORIGINAL.json`, `AUDIT_ORIGINAL.json`, `adapter.py` and `provenance/upstream.py` are exact readable copies. Their statements about unavailable writes or uncreated branches describe the earlier session and are preserved, not current publication status. The actual delivery branch is `research/predicate-ingress-retained-20260926-q2h7`; live PR/main status belongs in Issue #4398.

Original raw SHA256: `c13892ce105c69d60564c4949bf9c532d5831249db07ca99b73abe4dee3e1c03`.
Original freeze SHA256: `f93b555f597de5d5b88ed1025aeaf9cf55857b4c0e6d6467e2382490b221ae74`.
Original upstream Git blob: `51373e57f9966c7ca8d8518a218d02363867fb81`.

The original failed source DNS acquisition and packaging-driver import error remain inside the capsule. They are not scientific failures or permission to rerun the consumed matrix.

## Read-only reconstruction

Run from this directory, using a NEW destination and CPython 3.13 standard library:

```sh
python -S -B -m unittest -v test_restore
python -S -B restore.py /tmp/ingress4398-review-new
python -S -B /tmp/ingress4398-review-new/verify.py
```

The restorer verifies segment/archive/member extent and hashes plus all six readable copies before writing files. It does not execute archived code. The explicitly invoked original verifier checks 38 manifest entries, re-audits saved data and runs eight unit methods; it does not run the consumed matrix. Do not invoke archived execute.py or runner.py for this delivery.

Local fresh restoration matches every original byte, reproduces original audit/control outputs byte-for-byte and passes eight packaging tests. LOCAL_PUBLICATION_CHECK.json retains actual verification PIDs/exits. These local checks do not stand in for GitHub CI or independent approval. The publication parent directory is trusted/quiescent, not an adversarial filesystem sandbox.

## Integration decision and remaining roadmap

This delivers a reviewable input-boundary candidate and its evidence, not runtime-default changes. Keep invalid current evidence distinct from invalid cache, and place actual authority in the existing admission layer rather than this diagnostic result.

Source/read-only revalidation -> complete additive publication -> exact Git-object readback/reconstruction -> evidence PR -> applicable exact-head CI and scoped review -> qualified evidence-only merge/main readback -> only supported dependency-safe owned-ref cleanup. A merged evidence record does not turn the old #4236 HOLD into PASS or close the overall product roadmap.
