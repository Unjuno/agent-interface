# Issue #4307 — local Docker source/construction revalidation

## Disposition

`PASS_DOCKER_CONSTRUCTION_REVALIDATION_SCOPED` for the newly preregistered c02 three-case construction set. This is not the `nrs4307-c7e4-formal01` 48-case allocation, does not validate its reported aggregate, and does not close Issue #4307. Formal invocations: 0.

The prior c01 construction set remains **HOLD**. Its frozen raw-only auditor ran 490 checks and found only `app-stderr-empty` in each of three records. The stderr was the repeated Fontconfig message `Fontconfig error: No writable cache directories`; all Tk application and Xvfb processes exited 0, and the observed GUI effects and cleanup otherwise completed. c01 bytes were not modified. c02 used a distinct preregistered allocation/output/session namespace and routed HOME/XDG/Fontconfig cache into writable tmpfs.

## H / T / D / C / U

**H — hypothesis.** A completed child's positive currentness check must not justify a parent's suffix. The frozen per-frame policy should preserve stable nested progress and refuse a changed root target; the deliberately weak session-wide cache should expose the counterexample.

**T — treatment.** Three fresh depth-2 `CONSTRUCTION` sessions from the public frozen source capsule:

| Scenario / policy | Observed result |
|---|---|
| STABLE / PER_FRAME_RECHECK | child and root each received `b`; both ended at `ab` |
| ROOT_TARGET_REPLACED / GLOBAL_SUCCESS_CACHE | child completed; cached child validation emitted `b` into replacement root; auditor classified wrong continuation effect |
| ROOT_TARGET_REPLACED / PER_FRAME_RECHECK | child completed; root returned `REVALIDATE_TARGET`; no root suffix was emitted |

Every XTEST key edge had a press/release pair. All final logical keymaps were neutral; all app/Xvfb exits were 0; both processes were dead and owned display sockets absent. Frozen raw-only auditor: 490 checks, `errors=[]`. All three app and Xvfb stderr files are 0 bytes. Full raw artifacts, execution metadata, independent raw audit, summary and SHA256SUMS are in this directory.

**D — decision.** The bounded construction gates passed. The aggregate is directionally consistent with the specified contract: candidate unsafe suffixes/wrong-target effects 0/0 (2 cases); weak comparator 1/1 (1 case, directed target-replacement control). These deliberately selected cases are not rates or estimates. c01 remains HOLD and is not pooled with c02.

**C — controls / provenance.** Issue #4307's source capsule was retrieved through GitHub MCP. All three `SOURCE.*.b64` chunk SHA-256s matched `SOURCE_META.json`; decoded compressed archive was 18,908 bytes with SHA-256 `9d39d91744de2b74a8ac01a5612624401be882bdaa163228d9664e0bb3534c25`. The source tree was extracted read-only inside the container. Local cached image: `agent-interface-golden-ipc-2705:20260920-wmctrl`, image ID `sha256:7e4a3b6f3917baa9f153b4dfd011a4ea528a3d200a7a3d274aa9479900ee7b41`, linux/amd64; Docker Engine 29.8.0; CPython 3.11.16; Tk 8.6; Python-Xlib 0.33. Docker used `--pull=never --network none --read-only`, read-only source bind, dedicated writable result bind, and bounded tmpfs for private Xvfb/auth/cache. The test-only Xauthority writer SHA-256 is `0c7bf8730507243ae2715e373c348ae7be76dc7d86bbb0f44acd8d2e92750b09`. It is not part of the frozen source and grants access only to each private test display.

At intake the five publicly listed `EVIDENCE.*.b64` chunks did not form a complete archive: base64 decoding followed by LZMA decompression stopped before the end-of-stream marker. Thus the 48-case raw/process corpus could not be independently audited here. The branch's `REPORT.md` claims 48/48 and PASS, but that claim is not revalidated by this construction result. Preserve the issue's existing open acceptance state until the full formal artifacts are available and independently auditable.

**U — scope.** Three directed cooperative Tk/XTEST controls only. No model, production runtime, host desktop, workflow-based experiment, arbitrary-depth/toolkit generality, external human review, or product claim. The Python 3.11 Docker environment is a construction revalidation, not an image-attested replication of the reported Python 3.13.5 formal environment.

## Reproducibility

The frozen child-case argv is retained in `EXECUTION.json`; source archive and helper identities are above. Each case's `CASE.json` contains raw file hashes and event/process records. `RAW_AUDIT.json` is generated by the frozen, separate raw-only `audit.py`. Verify retained output files with `SHA256SUMS`.
