# Excluded construction

construction-01: STOP before task input. Tk focus_force selected the containing native toplevel while winfo_id identified Entry. The strict exact-focus precondition rejected this; no XTEST command was emitted. Receiver cleanup exit -15 and Xvfb exit 0 are retained, not relabelled. The six exact source versions and partial row/BATCH/stdout/stderr/actual outer exit remain.

Before construction-02, setup explicitly sets native focus to the owned Entry XID after event selection and checks readback; this is fixture setup, not a changed hypothesis or production guard. No formal case has run.

construction-02: 10 complete fresh c-letter sessions, all child exits 0, Xvfb exit 0 and socket absent. Duplicate-down inserted c once in each repeat mode; two taps inserted cc. Long hold with repeat OFF/ON inserted 1/9 characters. This is excluded construction, not formal evidence. Native and Tk press counts agreed in all ten. No chosen numerical repeat count (9) becomes a formal gate; only >1 is preregistered.

construction-audit-01: 4 test methods ran; positive method had 8 failing input-bearing subtests because native/Tk release equality was an overstrong additional construction assumption. Exact failed auditor/tests/PLAN/output retained. Before formal, use exact press matching plus ordered Tk-release inclusion in the full native release stream, with additional-native-release count exposed. No raw row or live command rerun. Cause of the extra leading native release is not established.

Preformal source inspection found two timeout-only harness issues. A Tk callback exception need not exit mainloop; change the receiver deadline to an explicit deadline record, destroy and exit 2. A terminated outer runner could leave owned child processes; give it a private process group and terminate only that group on timeout. The old source/freezing draft is retained under construction-hardening-01. These are construction repairs within #4032; no formal case or scientific gate changes, no new Issue, no completed live case rerun. Normal construction control flow is unchanged.
