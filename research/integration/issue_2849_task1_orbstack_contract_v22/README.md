# Issue #2849 OrbStack socket/workspace construction confirmation v22

Seed 284922 repeats only the bounded no-task v21 contract using a corrected status predicate. It starts at most one pinned outer container with networking disabled to query Docker through the mounted OrbStack socket. The selected backend argv is constructed, never executed.

No task allocation, host broker, nested model container, host model CLI, task token, or submission is part of this experiment.
