# Issue #3628 evidence package

This package contains the H/T/D/C/U freeze, the unchanged portable MCP archive and runner, the additive Tk fixture witness, all seven preflight controls, exactly three formal OrbStack rows, immutable raw outputs, the inherited auditor HOLD, its invocation STOP, the corrected successor auditor, and the scoped PASS result.

Start with `PREREGISTRATION.md`, `RESULT.md`, `evidence/FREEZE.json`, and `evidence/AUDIT.json`. Formal allocation raw evidence is under `evidence/formal-01` through `formal-03`; `evidence/construction` is excluded from formal count. Do not substitute the retained historical `HOLD_OR_FAIL` with the scoped successor result: both answer their separately frozen gates.
