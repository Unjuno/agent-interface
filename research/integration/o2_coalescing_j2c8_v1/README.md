# O2 coalescing composition evidence (#4392)

Result: **PASS_O2_COALESCING_COMPOSITION_SCOPED**. Research evidence only; no production runtime/default changes. Parent #43/#57/#2789 and global ROADMAP remain open.

Read [REPORT.md](REPORT.md) for the full result and limits, and [PROTOCOL.md](PROTOCOL.md) for the prospective H/T/D/C/U and conditional proof. Existing queue/codec modules are complete unchanged copies under vendor/. This is not another queue reducer or codec-memory optimization; the earlier p6k2 HOLD is preserved outside this study.

## Measured boundary

36 cases /72 real local actor processes, one public-source-first allocation in six batches. FIFO encoded/delivered52/52 frames; selection after encoding52/28 with12 decoder refusals across10 cases; selection before encoding28/28 with0 refusals. All four authored critical labels per mode survive. These synthetic labels are not real focus/effect events; no GUI/input/model ran. Counts and packet bytes are not performance or token measurements. Equal retained pixels do not establish receipt of the latest source identity. No mode establishes continuous visual coverage.

The separate raw auditor checks1499 predicates with no errors;10 effective copied-evidence mutations reject. Same author/separate implementation and process is not independent human review. Eight unit methods passed before measurement. Full source was published before all formal cases at5369ea4613da9ed3726cef6ffc3f6d759e8fdb19, followed by issue comment5841170700. First-outcome comment5841184421 preceded packaging. Formal reruns/replacements/exclusions/postfreeze source changes0.

## Complete retained evidence

PACK.json binds six binary fragments of one34,812-byte XZ-compressed UTF-8 file map. Data-only restoration yields all79 original files /918,144 member bytes, including source, excluded construction v0 and corrected v1, full raw stdin/stdout/packets/decoder states, all actor and batch exits, audit/controls and original reports. Original78-member RAW_MANIFEST.json is checked before writing. Archive SHA256 ac787ba01893de7d1426946edb77487dddb8e1183450c8be58c93c71499e6a1e. No original record is replaced by this summary.

Read-only review, Python3.13 standard library:

```sh
python -S -B verify.py
python -S -B -m unittest -v test_restore
```

These commands restore into a fresh temporary directory and rerun only saved-data auditing. They never invoke launch.py/run.py or restart scientific actors. verify.py requires published frozen source bytes to equal the archive and reproduces original AUDIT.json/CONTROLS.json byte-for-byte. test_restore covers five packaging refusal controls, with intact restoration verified first. To inspect the retained source/raw directly: `python -S -B restore.py /tmp/j2c8-review-new` with an absent destination. The restorer is bounded data handling for this trusted/quiescent publication, not an adversarial filesystem sandbox or authentication mechanism.

## Scope and delivery gates

Linux x86_64/CPython3.13.5/NumPy2.3.5/zlib1.3.1; no Docker/OrbStack image attestation. Reliable ordered delivery after selection/encoding and separate per-scope codec states are assumptions. Post-selection loss, reconnect, fresh-at-display, actual capture/critical detection, model decisions and end-to-end benefit are untested. Never renumber arbitrary encoded deltas. Shared runtime, workflow, root index and historical files are unchanged.

All six uploaded part blob IDs matched local exact bytes. Local verification is not GitHub CI or external approval. Exact-head applicable CI/scoped review and main readback are separate gates recorded in the PR and Issue; this file does not claim them before completion. Keep owned branch while unmerged/dependent. Publication repairs, if any, remain under this same Issue.
