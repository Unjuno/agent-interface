# Issue #4039 — bootstrap scope, incomplete formal allocation

**Final disposition: STOP_SUPERVISION_TIMEOUT / HOLD_FORMAL_INCOMPLETE.**
This is archival research evidence, not a candidate PASS, runtime change, or roadmap completion.

## What was executed

One publicly hash-frozen18-case local allocation. At the registered30-second limit the outer supervisor killed only its owned group; actual runner exit was -9. Nine cases (0–8) serialized complete raw evidence; case9 had started but retained no raw/terminal receipt; eight later cases were not started. No END.json exists, no retry/pooling/gate change was made. The unchanged full auditor exits1 at the missing terminal file. Its first AUDIT.json is empty; audit.exit and full-audit-recheck.stderr preserve this limitation. Empty stderr does not prove absent in-flight work.

Construction-01 separately completed six cases,12 unit test methods, and12/12 semantic corruption challenges. These rows never count toward the formal18. The30-second outer envelope proved insufficient; this is a local execution-planning error, not evidence of a repository-wide resource blocker or a reason for another wrapper-only Issue.

## Retained observations, not a full acceptance result

| Completed condition | Cases | Exact #3950 tracker | Bootstrap-aware comparison |
|---|---:|---|---|
| Unchanged target |2|MATCH2|MATCH2|
| Known later-created bystander destroyed |2|MATCH2|MATCH2|
| Preexisting bystander destroyed |2|UNKNOWN2|MATCH2|
| Same-content target repaint |1|MATCH1|MATCH1|
| Target destroyed |1|UNKNOWN1|UNKNOWN1|
| Observer reconnected |1|UNKNOWN1|UNKNOWN1|

Counts are unbalanced prefix coverage, not failure probabilities or avoided model calls. In two retained cases an ordinary unrelated exit poisoned the legacy global complete flag, while an existence-only bootstrap inventory explained the missing birth without fabricating a target lifetime. Target-destruction/reconnect controls remain unknown in their single retained cases. Candidate receives extra bootstrap information; no equal-information performance comparison is claimed.

Nine complete rows reconstruct with zero errors using the frozen independent raw checker. Prefix interpretation is explicitly postformal (prefix_audit.py), always formal_PASS=false;12 semantic evidence corruptions reject on retained case2. The formal gate requiring all18 remains unmet. Original #3950's fully covered fixture/result is unchanged.

## Input isolation and empirical boundary

Each policy invocation is a separate Python -I -B subprocess, cwd=/tmp, sanitized environment. Exact retained stdin contains only opaque32-character epochs, numeric target/bootstrapped IDs, native projected events and an explicit gap flag. No condition names, expected answers, witness observations, or case paths are supplied. Exact schema rejects extra fields. This is cooperative input separation, not a security sandbox against malicious code.

Independent X connections observe actual root children/events and target pixel bytes. The renderer lives in another process. Existing windows have no invented CreateNotify or birth token. Quiescent bootstrap, direct-root child windows, no same-XID reuse/reparenting, trusted native event delivery, and unchanged subscriptions are deliberate assumptions. Unexplained event loss is not diagnosed automatically; declared gaps and unexplained creation/destruction fail unknown. No GUI input, XTEST, real model, provider, user desktop/document, credentials publication or experiment network. All outputs grant no input authority.

## Execution and cleanup

Provided Linux6.18.44/x86_64 execution container, CPython3.13.5, installed Xvfb/Python-Xlib; binary hashes/package metadata are in ENVIRONMENT.json. Docker/Podman CLI and image identity were unavailable, so no Docker/OrbStack replication is claimed. Timings are diagnostics, not benchmarks.

Nine completed actors and servers have recorded exit0, empty terminal tree/keymap/buttons and removed sockets. Post-stop /proc inspection found no members of the supervised group732. That is not an original exit attestation for the incomplete case. Its private ephemeral Xauthority cookie was removed for credential cleanup; scientific raw records were not deleted. Full terminal cleanup remains incomplete for case9.

## Prior conversation HOLD, kept separate

The previous subscription-handoff24-case study retains HOLD_PREREG_INPUT_METADATA_SEPARATION. Its original unchanged raw auditor still produces its initial PASS output, but a later review found condition words in classifier metadata; posthoc nonuse/renaming did not repair preregistration compliance. PREVIOUS_HOLD_RECHECK.json records this continuation's fresh hash verification of196 manifest entries/199 archived members and byte-identical old audit output. No old allocation was rerun. A retrospective comment on #3950 records the chronology. **That full previous ZIP remains conversation-hosted, not included in this repository capsule.** Current publication must not imply otherwise.

## Review/reproduce without running a formal allocation

`python -I -S -B unpack.py /absolute/new/review-directory`

Then, from the restored directory:

```
python -B prefix_audit.py > /tmp/4039-prefix-recheck.json
cmp PREFIX_AUDIT.json /tmp/4039-prefix-recheck.json
python -B mutation_checks.py formal-01
python -B -m unittest test_contract -v
python -B audit.py formal-01
```

The last command is EXPECTED to exit1 because END.json is missing. Do not substitute prefix success for full formal acceptance. Do not rerun the consumed run.py. New live work requires a separately frozen allocation under this same scientific question, not another wrapper successor.

The lossless capsule holds complete new source, construction, raw prefix, partial logs, freezes, actual outer exit, unchanged full-audit failure, prefix review, tests and prior-HOLD recheck note. Unpacker verifies exact expanded size/digest, entry hashes and fresh destination, and executes no study. Public FREEZE commit79dabb6faa42e2ad2243a1cee1cc24c745eaa951 preceded formal invocation; this was a public hash commitment, not earlier full-source publication. Sources and scopes are in PLAN.md. #4039 is not scientifically complete; #2/#2789 and global ROADMAP remain outside this result.
