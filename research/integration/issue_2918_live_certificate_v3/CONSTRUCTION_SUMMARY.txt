Before formal start, allocation 03 passed a read-only OrbStack container compile/import check with PYTHONPATH=/repo, including the frozen #1904 expected ES certificate gate.

A negative construction audit mounted allocation 02 raw evidence read-only and invoked the new v3 auditor with a separate temporary report mount. It failed as expected at the missing `input_dispatched` lineage field before accepting the old evidence. This was a test of the new HOLD gate, not a formal API observation and not a modification/retry of allocation 02.

The auditor's real report path is a separate writable mount from the raw input. The formal runner retains complete per-case context in candidate_results.json and sealed_oracle.json; the independent auditor is frozen to require that lineage and to verify the target-replacement and intent-mismatch PNGs too.
