# Construction 03 — correct v4 runner, two declared cells

This invocation mounted the v4 experiment directory itself at `/exp` and its frozen 10-file runtime copy at `/src/runtime`; the prior construction invocation accidentally used the v3 runner and ran its 14-cell matrix. The v4 runner executed exactly the two preregistered cleanup-failure cells, with no formal invocation.

The bare cell observed the independent press and the one supervisor release, with the key up at terminal sampling. The Openbox cell stopped as `STOP_CONSTRUCTION_ONLY` with `HOLD_KEY_PRESS_NOT_OBSERVED`; it emitted no observer press event. This is a setup/evidence HOLD. Neither cell is formal data, and no v4 hypothesis result is claimed. Preserve the complete raw record and do not retry under this allocation.
