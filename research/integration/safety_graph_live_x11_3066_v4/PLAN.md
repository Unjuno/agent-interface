# Issue #3066 successor allocation v4 — immediate typed recovery

## H/T/D/C/U

- **H — hypothesis:** In v3, the worker creates an `execution_failed` / `recovery_required=true` receipt before the lease deadline; the parent observes that receipt at deadline + 23.098576 ms (bare) and + 22.862416 ms (Openbox), but waits until deadline + 60 ms before invoking its external X11 supervisor. The supervisor's own release receipt completes at +71.177191/+70.903452 ms, and the separate keymap sampler observes F8 up at +72.793112/+72.218083 ms. With the same runtime, action, failure injection, pinned image, and 150 ms deadline + 50 ms grace, invoking exactly one supervisor release as soon as that typed receipt is observed will leave X-server F8 state up within the frozen bound in both configurations.
- **T — test:** Against current `main` commit `c9bfb14c41b9ce72782a03ed4bfa0d814baf6a8f`, run two fresh Xvfb cells in fixed order: `bare/cleanup_failure` then `openbox/cleanup_failure`. Reuse the exact v3 runtime blobs (verified unchanged against current main); change only the additive runner schedule, display numbers, allocation identity, and supervisor trigger timing. The worker presses F8, waits 120 ms under a 150 ms lease, and injects the same `release_all` exception. The parent reads the worker result, records when it observes the typed recovery receipt, performs one supervisor release immediately, and records the independent X-server keymap transition. One formal container invocation only.
- **D — decision:** `PASS_EARLY_SUPERVISOR_RECOVERY_SCOPED` requires exactly one valid row per configuration, typed `recovery_required=true`, exactly one supervisor attempt, verified supervisor release, independently sampled F8-up no later than deadline + 50 ms, and an up terminal sample. A witnessed late release, duplicate attempt, missing typed recovery, failed release, or residual key-down is FAIL. Missing identity/timing/keymap evidence is HOLD. Fixture/setup failure before a press is STOP. No result changes v3.
- **C — controls:** Pinned image `sha256:03bb8f908cc4d0bcfcda4e521c5318c651543bb8dffc2f6b09bb874a1d4f9adc`; Docker Desktop `desktop-linux`, Linux/amd64; `--network none`, read-only rootfs, 1 GiB memory, 2 CPUs, 96 PIDs, read-only experiment/runtime mounts, one writable result mount. Synthetic audit controls exercise typed-recovery, duplicate-supervisor, late-time, missing-independent-witness, failed-release, residual-input and allocation-mismatch rejection. No model, external network, host desktop, physical HID or task effect.
- **U — unknowns:** Whether the parent can observe the typed result early enough and release within the bound on both display configurations. This allocation does not repair v3's socket graph reconciliation, generalize to physical desktops, or establish the full #3066 acceptance boundary. Even a scoped PASS leaves v3's graph finding and other fault schedules open.

## Rationale and explicit delta from v3

The retained v3 raw record shows the typed worker result was read at deadline + 23.098576 ms (bare) and + 22.862416 ms (Openbox). The frozen v3 runner then intentionally slept until deadline + 60 ms before taking its terminal sample and calling `supervisor_release`; the supervisor receipts completed at +71.177191/+70.903452 ms, and the independent keymap sampler observed F8 up at +72.793112/+72.218083 ms. V4 removes that fixed post-deadline wait and tests the specific immediate-recovery behavior. It preserves the v3 FAIL unchanged and does not reinterpret it as a runtime PASS.

## Formal and retention policy

Construction output is separate and excluded from the formal denominator. Before formal execution, publish this plan, exact source/image identities, checksums, and the one-shot command to Issue #3066. The formal output path is fresh and immutable. No rerun, replacement, tuning, or second supervisor attempt is allowed. Preserve the first result, raw receipt and any STOP/FAIL/HOLD exactly as produced.
