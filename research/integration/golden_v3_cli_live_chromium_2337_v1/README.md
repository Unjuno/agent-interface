# Golden v3 to CLI live Chromium adapter successor (#2337)

Status: PREREGISTERED / no live allocation

This additive path composes the retained V5 Chromium fixture with the current runtime/cli_v1/golden_v3.py boundary. It must not modify or regenerate retained V5 evidence.

## H — hypothesis

The V5 compiled GUI route can be wrapped by the CLI adapter without losing observation identity, lifecycle ordering, safe-yield progress, independent effect status, or verified release.

## T — frozen comparison

Use the retained V5 source/result as the control and run one fresh bounded block with the same disposable Chromium fixture and independent POST scorer:

1. V5_CONTROL: existing compiled caller, positive and changed-target cases.
2. CLI_ADAPTER: same task allocation and observation budget through dispatch_golden_v3.
3. UNKNOWN_LIFECYCLE: injected unknown event must fail closed.
4. UNCERTAIN_DELIVERY: ambiguous delivery must not replay Submit.
5. CLEANUP_FAILURE: retained execution/result must remain distinct from task success.

The adapter must preserve the V5 positive result and the changed case's unknown_state, completed_actions=1, confirmed_partial, no Submit input, and independent scorer=false. Every action still requires fresh admission and verified release.

## D — data and acceptance

Retain exact source blobs for runtime/cli_v1/api.py, runtime/cli_v1/golden_v3.py, research/live_control/run_compiled_gui_interface_live_v1.py, research/live_control/audit_compiled_gui_interface_live_v1.py, and research/live_control/results/compiled-gui-interface-live-01/**.

Record model attempts/usage, observation sequence and frame hashes, dispatches, refusals, repairs, delivery status, effect score, cleanup and release receipts. PASS only if field/order/identity preservation and independent effects agree for positive and changed cases, all controls fail closed, and no release/input invariant regresses. HOLD if model/GUI/effect or setup-cost evidence is absent. FAIL on dropped lifecycle fields, process-exit task success, unsafe replay, or cleanup reported as success.

## C / U

This is one Linux/X11 Chromium fixture and one retained V5 lineage. It does not claim generality, token savings, human tempo, second-domain transfer, or production readiness. Provider authority and live allocation must be explicit before execution. Historical V5 failures and results remain immutable.