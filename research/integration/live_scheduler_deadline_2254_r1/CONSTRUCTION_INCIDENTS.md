# Construction incidents

- construction-01: STOP before any scientific case. `surface_actor.py` exited 1 because the parent launched it with `python -S`, which disables site-package discovery and therefore hid installed Python-Xlib (`ModuleNotFoundError: Xlib`). Xvfb itself was alive and its displayfd handshake completed. Formal rows = 0.
- correction: remove `-S` only from the Xlib-using actor/watcher child commands. Keep `-B`; no scientific schedule, policy, threshold, decision gate, source event, or formal allocation identity changed.
- construction-02: STOP before any scientific case after removing `-S`. Xvfb displayfd completed, but Xlib children had no `XAUTHORITY` environment and attempted `/opt/xvfb/.Xauthority`; parent therefore received no ready JSON. Formal rows = 0.
- correction: bind each actor/watcher to the allocation-owned empty authorization file already passed to private Xvfb via `XAUTHORITY`. This changes setup only; no scientific schedule/policy/threshold/gate changed.
- construction-03: STOP before any scientific case. Binding `XAUTHORITY` to the allocation-owned empty file removed the missing-file exception, but Python-Xlib writes `Xlib.xauth: warning, no xauthority details available` to stdout before the actor JSON; the strict JSONL reader rejected that non-JSON line. Formal rows = 0.
- correction: replace dynamic displayfd + empty auth with a bounded free-display search (90..149), create a real MIT-MAGIC-COOKIE-1 entry before Xvfb starts, and pass the same XAUTHORITY to actors/watcher. Also make ready-line waits selector-bounded so startup failures retain child exit/stderr instead of blocking. Scientific schedules/policies/gates remain unchanged.
- construction-04 validation incident: the four-case construction output was created, then the outer shell invoked `audit.py` without its required `--out` argument. The experiment output is retained unchanged; only the read-only audit command is corrected. This is not a new construction allocation or formal rerun.
- construction-04: four live construction cases completed their outer processes, but watcher capture failed in three cases with `TypeError:string argument without an encoding`; this Python-Xlib build returns `Image.data` as a latin-1-preserving string. The retained raw construction output has zero records in those three cases and its read-only audit fails. Formal rows = 0.
- correction: normalize `Image.data` to bytes with latin-1 only when it is a string; byte-returning environments are unchanged. This is a representation compatibility correction before freeze, not a changed policy/schedule/gate.
- construction-05: all four live cases and all child/Xvfb exits were integral, but applying the formal decision gates to construction produced `fair_noncritical_bound` at 89.768489ms. Inspection showed the B state was observed before the entire ten-event critical backlog was generated and before a fixed collection wait, so the construction conflated pre-release fixture generation with scheduler fairness. Formal rows = 0.
- correction before freeze: keep the 50ms science threshold unchanged, but stage the ten real A focus events first, flush them, then capture the single B state immediately before scheduler release. This creates the intended queue state (critical backlog + fresh unrelated noncritical head) without ageing B through backlog construction. Construction audit is also restricted to provenance/identity/FIFO/authority/process integrity; formal-only outcome gates remain unchanged and apply only to formal data.
