# #4316 attention-cue evidence: publication STOP

**HOLD_PENDING_PERMITTED_PUBLICATION. Metadata only. Keep the PR Draft.**

This directory records the continuation's result and publication stop. It does
NOT contain the original source, raw frames, execution records or capsule, and
cannot independently reproduce the original pilot. Do not merge as complete
evidence delivery, promote the runtime, or close #4316 from this record.

## Preserved result and present verification

The previous locally source-frozen pilot remains PASS_LOCAL_RUNG0_BOUNDARY:
28 private X11 sessions, 14 conditions twice, seven first-outcome batches;
no human trials, model calls or public preregistration. It reported six ready
responses with twelve exact crops, sixteen rejections, four missing-evidence
HOLDs, two within-request duplicate ignores, and zero task-input events.
This is not human usefulness, autonomous task completion or production safety.

This continuation restored all 508 original files / 11,018,971 member bytes,
checked all 507 manifest entries and ten frozen sources, and reproduced the
original 1,750-check raw audit byte-for-byte. Twelve unit methods and twelve
effective evidence-corruption controls passed. Three control batches were
byte-identical; the fourth differs only in a temporary copy path in a missing-
file diagnostic. Eight additional data-only packaging tests passed locally.
No GUI, cue worker, model, human trial or scientific allocation was rerun.
REVALIDATION.json records these local checks, not GitHub CI or external review.

## Why full publication stopped

After creating unreferenced evidence objects, a create_tree request adding a
readable copy of the original audit.py was blocked by the tool:

> This tool call was blocked by OpenAI because we couldn't determine the safety status of the request.

No further reason was supplied. The source was not retried through another
operation, encoding, or previously created capsule. None of those source/raw
objects is attached by this metadata-only change. Object creation alone is not
reachable branch/main retention. The original conversation attachment and local
files remain unchanged. Historical write-unavailable statements remain intact;
this session's ordinary metadata writes work, so this is not a fleet-wide or
repository-wide write failure. See Issue #4316 comments 5807546032 and 5807825065.

## H / T / D / C / U and bounded roadmap

H: exact retained evidence can be restored without rerunning the science.
T: all member/source identities, original audit, unit methods and effective
controls; then full permitted GitHub byte publication and readback.
D: local reconstruction passed; full-evidence publication is HOLD because the
source/raw delivery gate is unmet. Scientific disposition stays unchanged.
C: temporary-path diagnostics are not wholly byte-identical across restores;
same-author independent code is not independent human/agent review.
U: no new evidence for human utility, real source authentication, cumulative
budgets, cross-request deduplication, restart, latency/tokens or production.

Completed: intake/ownership check -> immutable restore -> read-only audit and
controls -> metadata-only failure record. Stopped: full source/raw publication.
Consequently complete evidence PR acceptance and main integration remain blocked.
Do not evade the blocked operation or repeat science to obtain a publishable PASS.
The broader matched human-versus-autonomous experiment remains #4316's question.
This stop is recorded under that Issue, not a wrapper-only successor Issue.

## Change and cleanup boundary

Only this new directory is added. No shared runtime, workflow, index, prior
result or another worker's branch is modified. Keep the owned branch while this
Draft handoff is unresolved; age or ancestry alone does not justify deletion.
The repository ROADMAP remains open.

## Retained construction incident

The first local packaging script used an incorrect archive path and failed.
The path was corrected before any publication; scientific bytes never changed.
This is a packaging incident, not a new experiment or scientific failure.
