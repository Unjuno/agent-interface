# #4152 live task-ownership horizon result

Decision: **PASS_TASK_OWNERSHIP_HORIZON_SCOPED**.

## Chronology

This is one prospectively source-frozen private-X11/Tk allocation. Construction-01 stopped before input on controller Xauthority setup and remains excluded. Construction-02 passed the mechanism check. The exact plan, schedule, environment, source archive and hashes were committed and read back before formal case 0. A publication-only schedule formatting mismatch was corrected before formal; no scientific byte/gate changed. Formal allocation `task-ownership-horizon-4152-20260923-01` then ran exactly once with 12 fresh sessions and no retry/replacement/tuning.

## H / T / D / C / U

**H.** A short explicit task-ownership horizon can preserve useful multi-cycle local work during a fixed planner-delay window without extending per-action authority. If ownership becomes invalid while ordinary current action authority remains valid, the ownership layer must stop further local task actions.

**T.** STABLE versus INVALIDATE_AFTER_FIRST × NO_OWNERSHIP_HORIZON versus EXPLICIT_OWNERSHIP_HORIZON × three repetitions. Each fresh private Xvfb session ran an ordinary Tk Button effect. Every XTEST click was separately admitted from current focus XID, live window geometry and a finite action deadline. Ownership token checking was separate. No model/provider/network/user-desktop action.

**D.** All 12 case processes exited 0. The frozen raw-only audit returns errors=[]. In STABLE, no-horizon produced one effect and yielded in 3/3, while explicit horizon produced three effects and TASK_COMPLETE in 3/3: +6 verified effects across the three matched repetitions. In INVALIDATE_AFTER_FIRST, both policies produced exactly one effect in all six cases. Explicit horizon returned OWNERSHIP_INVALIDATED in 3/3 and emitted no second click; at that stop, a separately evaluated ordinary focus/window/geometry/lease authority check was still admitted in 3/3. Thus the stop was attributable to task ownership rather than loss of action authority. Terminal pointer Button1 state was neutral; app/Xvfb/case exits were all zero.

**C.** The task token and target geometry are cooperative fixture truth. Artificial planner delay is not a frontier-model call. The observed gain can be described as retaining local task scope across cycles; this experiment does not prove that a distinct production abstraction is better than every existing policy-envelope/cache representation.

**U.** Model comprehension, actual token/latency benefit, natural invalidation rates, semantic target discovery, second-application transfer, physical HID evidence, production integration and universal ownership duration remain unknown.

## Integrity

- preformal FREEZE SHA-256: `f8996f9c633ee0344774e980f99b2a73e03b3c6410cc1d46b06b1e093b772e61`
- preformal source archive SHA-256: `fa8ae0e7798ffdb40667768556100801431583941b9bcc07b1b32659c052b974`
- raw audit SHA-256: `9e5e4130bf345081ca62ee0bee9bbe01e582c6f1f8bb7168909f5ff8ea8d3dfd`
- formal receipts SHA-256: `df7097d21aeb5543b95d39f03c0fe9aec446e74dd4ea611d16947e9bf697964f`
- 3 frozen test methods pass after formal; 10 copied-evidence corruption controls reject; frozen source hash mismatches 0.

`EVIDENCE.part00..03.b64` plus `EVIDENCE_INFO.json` retains the complete formal evidence as one hash-bound lossless archive. `restore.py` only restores/verifies data and refuses an existing destination; it does not execute the consumed formal allocation.

## Integration meaning

Task ownership and input authority are separable state. A still-valid ownership horizon does not authorize an action; every action needs fresh admission. Conversely, current action authority does not justify continuing a task after its ownership/invalidation condition has changed. This component PASS does not close the broad #4152 model-facing question or the repository roadmap.

Evidence archive: 5360 bytes, SHA-256 `5d20763b7e2a0c251fb8d3b5ae90427d5e610d4a294740d910cfe5f4f897eaeb`, 98 files / 34190 member bytes.
