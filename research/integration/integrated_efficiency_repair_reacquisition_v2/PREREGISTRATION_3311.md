# Integrated efficiency evaluation — preregistration for #3311

Status: PREREGISTERED / NO RESULT

## H — hypothesis

A pinned composition of the existing native result boundary, bounded local continuation, source/target identity gates, compact feedback, verified release, and typed outcomes may reduce model-visible work over a fixed cold/warm/invalidation/repair workload without weakening correctness. Benefit magnitude is unknown.

## T — bounded comparison

The first block uses one existing independently scored desktop workflow and two arms: (A) the plain/current reference route and (B) the selected integrated route. Before execution, freeze task allocation, order, repetitions, model/settings, runtime/image digests, allowed attempts, budget, and stopping rule. Include cold acquisition, at least two warm reuses, one dependency-invalidating GUI change, bounded repair, and one post-repair reuse. Use the existing Docker gate with network isolation when the fixture supports it.

Every attempted stage must retain actual provider-reported input/output/cached/reasoning usage (unavailable is not zero), generations/resumptions, tool calls, model images, local observations, elapsed time by phase, independently scored effect, wrong/stale target input, collateral effects, safe stops, fallback, repair, and unfinished termination. Do not infer tokens from serialized bytes or sum previously reported percentages.

## D — required evidence

Retain immutable request/reply receipts, source/image/runtime digests, raw baseline and integrated rows, completion-prefix and cleanup records, a source/result/audit hash manifest, and an independent auditor. The final report must show cold, warm, invalidation, repair, and cumulative reuse-horizon accounting, followed by RETAIN/HOLD/REJECT.

## C — requirement-to-test matrix

| Requirement | Integrated enforcement | Required regression/control |
|---|---|---|
| typed outcomes / no-match / ambiguity | admission + typed terminal result | no-match, ambiguity, unavailable evidence |
| shared attempt accounting | one ledger across all stages | failed call, expansion, early stop, repair |
| schema/source preflight | preflight before authority | incompatible schema and source digest mismatch |
| target identity | revalidate after wait and before input | stale target, focus/surface replacement |
| runtime safety | lease, bounded dispatch, cancel, verified release | cancellation, uncertain delivery, no-progress |
| effect vs input | independent effect/collateral scorer | accepted input with no effect; collateral mutation |
| recovery | bounded repair and fresh evidence | invalidated dependency then re-observe/reuse |

## C — decision rule

RETAIN only if all applicable gates pass and a predeclared meaningful benefit is measured with no correctness regression. HOLD for incomplete evidence or unavailable live conditions. REJECT for regression or unjustified cost. No broad reliability, human-tempo, or price claim follows from this block.

## U — stop conditions and limits

Missing token semantics, non-comparable reference capability, missing independent scoring, missing raw bytes/digests, or an unrepeatable fixture force HOLD/STOP. Prior results and failures remain immutable. A negative or inconclusive result completes this bounded milestone; it does not justify feature expansion.