Review note for POSTFIX_AUDIT.json

That supplemental posthoc audit report passed its original row/image checks,
but its control-comparison sentence incorrectly said phase-support control
forwards the PREPARE transition. The frozen phase-support table is T/D/S, so
the change of E from 12 to 14 is already suppressible by that control. The
later pilot-05 independent audit computes this distinction explicitly:
candidate 2 suppressions, phase-support 1, global-support 0, with no cost or
generalization claim. `POSTFIX_AUDIT.json` remains unchanged as the earlier
audit output and is not the current comparison result.
