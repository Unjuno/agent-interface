Construction pilot 02 — STOP before any formal allocation

The OrbStack construction-pilot runner started XCalc and produced eleven
successful public observation PNGs, then exited while assembling the
no-observation ambiguous-target row. Exact exception:

`AttributeError: 'NoneType' object has no attribute 'get'`

The row builder dereferenced `raw['reply']` even though the intentional
pre-observation ambiguity branch sets it to null. All eleven PNG bytes are
retained under `captures/`; `CAPTURE_SHA256SUMS.txt` lists their hashes. The
runner had not yet serialized the candidate results, lineage, or oracle, so no
formal decision or audit can be reconstructed from this pilot. These files are
retained as unscored construction evidence only; they are not pooled into the
formal allocation.

The defect is fixed before preregistration by treating the absent reply as an
empty object only for optional region/frame metadata. A new full construction
pilot must validate the output/audit path before the source freeze. Exactly one
formal invocation remains planned after preregistration.
