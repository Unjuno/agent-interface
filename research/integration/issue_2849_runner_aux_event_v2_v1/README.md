# Issue #2849 auxiliary-event runner successor

This additive evidence bundle tests the event-count failure recorded in PR #3685. Read `PRE-REGISTRATION.md` before interpreting `RESULT.md`. The fresh model stream, host IPC request/broker receipts, container process/classification receipts, independent schema audit, and offline fail-closed controls are kept under `evidence/`.

The candidate parser/runner is at `research/live_control/issue_2849_runner_aux_event_v2_v1/event_accounting.py`. It is not wired into the shared v1 runner. `SHA256SUMS` records the principal raw-evidence hashes.
