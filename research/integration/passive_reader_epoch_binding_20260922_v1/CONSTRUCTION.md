# Excluded construction history

## construction-01 — STOP_OUTER_TIMEOUT

The first shell tool invocation had a 45-second outer timeout. It was stopped
before RAW.json serialization. Fourteen completed case JSON records and a
partial fifteenth directory remain unchanged. No terminal runner/child exit
receipt survived the outer tool stop; no complete construction or formal PASS
is claimed. A subsequent process-table check found no study runner/worker left.
The four source files used are preserved under construction-01-source/.

The next construction uses the same scientific sources and a separate bounded
supervisor with durable process-exit receipts and a 240-second allocation
budget. It uses a distinct output identity, not a rerun/replacement of the first
allocation. Construction evidence is excluded from the formal denominator.

The interactive terminal capability was also probed once and returned
StreamingExecNotEnabledContainerError, without executing any experiment.

## construction-02 — complete, excluded

One complete 16-cell construction ran with unchanged worker/runner/candidate
science and the bounded supervisor. Runner and separate auditor exited zero.
Raw SHA-256: e76ac1b36833c5369444b67927f842f2b8fd1c6f82e8de9c80713e0dbbf103bb.
The initial auditor returned the generic scoped hypothesis label even for
construction. Before formal freeze, only its reporting was clarified to return
PASS_CONSTRUCTION_EPOCH_ORACLE for construction; the original audit is retained.
Reauditing the same bytes with that label-only correction passed. No producer
or reader experiment was rerun. The supervisor was then extended to retain the
frozen test/control process exits as well as the raw-only auditor exit.

Eight unittest methods passed: six exposure-gate tests, unchanged raw baseline,
and eight distinct raw mutation challenges (subtests). All eight corruptions
were rejected. The source bytes below are frozen after these construction
checks. The first four source preimages and every partial first-attempt record
remain retained, not overwritten.
