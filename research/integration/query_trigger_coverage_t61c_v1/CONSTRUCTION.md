# Excluded construction and actual correction

All nine three-case construction blocks completed (27 cases,54 actors). The first block was condition5; the first raw audit reported six BOOT errors because the new auditor compared child sys.argv with the parent's executable/flag-prefixed command. Both raw input and the failed audit are retained. audit v0 was copied before correcting the comparison to parent argv[3:]. No actor logic or scientific outcome was changed by that correction. The corrected read-only audit passes all27 cases.

Before the remaining construction blocks, process-group ownership was tightened: the foreground supervisor owns one session and kills only its own group on timeout. This is a setup repair, not a new scientific hypothesis. Old runner/supervisor sources remain in records/construction_source_v0. There was no timeout or unknown actor exit in this construction. The first block's earlier supervisor lacks the later pid/timed_out fields but retains actual exit0; it is not silently rewritten.

Nine unit methods and eight effective construction mutations are required before freeze. Formal54 cases remain separate and run only after public exact source readback.

The first unit run passed8/9 methods: its mutation-effectiveness test mistakenly used Python object equality, under which False equals0. The raw auditor had already rejected that boolean exit correctly. The unit-only comparison was changed to serialized JSON equality; failed output/source and premature LOCAL freeze draft are retained. No public freeze or formal execution had occurred. The replacement public freeze is made only after the corrected9/9 suite passes.
