# Pre-registration

Task: `INTEGRATED-EFFICIENCY-WALL-DECOMPOSITION-RESCUE-20260917-002` (Issue #929).

Scientific H/T/D/C/U are frozen in Issue #929. This rescue changes only source publication/materialization relative to #847; the decomposition equations and pinned retained evidence are unchanged. Formal invocation count is 0 before source-first freeze. Formal budget: exactly one invocation, reruns/replacements/tuning 0.

Remainder semantics: `unattributed_remainder_ms` only. It must never be relabelled `local_time`; local observation/durable-call counts are descriptive with `timing_attribution=false`.
