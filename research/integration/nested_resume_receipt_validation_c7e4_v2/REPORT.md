# Receipt validation and X11 engineering composition

## Disposition and preservation

Refs #4307 only; do not close it. Intake main:
`f24584227343d68dbca4a5a74ff07763b0feb51c`.

The original correction remains **PASS_LOCAL_ENGINEERING_CHECKS**, a retrospective
conversation-local engineering result, not a publicly preregistered formal GUI
allocation. The fresh source-first smoke is
**PASS_ENGINEERING_X11_RECEIPT_COMPOSITION**. Neither promotes a shared runtime.

The earlier 48-case nested-frame pilot remains STOP: 39 raw cases, 38 observed
worker exit receipts, one raw case without an exit receipt, nine allocations with
no execution evidence and a missing batch END. All original 264 files and 263
manifest entries were rechecked unchanged. Their independent source/raw audit does
not invent the missing process status. This delivery does not republish those
264 files; #4307 owns that separate evidence lane. It includes all 65 original
receipt-correction files unchanged, including historical decision-point inputs.

## H / T / D / C / U

**H:** exact representation validation before eligibility should prevent the
private fixture driver from emitting continuation input for malformed or
frame-level-mismatched receipts while preserving nominal/typed-canceled behavior.
A valid child receipt is not generalized to ancestors. No production defect is
alleged: legacy and strict are the preceding research-only functions.

**T:** first restore and re-audit the consumed 6,653-request correction without
rerunning its evaluation workers. Then compose the unchanged functions with a
separate ordinary Tk Entry application and actual XTEST key input in a fresh
private authenticated TCP-disabled Xvfb per case. Six conditions, legacy then
strict each, two immutable six-case batches. Public source/gate readback preceded
all 12 new cases. No retries, replacements or post-result tuning. No nested-unwind
scientific matrix, user desktop/data, provider/model or external network experiment.

**D:** all 12 first outcomes, sources, app/policy/Xvfb exits, two launcher exits,
exact a/ab effects, focus, one app-observed press/release per emitted key, neutral
final full keymap/button state and cleanup must reconcile. Strict must refuse all
four representation/scope negatives and both arms must preserve stable/typed
cancel controls. A separate raw-only audit and all 12 effective copied-evidence
mutations must pass. Missing evidence is HOLD/STOP; contradictory complete results
are FAIL. No integrity gate was waived.

**C:** the producer and fixture are cooperative, the fault schedules are directed,
and the input driver has an explicit private-fixture permission. An upstream typed
transport could already rule out these malformed forms. The chosen token/domain
restrictions may reject a different valid producer contract. Receipt validation
is neither receipt authenticity nor independent proof of the current world.

**U:** no ancestor cancellation, arbitrary nesting, queue-semantic equivalence,
lost delivery, replay prevention, check/use atomicity, cross-platform behavior,
model/token/latency benefit or production acceptance is established. Same-author
separate audit code/process is not independent human review. Counts are exact and
dimensionless. CLOCK_MONOTONIC nanoseconds are diagnostic order only; CPU frequency
and host load are uncontrolled. No calibrated combined uncertainty or coverage
factor is estimated, and no natural failure probability is inferred.

## Retained correction: no new allocation

The old fixed corpus contains 6,144 well-typed semantic combinations, 438 malformed
mappings, 11 valid boundary mappings, 38 historical decision points and 22 wire
inputs: 6,653 requests to each of two workers, 13,306 responses. Original results:

| Gate | Retained result |
|---|---:|
| Strict/reference mismatch | 0 |
| Strict rejected malformed representations | 459/459 |
| Legacy RESUME from malformed representations | 256 |
| Strict unhandled exceptions | 0 |
| Historical decision dictionaries equal | 38/38 |
| Well-typed, level-matched legacy compatibility | 3,072/3,072 |
| Raw audit checks/errors | 76,792/0 |
| Effective corruption controls rejected | 14/14 |
| Unit methods | 24/24 |

These are finite engineering cases, not general GUI reliability measurements.
The 38 historical decision points came from 19 old GUI cases, not 38 new trials.
This continuation restored the original 65 files and obtained byte-identical
AUDIT/CONTROLS through the original read-only verifier. Evaluation-worker reruns=0.

## Fresh engineering smoke

| Condition | Legacy verdict / Entry | Strict verdict / Entry |
|---|---|---|
| STABLE | RESUME / ab | RESUME / ab |
| CANCELED_TYPED | CANCELED / a | CANCELED / a |
| CANCELED_STRING | RESUME / ab | YIELD_MALFORMED / a |
| GENERATION_BOOLEAN | RESUME / ab | YIELD_MALFORMED / a |
| LEVEL_CHANGED | RESUME / ab | YIELD_SCOPE / a |
| PENDING_BAD_ENUM | RESUME / ab | YIELD_MALFORMED / a |

Each cell is one fresh case, not a reliability estimate. Prefix a and any suffix b
are actual XTEST input, scored by the separate app's key/value journal. The app
reports typed cancellation; the string case changes only its transported field.
The strict policy receives no final scorer journal. Eligibility remains distinct
from authority; every policy output has input_authority=false.

12/12 cases complete. Both launcher exits, every application/policy exit and every
Xvfb termination receipt are 0. All full keymaps/buttons are neutral and all owned
sockets/locks are absent at cleanup. Frozen audit: 314 checks, errors=[].
Twelve mutation controls changed their targets and rejected; none was a no-op.
Batch invocations/reruns/replacements=2/0/0. Formal scientific invocations=0.

## Construction and chronology

Construction01 stopped before task input due to an Xauthority family mismatch.
Construction02 produced ordinary ab and clean release; its first audit incorrectly
expected nonempty KeyRelease.char. Before public freeze, the auditor was corrected
to require the actual empty release char plus exact paired keycode. Both original
sources/incidents remain retained. No formal result was repaired into PASS.

Public freeze commit `975a0b0399b5e6a7a034945304c9dbfa65aa5e19` binds the exact
legacy/strict functions, smoke runner, auditor, plan and environment. Source and
freeze Git blobs were read back before case0. Issue #4307 comments 5802897389 and
5803024110 distinguish reservation and executed result from its other allocation.

Environment: provided Linux x86_64 container, CPython3.13.5, glibc2.41,
Tk8.6.16, Python-Xlib0.15, private Xvfb/XTEST, same-host monotonic_ns. Docker CLI
and image identity were unavailable locally; no Docker/OrbStack equivalence claim.

## Reproduction and integration decision

The lossless capsule restores 162 files / 8,905,972 bytes, including all 65 original
correction files, all new raw cases/process receipts, construction incidents and
exact source. Original and new audits reproduce byte-for-byte without GUI or
scientific reruns. Eight separate packaging controls also pass. See README.md.

Concrete #2789 recovery constraint: validate receipt representation and exact
frame scope before treating it as resume eligibility; still obtain ordinary
current target/result/authority evidence before consequential input. This work
supports that engineering boundary only, not replacement of the parallel #4307,
#4302 or #4306 studies and not completion of #57 or the repository ROADMAP.

Bounded roadmap completed locally: immutable re-audit -> retained construction ->
public source freeze/readback -> two12-total-case engineering batches -> raw audit
and controls -> lossless delivery verification. Remaining publication stages at
this report's creation: exact GitHub tree readback, PR head review/applicable checks,
qualified merge/main readback, and only dependency-safe owned-ref cleanup.
