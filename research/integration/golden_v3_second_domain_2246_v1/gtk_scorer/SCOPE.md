# Independent GTK scorer scope

This scorer consumes only frozen GTK session/effect receipts. It does not import the adapter, send input, call a model/provider, grant authority, or replay ambiguous delivery.

A ready result is an independent receipt classification, not proof of live GTK coverage or the formal #2492 second-domain acceptance.
