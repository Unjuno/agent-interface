# Public CLI save reconciliation — Issue #4319

Source-first allocation `public-cli-save-requery-4319-20260924-k4m8`.
Owned additive path only. Base: `40493a9bb77eba00dd9d72207d683f2206ea45a5`.

This preformal commit retains exact source, plan, environment, source parity and construction notes. Formal invocations at publication: **0**. It does not claim a result or runtime promotion.

H: one same-app read-only save-result requery plus current continuation state can drive one guarded suffix through the ordinary public CLI, while preserving core stale-source/binding/lease refusals. Historical save, current eligibility, core admission, retained attempt and task effect remain separate.

T: 16 fresh private authenticated Xvfb/Tk sessions, 8 conditions x2 policies, two immutable8-case batches. Every task key uses the unmodified public CLI dispatch; each retained attempt is inspected read-only. No model/provider, user desktop or experiment network; provided Linux container, not Docker-image-attested replication.

D: exactly3 positive suffix effects;6 core refusal controls; remaining missing/canceled/changed-queue cases have no suffix. All16 source/process/raw records, zero unsafe/duplicate input, exact saved files, independent raw audit and12 effective corruption controls required. First contrary result remains FAIL/HOLD/STOP, never rerun into success.

C/U: cooperative app, authored identity counters and quiescent final-check interval. No source authentication, atomic check/use, arbitrary application, crash durability, model/token/latency or product guarantee. Same-author separate auditor is not independent human review.

`SOURCE.json` describes a lossless22928-byte tar.xz split across6 binary parts. It restores16 exact original files including the complete H/T/D/C/U `PLAN.md`, `FREEZE.json` and runner/auditor. `vendor/` reuses25 main-identical implementation blobs plus2 one-newline packaging initializers. FREEZE binds39 files.

Data-only restoration, new trusted destination:
```sh
python -B restore.py /tmp/cli-save-4319-source
```
This never invokes a GUI. Do not run the consumed formal allocation for audit. After result delivery, the same restorer will also restore `EVIDENCE.json` when present; read-only audit commands will be in REPORT.md.

Source archive SHA256: e1efc58c9ae94e72cba3b2a719072d2af8aac8692069fd1b80037e36dfa76b94
FREEZE SHA256: 4fb5441a6ff92da631c91d45e4bcf45063284c9414a5f282eb6ce2b0e8b1124c

Prior r9b2 pilot remains separate/unchanged; #4309 and #4311 allocations are neither rerun nor modified. No shared runtime, workflow or global index changes. Global ROADMAP remains open.
