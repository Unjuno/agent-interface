# Issue #3752 — request-temp crash successor allocation 02

Status: preregistration only. No formal execution has occurred.

Frozen Git blob identities: experiment runner `17bf0b08bd5d234c6f5949420fe0369bcf2ffbea`; independent auditor `918480c057ddc257276ebdf69d94760965fb8bda`; this plan `c9704f04fa91cef74cbb7d33e48bb9ac2482e297`; freeze manifest `0b57dee0a8e8e59356c85096c015506d3bc5ed7a`. Runner SHA-256 `81092a01f39ce8da04ce163119e032ca68e8634dff2909c2d12e7c70330af0a9`; auditor SHA-256 `a6e1925062961746eb4c99804d540604e2abcc0a273f5ba58913e7f1867c8a5d`.

## Parent allocation 01 disposition

Allocation `issue3752-request-temp-crash-01` remains unchanged and unexecuted. Its frozen runner/auditor are preserved on branch `research/issue-3752-request-temp-crash-20260921` (commit `11081e36536ac988a27a3194fd05bddaa37fbd6a`). This successor does not edit or relabel those inputs or claim a result for allocation 01. Reason for successor: make retry/reuse exercise the public CLI `dispatch` path rather than only calling `invoke()` directly; bind the exact final argument/contract artifacts; and strengthen the independent auditor to recompute the expected request and the full snapshots from raw evidence.

## H / T / D / C / U

- **H — Hypothesis:** An abrupt child-process exit after writing and flushing a strict prefix of `.request.json.tmp`, before `request.json` publication and backend dispatch, remains an unknown attempt. Two real `attempt-status` CLI reads report `unknown_or_incomplete`, disable replay, list the temp residue and preserve all bytes. Reusing the same run directory through public CLI `dispatch` refuses before backend invocation and leaves the full snapshot unchanged.
- **T — Test:** Freeze main `76965311d899815174b5ed081ff439bd4533ef63`, `attempt.py` blob `70cc62b450c8b9c8aaa0db49b1e116388368fe4c`, `__main__.py` blob `8a9178b7b8721b8a5471fabcfbeb1cb55976980c`, and the successor runner/auditor/fixture/manifest blobs in `FREEZE.json`. In one isolated Docker container, the child uses the frozen `invoke()` request-persistence boundary with a patched writer to flush an exact strict prefix and `os._exit(29)`. Parent runs two real status CLI subprocesses and then calls public CLI `main()` with only its `dispatch` callable patched to a marker-writing stub. Check exact expected request prefix, all status JSON/exit codes, no request/report/marker, snapshots at every stage, CLI refusal, and zero backend marker. No GUI, model, native input or network.
- **D — Decision:** PASS only if every preregistered invariant holds. Any scientific mismatch is FAIL. Image/engine/source/runner mismatch or execution setup failure before child launch is STOP, not scientific FAIL. Exactly one formal execution; no retry under this identity. Independent auditor runs separately in a fresh pinned container, with evidence mounted read-only and separate output; it re-hashes frozen source/runner/fixture/manifest, reconstructs expected request bytes, validates raw stdout and all snapshots against actual files, and emits an auditable disposition.
- **C — Constraints:** Local OrbStack Docker; `linux/arm64` (host native architecture), Python 3.12.14 image `python:3.12-slim@sha256:2f17fc044b579bab302c2e8054d3a686e2cb9a83de48e70534b94cd8ebbe06a9`; network disabled, root filesystem read-only, source mounted read-only, only fresh bounded output writable, tmpfs `/tmp`, one CPU, 512 MiB, PID bound, all capabilities dropped and no-new-privileges. Current preflight: 1.5 TiB available on host; Docker server 29.4.0 linux/aarch64; pinned image already present. Confirm exact image/platform and nonempty-output guard immediately before formal start.
- **U — Scope:** One process-level interruption point for one synthetic request journal. Does not establish power-loss durability, every filesystem, cleanup/deletion safety, native side effects, generic crash safety, or full #3711 adoption. Earlier #3725/#3735 evidence and allocation 01 remain immutable.

No formal command is permitted until this plan and `FREEZE.json` are committed and the exact runner/auditor hashes are posted on Issue #3752. Any mismatch is STOP and requires a new successor allocation.
