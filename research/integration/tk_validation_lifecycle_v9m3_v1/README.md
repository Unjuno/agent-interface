# Tk validation lifecycle: pre-allocation publication STOP

Refs #4367; predecessor #4091 / merged #4115 remains unchanged.
This is metadata-only Draft material. Do not merge as a complete research
bundle or implementation, close #4367, or infer formal acceptance.

## Disposition and chronology

STOP_PRE_ALLOCATION_PUBLICATION / HOLD_PENDING_PERMITTED_PUBLICATION.
Formal cases 0/24; formal batches 0/4. The protocol required complete public
source/gate publication and readback before case 0. That gate was not met.
A local source freeze is not a substitute for the declared public gate.

Issue and branch creation succeeded. One source tree was created but never
committed or referenced by this branch. The next source-tree operation was
blocked by the connected tool because its safety status could not be determined.
No additional reason was supplied. Blocked contents were not resent through
another operation, path, encoding or archive. This is an operation-specific
block, not general GitHub authentication/write unavailability or a scientific
contradiction. No implementation, raw corpus or source capsule is attached here.

## Actual construction, excluded from formal results

An initial Xauthority mismatch stopped construction before input. Its original
source, partial row and exit evidence were retained. A startup-only correction
was then tested in two separate six-case construction batches; both completed
with observed zero batch, supervisor, application and server exits.

The 12 construction cases compared classic Entry and themed ttk::entry,
three validation behaviors, and two continuation checks. In the normalizing
classic case, current text was abc while validate was none. The context-only
caller sent d without the d validator running; the mode-aware caller refused
before d input. The themed case kept key validation and rejected d insertion.
These are construction observations, not the planned 24-case acceptance result,
natural failure rates, a toolkit vulnerability or production evidence.

The separate raw-only construction audit reports 12 cases, no errors and eight
rejected evidence mutations. Eleven pure policy/source unit methods passed.
The auditor's scoped PASS label is explicitly construction-only. It does not
satisfy the unexecuted formal denominator or establish independent human review.

Environment: supplied Linux x86_64 container, CPython 3.13.5, Tcl/Tk 8.6.16,
Python-Xlib 0.15, private authenticated TCP-disabled Xvfb. No Docker/OrbStack
image attestation, model/provider, host desktop, clipboard or user-data work.
The old 36-case #4091 allocation was only re-audited read-only, never rerun.

## H / T / D / C / U and remaining roadmap

H: matching edit state may coexist with an inactive relied-upon validator.
T: excluded construction, then exact public source/gate readback and 24 cases.
D: construction observations retained; formal acceptance untested; publication STOP.
C: cooperative application, fixed ASCII and no intervening writer are assumptions.
U: arbitrary validators/apps, recovery, model utility, performance and production
qualification remain untested. Native-input refusal and app-insertion rejection
are different outcomes; a preserved abc is not completion of an abcd task.

Completed: intake/ownership, source recovery, excluded construction, local freeze,
Issue/branch, and STOP metadata. Remaining: permitted complete publication,
formal allocation, full raw audit, qualified evidence PR and main integration.
Keep this branch while its Draft depends on it. No shared runtime, workflow,
index, predecessor or foreign branch is changed. Global ROADMAP remains open.
