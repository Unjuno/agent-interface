# Excluded construction

One matched xterm pair, excluded from formal allocation.

- baseline `endpoint_only`, seed826001: later `clock` timed out under 300 ms; wrapper exit0.
- candidate `initial_observation_gate`, seed826001: later `clock` reached boundary; wrapper exit0.
- candidate initial append -> endpoint publication: 0.139062 ms.
- candidate endpoint publication -> clock emit: 4.698817 ms.

No construction row is pooled into formal. No timeout/gate/source changed after this observation.
