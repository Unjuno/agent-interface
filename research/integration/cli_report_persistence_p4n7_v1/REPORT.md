# CLIの実入力と結果保存を分離する検証 — #3711 / p4n7

## 結論と公開状態

**PASS_LOCAL_CLI_REPORT_PERSISTENCE_BOUNDARY**。16件の新規Tkアプリケーションを使い、未変更の配布CLIから実際のX11入力を行った。入力後の報告保存失敗8件でも、正常に動作するstdoutにはnative完了が残り、保存失敗と非ゼロ終了が明示された。ファイル経由の回収は未完了または不正な記録と判定し、入力を再送しなかった。

これは局所的な工学的検証のPASSであり、保存機構がすべての障害に耐えるという結果ではない。**正式報告ファイルを保存できなかった8件は、そのまま保存失敗である。** 完了した未公開の一時ファイル4件も、正式報告へ昇格させていない。

GitHubへのIssue・コメント・remote branch・PR・merge・branch削除は0件。このセッションには書込み操作が公開されておらず、代替探索でも実行可能な経路を得なかった。後日の公開は今回のローカル検証の事後公開であり、GitHub上の事前登録とは扱わない。#3711、#57、#2789、全体ROADMAPは未完了。

## 問いと前回との差

前回の `cli_native_output_loss_v1` は、report.json保存後のstdout損失と読取り専用回収を扱った。今回は障害位置を変え、**実際のキー配送後にreport.jsonの一時ファイル作成または最終renameが失敗する場合**を検証した。旧GUI試験は再実行せず、旧ZIPのSHA-256は不変である。

既存Issue #3711は結果保存失敗の明示、未完了attemptの回収、再送禁止を要求している。同じ問いの新しい実入力条件であり、監査・公開障害を理由に別の研究Issueを増やしていない。

GitHub MCPでmain、README、CURRENT_GOALの現行方針、ROADMAP、失敗分類、最近のopen/closed Issueとopen PR、最初の100 branch名、関連する保存課題とbranch検索を確認した。取得範囲で同一のnative報告保存実験は見つからなかったが、検索は非原子的・有限範囲で、未公開作業の不存在は保証しない。追加先は `research/integration/cli_report_persistence_p4n7_v1/` のみ。

## ソース・実装範囲・環境

開始時と終了時のmainは `4c701cc51b06296268ad8d9ae3eff1dd6f2d379d`。実行したGitHub Actions配布物は `e01dbbc40d798ed61f3c0e84c1abb6861f4601d9` から構築されている。両commit間の取得差分は研究パスのみで、runtimeの変更はなかった。新mainで再構築したとは表示しない。

- 外側ZIPのSHA-256: `60339fbdb86a62838fd68f37ce136d13bfb79aab1df6fe5f75f53b1e78e82c53`
- 実行pyzは173181バイト、SHA-256: `b7490f97a01991009e72e1b03410ca244cceeabba12103549c1288ef464c5f19`
- upstream `attempt.py` のGit blob: `bd1725a18b6aef6f45c62297cbd794c760ea0a9f`
- 配布物の31ソースメンバーはmanifestと照合済み。CLI・API・core admission・selector・X11 backend・保存処理は未変更。

提供されたLinux x86_64コンテナ、CPython3.13.5、Tcl/Tk8.6.16、Python-Xlib0.15、Pillow12.3.0。Docker/ghはなく、Docker/OrbStackイメージ固定再現ではない。専用の認証付き・TCP無効Xvfbを各バッチに新設し、各ケースには新しいTkアプリケーションを作った。実験用画面は640×360・depth24、撮影領域は420×160。ユーザーデスクトップ、ユーザーデータ、外部実験ネットワーク、モデル/providerには接続していない。

通常の `python -B agent-interface-runtime.pyz dispatch` を新規run-directoryで起動した。モック、APIの置換、backend直接呼出しによる代用はない。操作はfocus、固定待機30ms、文字7、固定待機200ms、撮影、release_all。固定待機はアプリ処理完了の証拠ではない。観測版41・binding版7・単調時計20秒先の期限は呼出し元が与えた値であり、意味上の最新状態の自動認証ではない。

## H/T/D/C/Uと実行前固定

| 項目 | 内容 |
|---|---|
| H | 入力後の報告保存失敗をnative実行失敗と混同せず、未保存報告の回収は未解決のままとする。入力前の保存先衝突は実行前に拒否する。 |
| T | 4保存条件×2Entry状態×2新規反復の16件。2つの固定8件バッチ、後半は前半の条件順を反転。構築用8件は除外。 |
| D | 全16件の固定ソース、原記録、実行終了、入力効果、保存状態、回収不変性、中立状態が一致し、原記録監査と10有効破損対照が合格すること。完全な反例はFAIL、不足はHOLD/STOP。 |
| C | 単一協調アプリ、既知の対象、正常stdout、信頼する私有保存先。2種類の名前空間・型エラーを意図的に導入する。任意の保存障害を代表しない。 |
| U | 自然な失敗確率、モデル回復、token/速度優位、電源断耐性、汎用GUI保証は不明。校正された合成標準不確かさ・包含係数は推定していない。 |

完全な条件、順序、停止基準、変数の意味・単位・定義域・型と単位チェックは `PLAN.md` に保存した。14ファイルを正式実行前にローカル固定し、実行後にも一致を確認した。

`FREEZE.json` SHA-256: `da26e2c4e7da044ea965249de6f1f0a94324395c481d8d506f25392f0dec4923`。

これは**ローカルで事前固定した工学的allocation**であり、公開事前登録ではない。正式バッチは2回とも一度ずつ実行し、再実行・行差替え・除外・固定後の科学ソース変更は0回。

## 観測結果

入力目標は「Aに7、Bは空」。各保存条件の4件は、通常Entry2件とreadonly Entry2件である。

| 保存条件 | native完了 | Aに7 | dispatch終了コード | attempt-status | 残存した報告一時パス |
|---|---:|---:|---:|---|---|
| NORMAL |4/4|2/4|0|report_recorded|なし|
| TMP_OPEN_BLOCK |4/4|2/4|2|unknown_or_incomplete|同名の障害用ディレクトリ|
| RENAME_BLOCK |4/4|2/4|2|invalid_record|完全だが正式保存されていないJSONファイル|
| REQUEST_CONFLICT |0/4|0/4|2|unknown_or_incomplete|なし|

TMP_OPEN_BLOCKでは、最初の実KeyReleaseを受けたアプリが、所有するattempt内に非空ディレクトリ `.report.json.tmp` を作成した。実CLIの排他的作成がFileExistsErrorで失敗した。RENAME_BLOCKでは同じ時点で非空ディレクトリ `report.json` を作成し、実CLIの最終renameをIsADirectoryErrorで失敗させた。障害導入の原イベントと実行区間を監査し、入力後に起きたことを確認した。共有ディレクトリ、権限、ディスク容量は変更していない。

REQUEST_CONFLICTでは入力前に新規の実験所有ディレクトリを作り、CLIの新規予約を失敗させた。4件とも `REQUEST_PERSISTENCE_FAILED` と `operation_invoked=false`、ネイティブ入力0件だった。

readonly Entryは実際のキーイベントを受けたが文字を挿入しなかった。このため、全体のnative完了12件に対して正しい文字効果は6件。Bは全16件で空のまま。成功した報告保存は4件、入力後の保存失敗は8件、入力前の拒否は4件だった。

**同じ終了コード2でも、「7が入力済み」「入力は配送されたが文字効果なし」「API呼出し前に拒否」の3種類があった。終了コードや保存失敗だけでは、再入力してよいとは判断できない。** NORMALの終了コード0も、readonlyでは文字効果を保証しない。

## 読取り専用回収と一時報告

各dispatchの実終了後に、別CLIプロセスで `attempt-status`、最終reportへの `receipt --raw`、再度 `attempt-status` を実行した。合計48回。これらの環境からDISPLAY/XAUTHORITYを除外した。原ディレクトリ全体のファイル種別・ハッシュ、アプリ値と入力イベントは読取り前後で一致した。

保存成功4件だけがreport_recordedとなり、回収JSONは原reportと一致した。残る12件のstatus/receipt読取りは終了コード2。すべてのattempt-statusで `replay_allowed=false` と `process_state=unknown` が保持された。実終了は実験監督が別に観測したもので、attempt-statusがプロセス生存状態を推測したのではない。

rename失敗4件には完全な一時報告が残った。オフライン監査では、正常stdoutからretentionを除いた原API結果と一致した。しかし、呼出し元の回収経路は一時ファイルを採用せず、最終reportパスを不正な記録とした。一時ファイルのrename・削除、欠落最終報告の再構築、入力再送は行っていない。

今回はstdout自体は正常である。保存障害とstdout損失を同時に起こした試験でも、プロセスクラッシュ・fsync失敗・ディスク満杯・電源断の試験でもない。

## 監査、失敗の保持、後処理

原記録のみを読む別実装の監査器は1157項目を検査し、errors=[]。10種類の意味・来歴の破損対照をすべて検出した。各対照では無変更の移設コピーが先に合格し、変更バイトと対象の意味エラーの検出を確認している。単なるparser crashや無効な変更ではない。監査器はアプリ・driver・runtimeをimportしない。ただし同じエージェントが作成しており、第三者レビューではない。

構築用8件では、最初の監査器がunknown_or_incomplete時のCLI終了コードを誤って0と期待し、4項目を失敗させた。元監査器と失敗出力を保持し、正式固定前に期待値だけを修正した。同じ構築データを読取り再監査した結果は588項目・errors=[]、10破損対照合格。GUI構築のやり直しはない。非存在のoptional moduleを照会した取得時KeyErrorも別途保存した。

全16終点で別のX接続が完全な32バイトkeymapの中立状態とボタンmask0を確認した。実行された12件には実際のrelease記録がある。入力前拒否4件に架空のrelease記録は足していない。これはXサーバーの論理状態で、物理HIDではない。

84の追跡対象子プロセス（16アプリ、16dispatch、48読取りCLI、2 Xvfb、2 driver）は実終了記録と終了後の同一PID/start-ticks不在を照合した。外側launcherとxauth設定は84件に含めず、その終了記録は別に残した。owned表示ソケット・認証一時ファイルを後処理し、実験所有プロセスは残っていない。

監査はPNGのファイルハッシュ、寸法、メタデータ結合も確認するが、X11 raw-image digestから画素を独立再構成するものではない。使用量が不明なことをtoken使用量0に読み替えず、時間の記録も速度改善として解釈しない。

## 引継ぎと適用判断

証拠は原source、配布pyz、request/reportと残存一時データ、stdout/stderr、イベント、PNG、actual exit、構築失敗、固定manifestを含む。後から追加した `verify_readonly.py` は配布物の検証用であり、実行前固定ソースに混ぜていない。元の正式監査・対照と、失敗したものを含む構築監査の出力を再現するが、新しいGUI試験は実行しない。

適用上の判断は、**実行結果、アプリケーション効果、正式報告の保存、回収可否を別の状態として扱うこと**。既存の保存失敗表示と再送禁止を維持する。この実験は新しい自動復旧方式を実装しておらず、研究用fixtureの障害導入コードを共有runtimeへ採用する提案でもない。

ローカルの課題確認・構築・固定・正式実行・原監査・後処理は完了。GitHub公開、レビュー/CI、main反映は別の未完了段階である。公開用パッチは最小ローカルGitの追加差分であり、remote mainの子孫コミットやrepository全体CIを主張しない。依存関係が未確認のremote branchは削除しない。

関連する3分野は、ソフトウェア信頼性（故障位置別の観測）、分散システム（実行と応答・永続化の分離）、HCI（配送と意味上の文字効果の分離）。いずれも本試験だけから汎用保証には拡張できない。

ERROR CHECK: 16件、2固定バッチ、14固定ファイル、1157監査項目、10有効対照、構築失敗と84追跡子の終了を照合。原結果を改変せず、ローカルPASSと保存障害・公開未完了を分離した。
