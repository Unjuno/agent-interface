# Retrospective delivery for #3711 — CLI report persistence p4n7

This directory publishes the already executed local allocation `cli_report_persistence_p4n7_v1`. It is **retrospective evidence**, not GitHub preregistration. The formal allocation was locally frozen before execution; GitHub write publication was unavailable in that earlier session. No scientific case is rerun here.

Disposition retained from the original allocation: **PASS_LOCAL_CLI_REPORT_PERSISTENCE_BOUNDARY**. Sixteen fresh Tk/X11 cases separated native execution, application text effect, report persistence, and read-only recovery. The same CLI exit code 2 occurred after a correct text effect, after native delivery with no text effect, and before API invocation. Missing or invalid retained reports remained non-replayable. See `REPORT.md` for H/T/D/C/U, limits, construction incident, process accounting, and exact result table.

Complete original evidence is retained losslessly as base64 text parts under `capsule/`. Reconstruct with:

```sh
python -S -B restore.py
```

Expected ZIP: `agent_interface_cli_report_persistence_evidence.zip`  
Size: `522175` bytes  
SHA-256: `9f9ef47584e2d6de6a264f1482b9ce91bf90e29427d8c7b289afc2d535c1eed4`

`DELIVERY_VALIDATION.json` records the original local distribution/patch verification. The historical report text saying GitHub writes were unavailable is intentionally preserved as chronology and is not rewritten. Publication state and scientific disposition remain separate. #3711, #57, #2789 and the global ROADMAP remain open.
