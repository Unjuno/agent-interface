# A2 supervision-only freeze

A1 stopped at the outer 45-second supervision ceiling after 7 complete result rows; m08 was partial with no result. A1 is not resumed or pooled.

A2 changes supervision only: same frozen intent/compiler/fixture/run_case/audit and same route/order/gates, fresh disjoint n01..n08 IDs and ports 9511..9518, exactly one case per outer invocation.

No A2 case has run at this freeze. No completed/partial A1 ID will be reused.
