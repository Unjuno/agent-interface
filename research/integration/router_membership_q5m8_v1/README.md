# Router membership revalidation — publication HOLD

Issue #4430. Lineage: #3446 / PR #4291; related uniqueness dependency #197.

This branch is a retrospective publication record for the already consumed local allocation `router-membership-3446-20260925-q5m8-01`. It is **not** GitHub preregistration and does not authorize a scientific rerun.

Retained scientific disposition: **PASS_LOCAL_ROUTER_MEMBERSHIP_BOUNDARY**.

- 16 fresh SQLite registry histories;
- 64 paired advisory validations (four policies on the same 16 histories);
- 96 distinct scientific child processes;
- four immutable formal batches;
- independent raw/SQLite audit: 751 checks, errors=[];
- 13/13 effective copied-evidence corruptions rejected;
- formal reruns/replacements/post-result tuning: 0/0/0.

Decision table:

| policy | RETAIN | YIELD | unsupported RETAIN | unnecessary YIELD |
|---|---:|---:|---:|---:|
| whole-registry revision | 2 | 14 | 0 | 6 |
| selected-adapter generation | 12 | 4 | 4 | 0 |
| request-scope revision | 6 | 10 | 0 | 2 |
| unique-selection recomputation + selected generation/epoch | 8 | 8 | 0 | 0 |

Every output remained advisory: `grants_authority=false`, `adapter_invoked=false`, `task_success=null`. The four unsupported retentions are newly ambiguous proposals, not observed wrong-target executions.

## Publication disposition

**STOP_PUBLICATION_CONTENT_TRANSFER_INTEGRITY / HOLD_RAW_NOT_COMMITTED**

The original complete conversation ZIP is 457 files and is retained outside this branch with SHA-256:
`bf3843a1bd145d03d8b6942fefd45dd009432f596ed05c50e3ff9ee3530c57e9`.

A deterministic complete evidence capsule and a smaller review capsule were reconstructed locally and passed read-only re-audit. However, this GitHub connector accepts blob content only as an in-band string and exposes no local-file/file-reference upload action. Repeated unreferenced blob staging detected content-address mismatches between local expected Git object IDs and returned remote IDs. **No mismatching blob was attached to a tree or commit.** The transfer was stopped rather than claiming remote evidence completeness.

Do not merge this branch as scientific evidence until the exact source/raw/process/audit bytes are reachable from a fixed Git commit and reconstructed/re-audited from those Git objects. Do not rerun the consumed scientific allocation merely to repair publication.

No shared runtime, workflow, root direction or predecessor evidence is changed here.
