# Preformal encoded-budget correction

Formal batches executed: 0. The PLAN.md descriptive Base64-image bound of4096 was an arithmetic error: three separately encoded1024-octet patches each have1368 Base64 characters (including padding), hence4104, not4096. The decoded-image gate3072 is unchanged. This correction supersedes only the descriptive4096 statement. No source, scenario, scientific threshold or outcome changed. PLAN.md is kept byte-identical to its original public version; this dated correction is included in the final preformal hash freeze.

No measured model token or network-byte reduction follows. JSON metadata is accounted separately.
