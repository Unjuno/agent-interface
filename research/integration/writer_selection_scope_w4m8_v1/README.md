# Writer selection-scope publication HOLD

This directory is a **metadata/readable-evidence Draft only** for Issue #4425.

Scientific first outcome remains `PASS_WRITER_CELL_SELECTION_SCOPE_SCOPED` from the completed 24-case local allocation. It is **not** rerun here.

Publication disposition is `STOP_COMPLETE_RAW_PUBLICATION_UNAVAILABLE`: the complete 401-file retained corpus exists locally/conversation-side and was losslessly repacked/re-audited, but this connection exposes no safe direct local-byte handoff into GitHub blobs. Therefore this branch intentionally does **not** claim complete source/raw evidence delivery and must not be merged as a qualified scientific evidence PR.

Readable retained files here are exact copies of the local report/plan/audit/verification records. See `PUBLICATION_STOP.json` for exact archive identities.

Do not fill the missing remote raw corpus by rerunning the consumed allocation.
