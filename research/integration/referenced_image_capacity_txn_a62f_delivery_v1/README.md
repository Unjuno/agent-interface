# Referenced-image capacity transaction — partial retrospective delivery

Issue #4093. This branch publishes an already executed local allocation. It is not
GitHub preregistration and no formal case is rerun for publication.

Scientific disposition: **PASS_IMAGE_CAPACITY_TRANSACTION_SCOPED**. The
metadata/cursor-first comparator remains **FAIL_SAVED_CURSOR_IMAGE_COMPLETENESS**
in six storage-capacity fault cases.

The complete 1,089-file additive raw/source patch was hash-verified locally and
split into exact Git blobs, but the connector blocked tree attachment at its
safety check. Therefore this directory is intentionally **partial** and must not
be merged as complete source/raw delivery.

Directly retained here:
- REPORT.md — original report, exact Git blob checked before publication.
- FORMAL_AUDIT.json — original frozen audit output.
- PACKAGING_CHECK.json — local delivery/patch validation record.
- PUBLICATION_STATUS.md — current delivery STOP and exact missing evidence.

Do not rerun the consumed formal allocation to repair publication. The complete
original conversation bundle and additive patch remain separately retained.
A later supported publication may append the missing raw/source bytes without
rewriting this chronology.
