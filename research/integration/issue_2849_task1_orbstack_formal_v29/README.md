# Issue #2849 formal task-1 OrbStack successor v29

Preregistered one-shot successor seed 284929: task-1 / layout A / cold / plain. It uses the response-type-filtering nested runner independently exercised by the v28 saved-response replay. The exact scope is one formal task, at most one host Codex image-backed call, no retry, network-none outer and nested containers, and no six-task or efficiency claim. A complete independent audit and immutable evidence bundle are required before any result is called PASS.

Pre-task STOP conditions include moved `origin/main`, changed predecessor source closure, any changed frozen source/input hash, unexpected output or IPC files, Docker socket/image/CLI identity mismatch, wrong selected backend, incorrect task identity, more than one IPC/model call, nonzero runner/outer result, or missing audit evidence. No task token should be requested until freeze validation and the OrbStack socket preflight pass.
