# #4197 retained-evidence reconciliation — s4a1

Engineering audit under the existing scientific question, not a successor live
experiment. Preserve #4197/#4202, #4242 and every old allocation unchanged.
Intake main: 4c701cc51b06296268ad8d9ae3eff1dd6f2d379d.
Own only research/integration/issue4197_readonly_qualification_s4a1/**.

## Already observed before this plan

The published source archive matches its declared SHA256 and contains the exact
original FREEZE and ENVIRONMENT. All six embedded Python sources match their
freeze. Outer FREEZE has equal JSON values with different formatting; outer
ENVIRONMENT omits only the original xvfb diagnostic array. The latter reports
an unsupported -version probe; it does not identify an Xvfb version.
The published construction archive has a verified Git blob ID but strict Base64
decoding fails with Incorrect padding. No padding/content repair is authorized.
The archived auditor source suggests missing consistency and exact-type checks.
These are discovery/inspection facts, not prospectively measured findings.

## H / T / D / C / U

H: the unchanged archived audit rejects every contract-inconsistent derivative
in CASES.json. The contrary result narrows its historical coverage, not the
truth of the native mechanism or the validity of independent predecessor work.

T: after public source/gate publication and exact identity readback, execute
verify_readonly.py once in the provided Linux container. It consumes the five
exact original repository blobs, extracts only the eight enumerated source
members, and verifies all archived hashes. Only the reviewed stdlib audit.py
and controls.py execute: one original raw input, twelve well-formed copied
inputs, and the ten original controls. Retain every input, stdout, stderr,
argv, PID and observed exit. No native actor, candidate, bridge, GUI, model or
historical scientific allocation runs. Then independently reconcile the saved
receipts with reconcile.py; it imports neither auditor nor runner.

D: all expected inputs/process receipts/hashes and effective derivative changes
must reconcile. An original-auditor PASS for any contradictory derivative is
FAIL_AUDITOR_SUMMARY_COVERAGE for that directed test, not a native/runtime FAIL.
All twelve rejected would support only those twelve checks. Malformed source,
missing rows, timeout or a process inconsistency is HOLD/STOP, not scientific
failure. Independently report original-source recovery, outer-file differences,
construction publication failure, and absence of independently retained native
pixels/events in the decodable source/raw portion. Do not relabel hashes or
summary flags as the missing native observations. Do not upgrade historical
scientific PASS based on this engineering audit.

C: derivative changes are source-guided diagnostic controls, not natural failure
rates. Python JSON boolean/number distinctions are compared by canonical bytes,
not value equality. A hash proves byte identity only against its stated anchor;
it is not proof of a process having executed or an external event occurring.
No statistical sample-size or speed claim is made. Existing sources/results are
not rewritten; public ancestors remain readable. Unavailable construction bytes
may have an original elsewhere, but they cannot be guessed from a checksum.

U: missing capture payloads and native event/process lineage cannot be repaired
by editing the auditor or rerunning old science. No third-party review, broad
GUI reliability, real-time bound, model/token/latency benefit or runtime adoption.
The original live hypothesis is not tested in this session.

## Field and unit table

| Field | Meaning | SI unit | Definition/range | Type |
|---|---|---|---|---|
| bytes | retained file/payload size | dimensionless, byte count | nonnegative integer | scalar integer |
| sha256/git_blob | content identifier | none | lowercase hexadecimal digest | string |
| pid | observed audit child process identifier | none | positive OS identifier; no incarnation claim | integer |
| exit_code | observed audit child return code | none | actual subprocess result; boolean invalid | integer |
| session | position in historical summary | none | zero-based integer, not boolean | integer |
| geometry | recorded X11 rectangle | dimensionless pixel count | x,y,width,height in historical record | four-integer vector |
| create_ordinal | recorded lifecycle sequence position | none | positive integer in stated observer epoch | integer |
| enter_boundary | recorded decision flag | none | JSON true/false only | boolean |

Unit check: this study compares file sizes to file sizes and hashes to hashes.
It does not convert recorded pixel coordinates, process exits or audit counts
into elapsed time, physical input or application success. Four source sessions
and twelve derivative records do not become sixteen independent live trials.

## Roadmap / stop discipline

Exact intake and original-byte recovery -> pure construction tests -> public
source/gate readback -> one retained data-only invocation -> independent receipt
check -> additive evidence/regression PR -> applicable exact-head CI and scoped
review -> qualified main readback. No rerun merely to replace an unfavorable
coverage result; read-only reproduction is separately labelled. Keep parent
#2789 and the global ROADMAP open. The immediate integration use is preventing
an overstated independent-evidence claim from becoming an adoption gate.
