# Integrated efficiency phase ledger v1

Issue #836. Immutable BASE `f9989b833611fb9cb8708007445b94a95d70c516`.

H: the retained comparison can be reconciled into a phase-complete multidimensional ledger. Persistent should expose less model-visible work but more local observation/durable-call work than plain control.

T: container-only deterministic reconstruction from a frozen extracted fixture tied to exact retained report/audit/#829 Git blobs. Shared schedule phases are preflight, acquisition(task1), repeated-A opportunity(tasks2-3), layout-change/repair opportunity(task4), repeated-B opportunity(tasks5-6). Only retained persistent route labels are interpreted as reuse/repair.

D: PASS only on exact arm/global usage, elapsed, call/image/task/correctness reconciliation and lower persistent model-side counts. Any source/reconciliation mismatch is FAIL_INTEGRITY; missing dimensions are HOLD.

C/U: local work counts are not latency/energy/cost units; fewer model calls may explain model-side reductions. One Chromium allocation cannot establish population, second-domain, human-tempo or monetary-cost claims.
