# Live typed negative outcome binding v1 — plan

Issue #4230; parent #39; predecessor #4174.

## H
Explicit current live-app evidence preserves typed negative/progress/conflict outcomes and safe required-change guidance better than effect/no-effect+timeout collapse, without granting authority.

## T
Private Xvfb/Tk/XTEST fixture; nine frozen conditions x three Latin-rotated repetitions =27 fresh lifetimes. Decision snapshot100ms after admitted click. IN_PROGRESS later truth is scoring-only. Precondition-negative rows dispatch no task input. Independent auditor does not import candidate policy. Formal one supervisor invocation, timeout60s; no rerun/replacement/tuning.

## D
PASS_LIVE_TYPED_NEGATIVE_OUTCOME_BINDING_SCOPED iff all27 rows, each label3 times, candidate/oracle agreement27/27, authority false27/27, identical retry false27/27, precondition-negative input0/15, live success3/3, pending-then-later-effect3/3, incomplete evidence remains FAILED_UNKNOWN3/3, conflict3/3, coarse baseline futile retry>=12 and premature terminal>=6, child/process cleanup complete, audit errors=[], >=10 corruption controls reject, supervisor exit0 and timeout=false. Any false success/terminal certainty/authority/retry/precondition input is FAIL. Missing process/raw/source/cleanup evidence STOP/HOLD.

## C
Cooperative Tk semantics are explicit fixture receipts, not arbitrary visual inference. Coarse comparator is deliberately incomplete. XTEST is X-server input, not physical HID.

## U
No planner/model comprehension, token/round-trip benefit, natural rates, second domain, public runtime promotion, crash/restart or production claim.
