# Result: #2704 crash-safe native exchange

Status: `HOLD_NATIVE_FIXTURE_UNAVAILABLE`.

The additive transport preflight passed all six registered loss-point contract
tests and hard-coded that unknown emission is never automatically replayable.
The formal native allocation did not start: the locally available GTK images
contain no LibreOffice/Calc executable. The GTK fixture image has Python GTK
introspection, but that is not the requested Calc owner and cannot be used as a
substitute for the issue's native Calc gate.

No native emission, effect, release, exactly-once, or crash-restart claim is
made. Existing owner-stop and exchange results remain immutable.
