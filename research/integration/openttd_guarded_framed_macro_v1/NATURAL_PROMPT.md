Compile only the stable structure already present in the retained OpenTTD procedure. Keep screen-fixed chrome distinct from window-relative map content, preserve the finite target/repeat branch, and require a current pointer binding before producing a resolved target. Do not invent semantic targets, new coordinates, new task outcomes, authority, or fallback behavior. If binding provenance is missing or changes after resolution, yield instead of guessing.
