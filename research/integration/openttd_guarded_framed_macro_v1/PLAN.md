# OPENTTD-GUARDED-FRAMED-MACRO-TRANSFER-20260917-001

H: A bounded OpenTTD procedure that already declares screen_chrome vs window_content can be compiled once into a current-binding-guarded framed macro that reproduces retained 1280/1152 target paths and branch semantics, while raw literal source-coordinate replay mismatches changed-resolution window-content paths.

T: Standard-library deterministic retained-evidence replay. Five fixed states x two representations = 10 formal rows. No GUI/model/network/input. Source-first freeze before the one formal invocation. Construction is toy-only.

D: PASS only if candidate matches all four retained ordinary path+branch states, refuses the retained post-resolution binding change before emitting a pointer target, literal mismatches all changed-resolution content paths while chrome remains fixed, negative controls fail closed, audit/integrity pass, invocation=1/reruns=0.

C: Success may simply reflect explicit frame tags already present in the source procedure; it does not prove automatic frame discovery or general compilation.

U: One retained OpenTTD seed/path, replay only. No new task-effect, token, latency, or production claim.
