# Issue #4242 — Allocation 02 prospective formal plan

Allocation 01 remains immutable `STOP_FIXTURE_PIXEL_CAPTURE_BADMATCH_BEFORE_BRIDGE` with 0/12 rows. Allocation 02 changes only the fixture construction exposed by that STOP: it waits until the client is actually X11 `IsViewable` before client-pixel sampling and selects a preregistered non-flat stripe-boundary mint point. The scientific factor and decision gates are unchanged.

## H

An exact bound-client DestroyNotify can advance one caller-owned generation through the existing NativeHandleBridge `review_window` / binding-revision / scope-revocation boundary. The predecessor alias is then unusable before native input while an explicitly fresh replacement alias remains usable.

## T

Execute exact repository NativeHandleBridge and direct dependencies. Four fresh private Xvfb/Openbox sessions, each containing three counterordered cases: `STABLE_FRESH`, `REPLACED_STATIC_DIAGNOSTIC`, `REPLACED_DESTROY_GENERATION`. Total 12 formal rows. Same `construction.py` fixture source as final no-input construction. Static replacement is diagnostic only and emits no task input. Candidate replacement performs old-alias refusal test and only then one fresh click. Exactly one formal invocation; retries/replacements/tuning 0.

Final excluded construction at head `a94632a2dd4a40042f4914de4cf498c1f1b89407` passed exact bridge observe/mint, same-client same-XID/same-pixel recreation, exact DestroyNotify, review/rebind, old alias MISSING, fresh mint, emissions0 and clean Xvfb/Openbox. Retained artifact digest `sha256:cbf2b27b672fa0a905343ea97fd0c91aa76eafa3d41d82c8b4803bf0d48fffc8`.

## D

`PASS_NATIVE_DESTROY_GENERATION_COMPOSITION_SCOPED` only if all 12 rows are complete/auditable; stable fresh 4/4 complete with effect1 and emission3; static replacement 4/4 proves same XID/pixels + old diagnostic eligible but emits/effects0; candidate replacement 4/4 observes exact DestroyNotify, review advances revision exactly once and changes scope, old alias refuses with zero emission/effect, fresh alias completes with effect1 and total emission3; all terminal release/cleanup/source/audit/corruption gates pass.

Old-alias admission/emission after generation => `FAIL_GENERATION_ESCAPE`. Fresh valid path regression => `FAIL_GENERATION_OVERINVALIDATION`. Missing discriminator with otherwise valid evidence => HOLD. Missing raw/source/release/cleanup => typed HOLD/STOP.

## C

Explicit `review_window` is still caller-driven. No automatic lifecycle watcher/public CLI plumbing, check/use atomicity, arbitrary toolkit/backend, model, token/latency or product claim.

## U

A scoped PASS would close only this current bridge composition rung. Automatic production observer plumbing and #2789/#3311 matched six-task evaluation remain open.
