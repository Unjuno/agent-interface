# Issue #2466 informative-censoring allocation-02

Preserve allocation-01 as STOP_EXTERNAL_EXECUTION_TIMEOUT/HOLD_INCOMPLETE_DENOMINATOR. Its 25-case biased prefix contributes zero rows here.

## H
Unchanged scientific hypothesis: typed censor cause plus re-observation should distinguish safe delayed completion from focus loss, owner death, unknown M and observed M violation, avoiding unsafe progression exposed by a generic censor-bucket comparator.

## T
Scientific matrix unchanged: 2 families x6 scenarios x2 policies x2 reps=48. Two execution-only/harness deltas: (1) dwell clock origin begins after Tk is ready and immediately before scheduling effect/censor events; (2) exact global case order is split into four immutable contiguous 12-case batches, each with its own private TCP-disabled Xvfb and terminal END/exit receipts. No allocation-01 rows pooled. Timing is frozen per family after excluded startup diagnostics: TK_CALLBACK horizon100ms/M250ms; WORKER_FILE horizon550ms/M850ms. UNKNOWN_BOUND has no M. The worker schedule includes measured fresh-Python startup (5 diagnostics: 447.3–517.7ms, median453.7ms) as part of that family’s observed dwell path. One invocation per batch, strict order, no retry/replacement/tuning. Aggregate only after all four complete.

## D
Unchanged: candidate proceeds every NORMAL/HORIZON_LATE cell, holds every FOCUS_LOSS/OWNER_DEATH/BOUND_VIOLATION/UNKNOWN_BOUND cell, zero unsafe proceeds; generic proceeds all48 and has exactly16 unsafe proceeds. All48 ordered rows, four batch END/outer exits, Xvfb exits, source hashes, raw event brackets and independent raw audit required. Six corruption/unit controls reject. Missing batch/evidence => STOP/HOLD.

## C
Directed private fixture; generic arm is deliberately weak; M is diagnostic. Post-ready timing fixes a harness origin error and does not imply a production clock contract. Batch execution changes only envelope/provenance, not cases.

## U
No model/tokens, empirical M calibration, third held-out surface, OS input authority, arbitrary toolkit, production reliability or human-tempo claim. Broad #2279/#2466 and ROADMAP remain open.
