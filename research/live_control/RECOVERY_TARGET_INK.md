# Same-focus target change refuses the post-recovery edit

A fresh Inkscape seed239 session recovers the intermediate X88/save result after actual deliberate response abandonment. The constrained responder then proposes a new edit toward final X104. Before admission, the fixture sends two Escape keys through the journal/runtime to clear selection while remaining in Inkscape, then obtains a fresh observation. This fault program is explicitly scripted and logged; it is not the stale model proposal.

The source and fresh pointer binding (focus, surface, geometry) remain exactly equal. The fresh screenshot visibly shows no selected object and a disabled X field. The unchanged sampled_target_contract_v1 checks the declared X-field rectangle508,90–606,123 and the model's proposed first click point, with a1000ms fresh-sample bound using the runtime clock. It refuses target_patch_changed. No X104 program is submitted. Independent SVG remains88/50/40/30; final_goal_achieved is false. This is expected-refusal success, not final task completion.

The audit replays75 events,13 exact transported frame/PNG comparisons,19 journal records, original request attribution and all source/model/image hashes. It verifies the exact recovery view supplied to the model, model proposal correspondence, same binding, exact fault steps, observation-only validation, unchanged saved SVG and a replay of the shared patch verdict. Four programs are admitted: selection, intermediate edit, deselection fault, fresh observation. Save occurs once, no old request is replayed, all terminals release input, driver exits0 and private sockets are removed. Evidence is results/recovery-target-ink-01; run audit_recovery_target_ink_v1.py under Linux without new model or GUI calls.

The one model invocation uses10545 input tokens (6912 cached),236 output and9.293s runner time. Original-result recovery takes0.941s. No matched speed comparison or general latency improvement is claimed. The assistant inspected the fresh image with disabled coordinate fields. Image filenames come from observation records and need not equal the sequential transport frame index.

Limitations: the field box is harness-declared, and this checks only the first click target, not all subsequent keyboard/save semantics. Exact pixel matching may refuse benign rendering changes, as already observed in INKSCAPE_GUARDED_CLICK.md. This case establishes one true change detection, not a false-positive rate, semantic identity proof or atomic capture-to-input guarantee. No target change after the final validation is covered. Source plan text retains the inherited continuation description; target-fault/verdict artifacts define this injected refusal case. The driver stops after refusal and does not yet autonomously select the rectangle again.

Decision: combine focus/binding and sampled target evidence in this research path; focus alone misses the demonstrated disabled-field change. Next feed the actual refusal and latest deselected screenshot back to the model, allow a new selection/observation before a new edit, and independently score final geometry. Preserve the old rejected proposal and never replay it automatically. Avoid more standalone refusal-only cases as a substitute for a working replan loop. Full live usability, human-tempo and broad domain coverage remain open.
