# Multi-cursor resource condition

PASS scoped.

See PLAN.md for the hypothesis and RESULT.json for the exhaustive result. The checked model found no counterexample when all logical cursors share one exclusive pointer resource. Independent resources can overlap; shared resources cannot.
