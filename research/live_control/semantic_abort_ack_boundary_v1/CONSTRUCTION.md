# Excluded construction and retained incidents

No construction is in the 18-case formal denominator. Distinct output directories were used; none was rerun or overwritten. Source snapshots preserve each changed construction revision.

| Attempt | Actual first outcome | Change made before a later attempt |
|---|---|---|
| construction01 | STOP before task input: inherited `/opt/xvfb/.Xauthority` did not exist. App/server termination retained. | Create a private authentication file for the owned server, never reuse inherited display authority. |
| construction02 | STOP before task input: this Xlib client did not match a FamilyWild-only authentication entry. The original partial-raw audit also raised while trying full-case mutation controls on missing rows. | Add the exact local hostname/display authentication record after Xvfb allocates a display; audit incomplete evidence as STOP without running full-case mutation controls. |
| construction03 | Normal completion case passed. Applied-cancel case observed zero callbacks and verified released input, then STOP at image recording (`TypeError: a bytes-like object is required, not 'str'`). App exit -15 is retained, not relabeled clean. | Installed Xlib String8 decodes valid UTF-8 image buffers (including all-zero black pixels) to `str`. Invert that exact decoding with UTF-8 encoding, retaining actual parser type, otherwise preserve bytes. |
| construction04 | Six cases completed; separate audit agreed and 10/10 mutation controls rejected. | Strengthen raw RPC/event/terminal binding and response-before-decision ordering; add four controls. The unsafe comparator now explicitly consumes the received receipt rather than only a scenario flag. |
| construction05 | Final frozen source: six cases completed; separate raw audit errors empty; 14/14 mutations rejected. | No scientific source changes after this construction and public freeze. |

The first metadata-only freeze attempt stopped at `PackageNotFoundError` because the installed Xlib did not expose distribution metadata. No environment/freeze file or formal output had been written. `freeze_environment.py` records distribution metadata as unavailable, keeps the module version and hashes all installed Xlib Python files; the later metadata capture completed. This is not an experimental retry.

The original construction02 audit traceback was visible in the execution-tool response but its stderr was not redirected to a file. Its empty stdout is retained; this document records that missing standalone stderr instead of inventing it. Subsequent audit stdout, stderr and exit files are retained. Original early tool exit statuses are not retroactively reconstructed as raw process receipts.

In construction03, final physical key/button queries remained neutral; the application was terminated after evidence recording failed. Construction03 is STOP despite the intermediate expected counter values. Formal source hashes and the no-rerun rule were published only after construction05 and the environment freeze.
