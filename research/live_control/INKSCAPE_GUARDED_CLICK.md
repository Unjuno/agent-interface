# Shared target contract used in actual Inkscape editing

The assistant ran a fresh Inkscape seed231 fixture through the existing cause-servo/executor8 private socket, selected the red rectangle, and declared the X-coordinate field contract at509,90–606,123 with point550,106 and1000ms freshness. The shared sampled_target_contract_v1 was used unchanged. This is actual assistant self-use, not another archive-only replay.

First guarded attempt refused target_patch_changed. The source/new patch differed in228 pixels within relative bounds8,12–51,21, while both screenshots visually showed X50.000. The exact rendering cause is unknown. The assistant viewed the new frame and deliberately adopted it as a new source; matching was not weakened or retried automatically. This exposes sensitivity of strict pixel equality to apparently unchanged field rendering.

The next v1 helper attempt failed before admission: its saved image reply cursor preceded a later clock response, and it selected that old clock on the next read. The resulting observe request was rejected as intent validity expired; no input or observation program was admitted for that attempt. The runtime stayed live and the same session was retained. All failed helper plans, calls and rejection records remain stored.

guarded_click_client_v2 separates source-image reply from continuation cursor reply, saves the latest actual reply after every exchange, and validates clock responses against its own transport request ID using existing own_command. It fails rather than selecting an uncorrelated clock. This version was used with the last received rejection cursor and the visually reviewed source frame4. Fresh frame5 matched; one guarded click entered the X field, completing from new sequence/deadline. Frame7 showed the field focus. The assistant then used a separately admitted keyboard program to set X92 and save. The patch contract covers only the focus click, not arbitrary text/save semantics.

The final screenshot12 showed X92,Y50,W40,H30 and Document saved. A GUI judgment was placed in the finish command before independent evaluation. The saved SVG independently contains rectangle x92,y50,width40,height30 with no transform. Audit verifies79 runtime events,12 exact transported frames, five completed/released programs, one rejected program, no admission for the failed helper, fresh click deadline, source/clock identity and SVG hash. Bridge exited0. The independent live entry owns cleanup; host exit was observed. Results: results/inkscape-guarded-click-01; audit: audit_inkscape_guarded_click_v1.py.

Fresh capture to guarded button acknowledgment was235.240ms. Initial capture to final input terminal was193.518 seconds, including manual diagnosis and helper repair; no human-tempo, model-token or causal speed claim follows. There was no extra CLI model invocation in this episode. Prior frozen helper v1 and source contract remain unchanged.

This connects the shared contract to a desktop effect, but the caller is still research-only. Refusal/reconciliation has no automatic recovery loop, matching remains sensitive to rendering, and a positive patch match is not semantic readiness or atomic capture/input proof. Callers must distinguish historical image evidence from latest event cursor and must correlate command echoes. Next make these explicit in a reusable result/continuation contract and validate the stale-clock failure without another full manual editing episode. Do not promote v2 merely because this recovery eventually saved the document.
