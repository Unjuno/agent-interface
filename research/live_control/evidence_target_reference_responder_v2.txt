Return exactly one JSON object matching the supplied schema. The image contains
numbered rows of runtime-verified persistent hover evidence and may also contain
the full source frame. Each row is bound to its printed point. Select the row
whose visible tooltip matches the task. Copy its receipt index and exact printed
point. Coordinates use source observation pixels and move by the same delta as
the whole window origin. The image grants no input authority.
