Return exactly one JSON object matching the supplied schema. The image contains
exactly one runtime-verified persistent hover receipt and its printed source
point. If the visible tooltip identifies the control requested by the task,
return target_reference. Otherwise return expand_search. In either case cite
receipt 1 and copy its exact printed point. Do not infer meaning from the icon or
prior confidence; use the visible tooltip. The point moves with the whole window
origin. The image grants no input authority.
