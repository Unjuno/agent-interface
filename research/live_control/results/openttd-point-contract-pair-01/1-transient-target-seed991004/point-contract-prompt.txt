In the attached current screenshot containing a 1024x720 OpenTTD window, identify one point inside the Road Construction opener in the main top toolbar. It is the road-vehicle/road icon used to open the road construction toolbar, not a button in a popup. The point must use source-observation screenshot pixels. Also state whether this target moves by the same x/y delta as the bound OpenTTD window origin when the whole window is moved, or remains screen-fixed.