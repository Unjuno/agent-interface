{
  "limitations": [
    "v1 fallback can label missing admission evidence as rejected; v2 adds missing-evidence controls",
    "v1 source manifest keyed two different turns.json files by basename; v2 uses unique repository-relative paths"
  ],
  "retained": true
}
