# #1099 preformal setup stop A2

Status: `PREFORMAL_SETUP_STOP_A2`; scientific disposition **NONE**.

The granted excluded construction budget (one matched pair / two fresh private-X11 sessions) was consumed using the exact V2 frozen source. Exact source gates passed for the v10 Git blob, v12 bundle/source, adapter, static runner and frozen auditor. Both construction arms then stopped before owner/XTEST F8 input because python-xlib attempted to read `~/.Xauthority` at `/opt/xvfb/.Xauthority`, which is absent in this disposable container even though Xvfb was started with `-ac`.

No KeyPress/KeyRelease program was issued, no physical-edge classification was observed, and no row is eligible for the scientific denominator. Fixture and Xvfb processes terminated; both private X11 sockets were absent after cleanup. Formal invocation remains 0 and reruns remain 0.

The grant capped construction at two fresh sessions, so this allocation is not repaired/retried. The only legal continuation is a separately identified harness-only successor changing private-display Xauthority plumbing only, with H/T/D/C/U, F8, 150 ms hold, arms, thresholds, scorer semantics and lineage unchanged, and with a fresh explicit finite live lease.
