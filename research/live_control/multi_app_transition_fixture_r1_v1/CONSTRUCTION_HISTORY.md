# Excluded construction history

No item below consumed the formal allocation.

1. Early scratch: Python-Xlib could not open private Xvfb because process `XAUTHORITY` was not set. Setup stop before scientific row.
2. Scratch replacement: retiring old Chromium before launching replacement allowed X resource-ID reuse; replacement identity was unobservable. Fixture-identifiability stop.
3. LibreOffice candidate second app: no top-level Calc window appeared within bounded startup checks, including a unique UserInstallation profile. Setup stop; not used in final fixture.
4. Inkscape `--new` candidate second app: no top-level window appeared within bounded startup check. Setup stop; not used in final fixture.
5. Chromium geometry request was ignored while maximized. Setup repair: explicitly remove maximized WM state before geometry change.
6. Local-file JavaScript alert was not a reliable modal discriminator in this Chromium setup; top-level/root hashes did not show a transition. Replaced pre-freeze with the built-in Clear browsing data dialog (`Ctrl+Shift+Delete`).
7. Final clean construction source: Chromium + XTerm; geometry, cross-app focus, distinct replacement, built-in modal framebuffer change, and input-neutral gates all PASS in one excluded session.

These are fixture-development changes before source freeze. They do not support a task/controller result.
