# Construction

Formal cases executed: 0.

The obligation-keying counterexample is analytically fixed by `A(o1), B(o2)`. Construction is limited to implementation validation: 4/4 policy units must pass; no live formal child is run before GitHub source readback and freeze.
