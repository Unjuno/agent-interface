# Refusal feeds a bounded model replan loop and final saved-task success

Fresh Inkscape seed239 retains the prior reply-abandonment/recovery and same-focus deselection fault. The first model X104 proposal is refused by the existing X-field patch contract. Instead of stopping, the driver gives that refusal, the rejected proposal and latest deselected image back to the same constrained responder. It permits at most five further model decisions; no rejected program is automatically resent.

Actual decisions: (1) old X104 edit refused after injected deselection; (2) new click664,391 selects the red rectangle, patch passes and the runtime observes; (3) X104 edit is proposed but refused because the new25x25 first-click patch differs; (4) the model reviews the newer image and proposes X104 again, this time patch passes and a new action ID is admitted; (5) visual verification requests independent scoring. Final SVG is104/50/40/30 without transform. Both edit proposals in steps3/4 happen to contain the same GUI steps; only step4 is submitted after a new model decision and fresh matching sample. This is not an automatic transport retry.

The additional refusal compares source016.png/fresh017.png: both visibly show X88 with the object selected, but all625 pixels in the25x25 patch differ. Background appearance changes; the rendering cause is not established. Exact matching was not relaxed and the old source was not silently replaced to pass. The new refusal/current sample was explicitly delivered to the model. This exposes a real extra model-turn cost while preserving a complete task outcome.

Five model calls use52456 input tokens (27392 cached),889 output; runner durations9.393/9.837/9.009/6.886/6.504s total41.629s. Initial capture to independent evaluation is47.110s; original lost-result read resolves in0.832s. These are one fault/replan episode's measurements, not a matched latency improvement or human-tempo result. Model wait remains dominant.

Audit replays147 events,24 exact transported frame/PNG comparisons and41 append records. It checks original lost request attribution, all prompted evidence and model messages, each fresh patch verdict, rejected-versus-submitted proposals, matching deadline/sequence for admitted new input, released terminals and independent SVG. Original saved edit occurs once; final edit saves once. Driver exits0, sockets removed and no error artifact. Raw evidence is results/recovery-replan-ink-01; audit_recovery_replan_ink_v1.py runs under Linux without more model or GUI calls. The assistant viewed the two extra-refusal screenshots. The diagnostic records exact changed-pixel count and overall time.

Limits: initial edit and deselection are scripted; post-refusal selection/edit/verification are model-controlled. The harness derives a25x25 patch around the first proposed click for replans, which does not validate all later keyboard actions, semantic identity or atomic capture-to-input. Each act must start with a pointer click. The original numeric action validator is reused after JSON decoding, not a full duplicate-key validation boundary. The bounded loop stops on unsupported proposals, pending input, or exhaustion; no general planning/cleanup guarantees follow. Inherited source-plan language does not fully describe the expanded replan loop; this report and actual recorded sequence define its scope.

Decision: refusal now leads to an actual new observation/decision/action path and final task success, rather than a terminal demonstration. The extra visual refusal is the next performance issue to investigate. Before weakening matching, characterize the returned post-selection frame and subsequent rendering with bounded passive observations, retaining the disabled-field true-change control. Test whether better observation readiness can avoid spending another model call on transient presentation changes. Do not claim semantic-equivalent images are safe merely because text appears unchanged. General domain coverage and human-like live control remain open.
