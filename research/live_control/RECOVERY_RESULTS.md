# Registered recovery study: explicit batching retained, speed unqualified

The registered eight episodes have finished. Stop this allocation; do not add
clean reruns. Retain the helper as explicit read-only batching convenience, with
no input, automatic retry, latency/token promotion, or Research Freeze claim.

| Pair | Domain / depth | Calls A / B | Reads A / B | Full seconds A / B | B minus A seconds |
| --- | --- | ---: | ---: | ---: | ---: |
| 1 | Inkscape / 1 | 1 / 1 | 1 / 1 | 51.799 / 104.123 | +52.323 |
| 2 | Inkscape / 3 | 4 / 1 | 4 / 4 | 92.787 / 59.039 | −33.748 |
| 3 | OpenTTD / 1 | 1 / 1 | 1 / 1 | 64.668 / 317.617 | +252.949 |
| 4 | OpenTTD / 3 | 4 / 1 | 4 / 4 | 112.397 / 65.583 | −46.814 |

Across four pairs, B-minus-A structural recovery-call deltas are [0, −3, 0, −3],
median −1.5 and range −3 to 0. Read deltas are all zero. Full elapsed deltas have
median +9.287697 seconds and range −46.814242 to +252.948865 seconds. Do not turn
this mixed-domain median into a per-task prediction or a population effect.

The numerical call threshold is met for the recorded recovery stages. However,
the registered primary endpoint includes history/image delivery: pair 4 A's final
image appeared black, and an additional image-only inspection also appeared black.
The decision used a previously visible identical source image. Report four recovery
calls plus one additional inspection, not a proven four-call successful final
image delivery. Full primary-endpoint qualification therefore remains false.

All final task contracts passed. Pair 1 B required a new reviewed task program
after a focus interruption; the initial needs-decision terminal is retained.
Pair 3 B's build output was truncated and recovered by saved-result inspection.
Pair 4 A's source PNG is nonblack and exactly matches its encoded frame, but the
presentation anomaly is unresolved. No failures or their elapsed time are removed.
Preparation artificially creates stale histories; this does not establish their
frequency in ordinary use. B's cooperative two-second budget and A's inter-call
model review are different timeout conditions.

Within each pair, the aggregator verifies initial PNG equality, explicit initial
task-step equality, runtime source equality, pinned source hashes, plan/protocol
hashes, execution labels, and recovery reads against raw event positions. It
derives elapsed time from the first capture and final independent evaluation.
Each individual audit separately verifies full event coverage, exact frame pixels,
and independent task scoring. The aggregate hashes all archived arm files so later
changes are detectable; it does not replace those individual audits.

Exact model identity/configuration, receipt timestamps, actual model tokens and
cost remain missing. Saved byte counts are not tokens. Neither observed task
correctness nor structural call reduction proves human-speed control or latency
benefit. This study does not qualify the general architecture.

Reproduce: `python3 research/live_control/aggregate_recovery_v1.py` in the Linux
research environment. Output: `results/recovery-aggregate-01/report.json`.
The originally registered protocol and plan remain unchanged as historical records.

## Next bounded improvement

Implement an opt-in offline, resumable presentation reader for saved reports:
bounded page payloads, stable evidence digest and cursor, explicit incomplete
coverage, and an exact reconstruction check. A missing/unseen page must not become
implicit task completion or input approval. Keep full originals and all unique
events; do not silently summarize away errors. Exercise real pair 3's overflowing
report and adversarial page boundaries before a fresh self-use allocation.

Image delivery needs separate evidence: record source identity, requested detail,
and whether the model could inspect it; runtime capture success is insufficient.
Do not claim an offline text reader fixes the black-image path. Establish an
authoritative receipt/token measurement path before any new speed/cost claim.
