# #1340 P0 live A6

Direct live predecessor #1325 stopped preformal at exact #988 `occupancy_only()` duration expansion. #1328 formally proves the interval-union helper equivalent at the occupancy semantics boundary, and #1339 dry-runs the exact live `temporal.analyze()` shape at 150 ms raw-ns with large headroom.

Scientific change: only the temporal occupancy implementation. The wrapper delegates to exact #1325 science runner, exact case child, exact InputOwner-v12/adapter, exact clock gate, fixture, scorer, hold and release semantics. The temporal candidate loads exact #988 parent classification and replaces only `occupancy_only` with the exact #1328 helper.

No live execution before #60 grant. First budget is one matched construction pair /2 sessions. Formal6 pairs/12 sessions only after complete construction and mandatory reread. Reruns/replacements/tuning0.
