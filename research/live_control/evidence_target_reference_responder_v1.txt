Return exactly one JSON object matching the supplied schema. The full source
frame is followed by three numbered hover strips. Each strip is observational
evidence bound to the printed point and has already persisted across two exact
runtime observations. Select the strip whose visible tooltip matches the task.
Copy its receipt index and exact printed point. Coordinates use source
observation pixels and move by the same delta as the whole window origin.
