# Passive samples locate a post-selection image transition

A fresh Inkscape seed239 fixture reproduces intermediate X88/save recovery, deselection and selection click664,391 using the same shared runtime/journal. No model is called. After the selection result it schedules observation-only requests at nominal offsets0,0.1,0.25,0.5,1,2,4,8 seconds. Each request has its own clock and admission, so these are not exact capture times or fixed-frequency sampling. Actual driver elapsed and runtime capture timestamps are retained in samples.json.

The returned selection image's25x25 X-field patch differs from every later sample. The first additional sample returns at0.289s after selection-call completion; subsequent returns at0.497,0.729,0.966,1.237,2.226,4.215,8.233s all have the same patch hash1a9d6ec6.... Pointer focus/surface/geometry binding stays identical. The selection-result center pixel is65/66/66; subsequent samples26/26/26. This records changing pixels without assigning an unproven rendering mechanism.

The stable later hash exactly matches the fresh image that passed after the extra refusal in recovery-replan-ink-01. The early hash does not exactly match that earlier episode's source hash; do not claim identical transition trajectories. The evidence supports a post-selection rendering/readiness issue as an explanation for extra patch refusals, but does not prove0.289s is a sufficient universal delay or that two matching samples establish semantic readiness. Sampling may miss changes between frames or after the last frame.

Audit replays142 events,22 exact transported frame/PNG comparisons and49 journal records. It verifies all nine recorded patch hashes/pixels against files, source/runtime hashes, original request attribution, observation-only post-selection sampling, unchanged binding, twelve released programs and exact saved SVG88/50/40/30. Only the original intermediate edit saves; no post-selection editing occurs. Driver exits0, sockets removed, no error artifact. Results are results/selection-readiness-ink-01; audit_selection_readiness_ink_v1.py replays the evidence under Linux without model or GUI calls. Nominal offset labels are scheduling targets, not measured elapsed values. Source plan retains unused model-file hashes inherited from the fixture; there were zero model invocations.

Decision: test a bounded passive readiness step before delivering post-selection evidence to the model, rather than weakening pixel matching. Require a new matching sample pair for the declared target region and unchanged binding; retain a fixed capture/time bound and report unsettled on exhaustion. This would be sampled visual stability only, not task completion or permission to bypass fresh input admission. Keep the actual disabled-field true-change case as a regression control. The next live comparison must establish whether added subsecond observations avoid the extra seconds/tokens of another model decision; this trace alone establishes no end-to-end speedup. Human-tempo and broader coverage remain open.
