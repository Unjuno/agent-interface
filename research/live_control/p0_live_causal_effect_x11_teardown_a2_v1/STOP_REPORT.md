# #1301 preformal setup stop

Disposition: `PREFORMAL_SETUP_STOP_EFFECT_CHILD_UNDIAGNOSED`; scientific disposition **NONE**.

- Construction budget2 was consumed; formal0/reruns0.
- Exact science source and dependency gates passed.
- NO_ACTION_CONTROL child persisted a complete correct row: pre/post red centroid59.5, no app/effect event, terminal F8 UP, exceptions0.
- That successful child demonstrates the external-Xvfb/client-exit teardown ordering removed the parent #1287 control-side XIO failure.
- V12_EFFECT child created its case directory/Xauthority but persisted no row.
- The supervisor did not persist child exit/stdout/stderr before aggregate; then inherited `summarize()` raised on the minimal failed row, so exact action-child cause is UNKNOWN.
- No effect/physical result is reconstructed from missing memory state.
- After stop, related processes and display sockets were0.

Fresh successor may change only failure observability/aggregation: persist per-child status and stderr before summary and make summary failure-tolerant. Do not change the exact science runner or effect/scoring contract.
