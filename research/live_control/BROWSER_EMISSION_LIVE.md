# Local emission measurement during browser confirmation recovery

An explicit browser_emission_live_v1 client preserves the existing combined response and calls presentation_emission_v1 on its binary stdout. Its initial plan pins candidate source hashes. emission_confirmation_entry_v1 reuses the existing confirmation fixture policy with cause interactive v4 / executor v8; emission_confirmation_socket_v1 selects that entry. Frozen sources and defaults are untouched.

The assistant viewed about:blank, navigated to the supplied local URL, viewed the prefilled old-draft-42 form, replaced the field with t000227 and pressed Return. The five-step input program completed, while screenshot 012 explicitly said Nothing has been saved and required replacement confirmation. The assistant recorded this negative interpretation before clicking the visible checkbox and Save. Screenshot 016 showed Submission received. A GUI prediction hash was sent in a clock command before requesting independent evaluation, which confirmed value=[t000227]. Runtime/bridge exit 0 was observed. No direct POST or DOM manipulation was used for agent actions.

This is a known confirmation fixture reused with a new seed and new emission client, not a novel or blinded task. All three programs completed without focus interruption; the extra confirmation program is semantic task recovery, not runtime error recovery. Full receipts retain unknown attention fields and task-success uncertainty. Optional bounded followup was not needed. Submission-attempt server logs were not copied out before fixture cleanup; the intermediate rejection is evidenced by the screenshot and recorded GUI interpretation, not a retained independent intermediate-state oracle.

Audit verifies all 108 events in nine socket exchanges, command echoes, regenerated v5 receipts and state tables, source hashes, 17 exact PNG/AIT frames, and final saved POST data. Five emission payloads decode to the exact saved result JSON, hash and acknowledged byte lengths match, and local clock fields are ordered. Tool-layer byte preservation is not independently measured by this live audit; the separate emission probe covers an actual subprocess pipe.

Output sizes for initial/navigate/replace/confirm/finish are 3420/11413/10945/9578/532 bytes. Local serialization is 0.072–0.292 ms and write plus flush is 0.039–0.151 ms. Outer preparation for the three programs is 1303/1270/861 ms; it excludes output rendering. Initial capture to pre-score GUI commitment is 101.283 seconds and includes the assistant/tool workflow. No cross-process clock alignment proof, authoritative model receipt, model input tokens, cost, matched-model speed comparison or human baseline is supplied. Do not interpret these timings as planner latency or a performance gain.

The first launch failed argument parsing because serve required a -- separator; it never started a runtime. Corrected launch produced the single measured session. The failure note remains with results/browser-emission-live-01.

Decision: keep local emission as an explicit measurement candidate. It succeeds on a case where program completion and semantic completion differ. This trial does not justify another output encoding or default promotion. Next prioritize exposing authoritative downstream model timing/usage or a controlled comparison harness; otherwise report these endpoints as missing rather than collecting more similar wall-clock runs.
