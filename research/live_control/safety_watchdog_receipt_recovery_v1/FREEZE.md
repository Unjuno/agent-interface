# Safety watchdog receipt recovery v1 — source-first freeze

Task `SAFETY-WATCHDOG-RECEIPT-RECOVERY-20260917-001`, Issue #827.

Immutable publication BASE: `40072ec1962afe9fe1a9f6ade79ce36a2e8cc93f`.

Formal rows before this freeze: **0/8**. Formal rerun budget: **0**.

Single scientific factor: post-verified-release watchdog cleanup receipt retention (`pipe_only` versus `watchdog_local_journal`). Physical owner-crash/watchdog cleanup path and blocked ordinary pipe are fixed.

The exact scientific source identities are in `source_hashes.json`. Excluded construction used fresh IDs and is not pooled.
