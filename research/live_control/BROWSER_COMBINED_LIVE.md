# Browser self-use with lifecycle receipt and conditional followup

browser_combined_live_v1 is an explicit copy of the combined Calc client using receipt v5 and a Chromium plan. cause_servo_socket_v5 adds Chromium to the explicit entry map; underlying interactive v4/executor v8 remain unchanged. This is a different desktop task: navigate from about:blank to the provided local form, visually identify its input, type t000226 and submit.

The assistant viewed initial browser, loaded form and submission receipt screenshots before the relevant next steps. Navigation used Ctrl+L, URL text, Return and bounded settle; form submission used a screenshot-selected pointer click, text, Return and bounded settle. Both four-step programs completed without interruption. Optional bounded followup was skipped in both cases: exactly clock+submit exchanges and no bounded-request artifact. New receipt v5 verified both actual command/terminal bindings while retaining unknown-field/state attention.

GUI-only prediction (Submission received and AI FORM SAVED shown, stored token still uninspected) was hashed into a clock command before independent scoring. The fixture's saved POST body parses to value=[t000226], success true. No extra navigation, retry, DOM manipulation or direct server submission was used by the agent. Runtime/bridge exited 0.

Separate audit verifies source hashes, all 84 events across seven socket exchanges, exact command echoes, regenerated v5 receipts/state tables, verified owner releases, skipped followup, pre-score GUI commitment, thirteen exact PNG/AIT frames and saved POST data. Outer preparation intervals were 1225.162ms for navigation and 1039.144ms for submission; they exclude output rendering and model receipt. Initial capture to GUI commitment was 76.655s. No matched model or causal speed/token claim follows.

## Coverage and Issue alignment

The active Issue list was re-read, including #46 (end-to-end timing envelope) and #48 (readiness). This trial confirms ordinary direct completion in another app, complementing prior Calc modal recovery; it does not reproduce a browser modal interruption or validate a readiness classifier. The agent inferred form availability from the actual screenshot; quiet pixels or a ready title were not promoted into a general action-ready contract.

Issue #46 remains open: authoritative model-receipt/generation timestamps and comparable model identity are missing. Do not label local exchange timings as planner latency. Issue #48 remains open: no cross-domain loading/startup/black-screen readiness experiment was performed. No comments or issue state changes were sent.

Next reduce duplicate lifecycle/observation payloads while retaining record references and all unknown/negative evidence, and instrument the resulting model-facing boundaries before any cost claim. The raw reports and full receipts remain authoritative. Avoid proliferating identical successful browser/Calc timing episodes; use a changed transition or failure condition for subsequent live validation. No default runtime/client is promoted.
