{
  "schema":"authority-ended-durable-token-state-prereg-v2",
  "question":"can restart preserve both replay safety and liveness for an issued-but-unconsumed replan token without allowing more than one durable consume?",
  "candidate":"fsync/atomic durable state mapping runtime authority_end_id -> {post_sequence,status=pending|consumed}; issue persists pending before token handoff; explicit recover_pending reconstructs the same logical token; ledger.consume persists pending->consumed and is the single execution gate",
  "cases":[
    "issue pending then new process recovers same id/post_sequence",
    "duplicate terminal issue while pending rejects",
    "two recovered token objects cannot both consume: first persists consumed, second rejects",
    "new process after consume cannot recover or issue same id",
    "crash after pending replace/fsync but before issue return: restart recovers pending token",
    "crash after consumed replace/fsync but before consume return: restart treats token consumed (safe, liveness loss explicit)",
    "distinct runtime authority id issues and consumes normally",
    "corrupted state fails closed"
  ],
  "benchmark":"20 distinct issue+consume cycles; report issue and consume persistence wall times including fsync/replace/dir-fsync",
  "decision":"RETAIN_DURABLE_TOKEN_STATE_CANDIDATE only if every frozen case passes",
  "no_live_input":true,
  "no_model_calls":true,
  "limits":["single-writer state file only","crash after consume persistence but before OS submit remains fail-safe/liveness gap","no adversarial state-file authentication claim"]
}
