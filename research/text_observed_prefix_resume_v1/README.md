# text observed-prefix resume v1

Isolated Research Preview experiment for partial text recovery. It compares blind retry, sender-count suffix retry, and independently observed-prefix recovery after controlled mid-flight divergence. It does not modify shared runtime semantics.
