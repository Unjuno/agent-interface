# Construction runner/audit pilot 06 — STOP/HOLD

The corrected container entrypoint invocation reached all four Xvfb cases. All three German cases stopped at the layout evidence handler with `NameError: map_events` (a stale variable name after the logging refactor); the US control completed. No formal allocation ran. The independent auditor then raised `AttributeError` on a STOP row whose `unsupported` field was null. Both are construction harness defects, not candidate or XKB results. Partial raw JSON and xev logs are retained unchanged.
