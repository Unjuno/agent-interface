# Construction pilot 07 — candidate PASS / audit HOLD

All four runner rows passed in the pinned container: InputOnly class and focus, exact receiver-only control, three German layout/core-map/server-dump transitions, zero-event Euro refusal, exact formula KeyPress text, full planned event counts, and empty held-key state. The independent auditor held the bundle because the runner populated `xev_events_before_candidate` only on German rows and omitted it on the US control. Candidate/raw evidence and the audit output are retained unchanged; this is not formal evidence.
