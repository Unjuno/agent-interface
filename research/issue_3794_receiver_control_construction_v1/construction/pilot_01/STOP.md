# Construction pilot 01 — STOP

Private four-row construction invocation stopped while reading the Python-Xlib `GetWindowAttributes` reply: the harness assumed the field was named `class`; Python-Xlib exposes it as `win_class`. No German layout change or candidate text delivery was attempted. The partial `formal_raw.json` and receiver logs are retained. This is a harness defect, not a candidate result and not formal allocation evidence.
