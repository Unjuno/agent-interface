# Issue #3794 — corrected XLookupString receiver audit, formal-01

## H / T / D / C / U

**H — hypothesis.** The #3789 STOP was caused by a false receiver-control predicate requiring empty `XLookupString` bytes on KeyRelease. Release events may legitimately return the associated keysym bytes; count text only on KeyPress and verify release by matching keycode/state. A mapped private InputOnly window, independently verified focus, and a receiver-only XTEST key control will receive the exact `=B2*A2` candidate plan on three German XKB servers and one US control. Unsupported trailing Euro must be refused before any XTEST event.

**T — frozen allocation.** Allocation `issue-3794-german-xkb-receiver-release-audit-formal-01`, additive path `research/issue_3784_focused_receiver_v1/`, branch `research/issue-3784-focused-x11-receiver-v1`, base `d685f881cb5050bc42b6b96007978df9f532fe78`. Candidate/runtime closure is byte-frozen in `source_manifest.json`. Run four fresh private Xvfb `-noreset` servers. Begin each with verified US query, server dump and client core mapping; create/map a 1×1 InputOnly receiver, independently assert `GetInputFocus` returns its XID, and attach `xev -id` to that XID. Send one `a` KeyPress/KeyRelease diagnostic and require exact keycode/state pair plus KeyPress text `a` before proceeding; release XLookupString payload is recorded but not counted as character input. On three rows, execute exactly one `setxkbmap -layout de`, synchronize MappingNotify, query active server map, and capture changed server dump and client core mapping. On all rows, preflight `=B2*A2€` and require refusal, zero emitted keys, and unchanged receiver log; preflight supported formula without emission, then call current-main candidate text once. Retain baseline/after server dumps, complete receiver log, parsed key/state traces, source hashes, process return codes and cleanup evidence. A separate container performs read-only audit and three isolated auditor-corruption controls. No retry or source/gate edits after formal start.

**D — decision gates.** `PASS_FOCUSED_RECEIVER_XKB_DELIVERY_SCOPED` only if all four rows: receiver class is InputOnly; focus XID independently confirmed; control produces the exact `a` KeyPress and matching keycode/state KeyRelease (KeyRelease lookup payload is allowed and ignored for text); all three German rows query as `de`, have changed successful server dump and changed core map; the US control map remains unchanged; unsupported input is refused with zero candidate emissions and zero new key events; candidate KeyPress lookup bytes concatenate exactly to `=B2*A2`, with full keycode/state press/release trace matching the plan; and held-key state is empty. The independent audit and all corruption controls must also pass. Setup/focus/control/layout uncertainty is `STOP`; wrong text, event trace, unsupported preflight or release state is `FAIL`. No claim about application effects.

**C — constraints.** OrbStack Docker, pinned image `agent-interface-2972@sha256:69bc215db0514ee1bc4f730cceb296ecef89e4418cea8d4b2fc2ca3101101e27` (`linux/arm64`), `--network none`, read-only root/source, isolated writable raw output, bounded CPU/memory/PIDs and tmpfs. No host display, physical input, app, model/provider, package install, or production state. XTEST only targets the private Xvfb receiver.

**U — limits.** One current-main X11/XTEST implementation, standard German and US XKB maps, one synthetic ASCII formula, one control and three DE cases. This does not establish Calc/task effects, real desktop delivery, other maps/IME/Compose, physical input, other backends, application compatibility, performance, or product readiness. The receiver test only resolves whether the earlier zero-event result depended on the observation receiver arrangement.

## Formal execution discipline

Construction pilots are separate immutable records under `construction/`. The formal runner is invoked exactly once after `ALLOCATION_FREEZE.json` is committed and source/runtime hashes rechecked. On nonzero runner exit, preserve partial files and classify STOP/FAIL; do not retry. Auditor and corruption controls run only after successful raw output and use read-only raw mounts. Their output directories never sit inside the raw input.
