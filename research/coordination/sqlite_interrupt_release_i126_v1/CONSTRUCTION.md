# Excluded construction

A 15-cell same-process discovery probe (16 rows) preceded issue creation.
Its JSON outcomes are retained in DISCOVERY.json; temporary probe databases were
removed by TemporaryDirectory, so this is not a full-evidence scientific result.
The new separate-process construction ran all15 cells once with16 rows. All30
actors and the runner exited0. Its raw audit is1193 checks/errors=[] and all12
copied-data controls are effective/rejected. Source snapshots are retained here.
The first shell audit stdout exists, but its own PID/exit were not separately
retained. Later captured rechecks are separate; never infer that missing receipt.
Before freeze, supervisor timeout cleanup was changed from killing just its
runner to killing its owned process group. No timeout occurred and no science
was rerun for this edit. Formal will use64 rows,30 new cases,three batches.
