# Observed read receipts v1 — source-first freeze

Task `COORD-OBSERVED-READ-RECEIPTS-20260916-018`, Issue #501.
Publication base `e2f3afadef75f8778a4e9f07b51f7c9329b23c76`.
Formal allocation `observed-read-receipts-20260916-a1` contains exactly six first outcomes in `plan.json`; rerun budget is zero.

The only policy factor is dependency-set provenance: `broad` includes A/B/U; `observed` uses receipts generated by the actual A/B reads. The effect owner checks all token revisions and writes generation=2 in the same SQLite `BEGIN IMMEDIATE` transaction. No formal case runs before GitHub source readback identity is verified.
