# #4387: unequal-interval reversal inference

Research evidence only. Parents #2442/#4255 remain separate; no runtime/default promotion.

The new question holds three source observations, known speed 73 px/s and the 1 px point-error envelope fixed, changing only source interval geometry. It tests whether the full-displacement heuristic can mistake an ambiguous short interval for evidence of a reversal. The candidate enumerates continuous one-reversal compatibility and returns a direction only for a singleton feasible set. Query time is exactly the newest source sample, not later action time.

## Public preformal freeze

SOURCE_PACK.json and four source.*.b64 parts losslessly preserve all 14 preformal files, including the complete 360-case INPUT.json, full unchanged legacy.py, candidate, runner, independent auditor, eight unit methods, construction logs, environment, H/T/D/C/U PLAN and full PROOF with variable/unit table. Source capsule SHA256 97672fa8b7a29ca7a795aeebe57591c84c03b67d9a21d8d4610fffa2b9dec64e. The candidate and freeze are also directly readable here. Restore never executes an experiment.

```sh
python -B -S restore.py /tmp/reversal-g8m2-review
cd /tmp/reversal-g8m2-review
python -B -S -m unittest -v test_solver
```

When RESULT_PACK.json is present, the same restoration also includes retained formal outputs and process receipts. Then use `python -B -S audit.py .` for read-only reconstruction, and `python -B -S audit.py . --controls` for semantic corruption checks. Do not rerun the consumed run.py allocation. No Python -O; use a new output directory under a trusted quiescent parent.

One deterministic synthetic matrix, not GUI/capture/task evidence or random reliability trials. Exact rational oracle/proof is conditional on known speed, bounded point error, exact source times and at most one reversal. Same-author separate implementation/process is not independent human review. Current-source/input hashes and first stdout/stderr/exits are retained without rewriting previous studies. No old source-age live allocation is rerun or imported as new evidence.
