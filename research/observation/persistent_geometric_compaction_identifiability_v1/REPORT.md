# Persistent geometric state compaction-benefit identifiability — retained result

Task `PERSISTENT-GEOMETRIC-COMPACTION-BENEFIT-IDENTIFIABILITY-20260918-001`, Issue #1572.

## Decision

**`PASS_RETAINED_PGWS_COMPACTION_BENEFIT_NOT_IDENTIFIABLE_SCOPED`**

Four distinct retained real/model-facing families were inspected and no admissible pair was found in which task/decision state, model/effort, current evidence, pre-compaction world evidence, compaction boundary, prompt/capability, session/cache policy and independent oracle were all matched while only provenance-safe external persistent geometric state availability differed.

### Families

1. OpenTTD recovery pair3 contains a real context-compaction/presentation failure, but A and B do not share the same compaction boundary or subsequent current evidence/presentation path; exact model configuration is unavailable and the experimental factor is a recovery/presentation helper, not external PGWS.
2. Persistent planner continuity proves same-thread conversational recall and accounting, but has no context-compaction event and no external-state counterfactual; the two turns intentionally differ in input.
3. Integrated persistent interface evidence shows real post-repair method/reference reuse economics with an independent task oracle, but it changes grounding/model-call behavior, contains no model-context compaction factor and is not a geometric-state-only ablation.
4. The OpenTTD persistent-effect A/B/B/A family is close to a representation-only comparison: same frozen image/context and model route. It still has no context compaction, changes the prompt by adding a compact visual receipt rather than an external PGWS, and its primary checkpoint-status endpoint is not the independent task-effect counterfactual required here.

Synthetic #1560 and #1562 are explicitly excluded from efficacy evidence: they establish storage/role/provenance/currentness safety prerequisites, not model benefit.

## Integrity

The single deterministic ledger evaluation produced 4 candidate rows, 4 distinct families, 0 admissible pairs, and ledger SHA-256 `4e3821530dfacd0d28cbcf7473f54fa63393b383fd4cc66077c875d70cf257f7`. Primary audit PASS; separately structured independent audit PASS. Seven corruption controls pass, including count/decision/digest/invocation/family mutations, a gate flip that changes admissibility, and malformed source identity.

## Publication-order caveat

The H/T/D/C/U and matchedness gates were preregistered in Issue #1572 before the retained-data invocation, but the exact analysis source bytes were not remotely frozen before that invocation. The first invocation is retained as consumed; it was not rerun, replaced or tuned. The exact used source/ledger/result bytes are retained after the fact, and the independent auditor is separately retained. This is weaker provenance than a source-first formal allocation and must not be described otherwise.

## Next legal rung

A fresh model-facing study is needed if the idea is to advance. Both arms must receive the same task, current observation, pre-compaction historical world evidence, model/effort, instructions, session/cache policy and independent oracle. Force the same bounded compaction boundary in both. After compaction, differ only in whether the preregistered provenance-safe external geometric state is supplied as non-authoritative planner context. Any action still requires fresh current-evidence admission. The experiment must charge actual model usage and task correctness and may not treat persistent planner-thread recall or method-cache reuse as the PGWS factor.
