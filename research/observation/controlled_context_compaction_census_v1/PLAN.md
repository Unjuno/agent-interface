# Controlled context-compaction primitive census — Issue #1585

Task `CONTROLLED-CONTEXT-COMPACTION-PRIMITIVE-CENSUS-20260918-001`.

Pinned-repository capability census only. A matched PGWS model test needs one operation that is explicitly requestable, thread/target bound, completion-observable, boundary-identifiable, and repository-evidenced. Generic arbitrary-method transport is not enough. Natural compaction is evidence that compaction occurs, not a controllable matched-arm primitive.
