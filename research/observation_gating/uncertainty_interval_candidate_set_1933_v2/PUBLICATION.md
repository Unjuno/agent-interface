# Publication record

Issue #4276. Scientific first outcome is immutable: PASS_INTERVAL_AMBIGUITY_SET_SCOPED.

The full formal evidence package includes the exact preformal source/corpus plus FORMAL_RESULT, AUDIT, CONTROLS, execution exit/stderr records and postformal contract-test records. Restoring evidence is read-only and must not rerun `study.py` under the consumed allocation identity.
