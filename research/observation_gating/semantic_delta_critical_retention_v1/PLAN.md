# SEMANTIC-DELTA-CRITICAL-RETENTION-20260917-001

H: Event-aware typed semantic deltas preserve critical transitions that endpoint-only state deltas can alias, while preserving explicit UNKNOWN/base mismatch/no-authority semantics across retained Calc and OpenTTD evidence.

T: Deterministic retained-evidence replay only. Eight frozen cases x two representations = 16 rows. Exact retained Git blobs are source identities. No GUI/model/network/task input. Source-first freeze before the single formal invocation.

D: PASS iff event-aware preserves ordered Calc main->dialog->main focus edges, OpenTTD guard/effect/binding transitions, UNKNOWN semantics, base mismatch refusal and no-authority; endpoint-only misses the Calc workflow excursion; independent audit and corruption controls pass; formal invocation1/reruns0.

C: Typed retained events may make extraction easy; this does not prove semantic extraction from pixels.

U: One retained Calc workflow and OpenTTD fixture family; representation mechanics only.
