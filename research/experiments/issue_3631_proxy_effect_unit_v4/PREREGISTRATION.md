# Issue #3631 — representation-bound proxy effect unit, formal-04

Successor to #3619 formal-02 HOLD; distinct from #3626 formal-03 PASS. Preserve #3610/#3619/#3626 raw, freeze and audit bytes unchanged. Allocation: `issue3631-proxy-effect-unit-formal-04`.

## H/T/D/C/U

- **H:** In a disposable GTK/X11 counter fixture, four authority-neutral representations can each derive an increment target from their own assigned current representation; admission independently binds the XID to the actual live XRes PID and `/proc` start ticks. Stale/replaced/unavailable/ambiguous/macro-failure conditions refuse or yield before input, and a no-effect acknowledgement is not task success.
- **T:** First close all construction gates: (1) query XRes LocalClientPID from the selected XID and bind PID/start ticks/cmdline to the fixture's ready event; no caller-supplied pid hint establishes identity; (2) screenshot pixels, rendered proxy pixels, structured control geometry, and hybrid inputs each independently derive and record a target/coordinate and representation hash; no shared literal action coordinate; (3) freeze the canonical output path and prove wrong and symlink-alias paths are rejected before formal runner invocation; (4) wait for two ready fixture processes and two distinct visible XIDs before the ambiguous decision; (5) query Button1 state on the X server root resource and fail closed when pressed/unavailable. Then freeze source/prereg/auditor/image/paths. Run exactly one 4 arms × 7 cases = 28-row allocation in OrbStack Linux/arm64, network none, read-only root/source, dedicated fresh evidence output. Cases: positive, acknowledged no-effect, stale version, target replaced, unavailable, ambiguous, macro failure. One invocation, no retries.
- **D:** PASS only if all 28 unique rows reconcile: one exact 0→1 positive per arm; six negative cases per arm have zero task effects and the preregistered refuse/YIELD (no-effect may emit one click but must yield); every selected XID's XRes PID and live process start ticks match the fixture owner; each arm's derivation consumes its own recorded representation bytes/state and yields an auditable target; canonical output accepted while wrong/symlink paths stop pre-runner; ambiguous targets are established before dispatch and produce zero emissions/effects; all release queries observe Button1 up; cleanup, raw hashes, independent audit and corruption challenges pass. Any unsafe effect is FAIL; missing identity/representation/path evidence is HOLD; setup failure is STOP. No arm winner or benefit claim.
- **C:** Same synthetic GTK task, four arms, seven cases, pinned image `sha256:69bc215db0514ee1bc4f730cceb296ecef89e4418cea8d4b2fc2ca3101101e27` (linux/arm64), and prior action/effect semantics. The specific added factors are independent XRes/process identity, representation-bound derivation, and pre-runner canonical-path enforcement. No edit to predecessor outputs.
- **U:** One deterministic GTK/Xvfb fixture only. No model/human usability comparison, latency/token/cost advantage, general GUI safety, production authority, or integrated desktop claim.

## Predecessor boundary

#3619 formal-02 remains `HOLD_EVIDENCE_INCOMPLETE`; #3626 formal-03 remains its own `PASS_PROXY_BINDING_EFFECT_UNIT`. This allocation tests the three additional gaps identified in #3631 plus the same closed ambiguity/release controls; it does not repeat either result or claim representation superiority.
