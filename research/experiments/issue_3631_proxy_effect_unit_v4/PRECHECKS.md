# Construction, preflight, and formal record

Allocation: `issue3631-proxy-effect-unit-formal-04`.

Construction retries are permitted only before the frozen formal output is invoked. Every STOP/FAIL and correction is retained here. After formal runner invocation, no retry is allowed.

The gate order before freeze is: XRes XID→PID/start-tick binding; screenshot/proxy/structured/hybrid-specific derivation; canonical-path and symlink rejection; exactly-two-target ambiguity synchronization; root-window Button1 release observer; GTK construction effect; unit and corruption tests. The formal launcher reruns all preflight gates against the frozen source/image before its sole formal command.

The first two-target preflight passed its gates but its summary field reused the loop variable and mislabeled the initial XRes PID as the second target's PID. No formal allocation had run. The summary now stores the initial owner separately; the next preflight record is the retained construction record.

Pre-formal final gates: frozen hashes/source/image PASS; six unit tests PASS; GTK/Xvfb XRes and actual XImage decode PASS; screenshot/structured target agreement `[236,88]`; two process/two XID ambiguity and no-new-action gate PASS; root Button1 up PASS; wrong-path and symlink-alias launcher tests PASS. Formal launch then completed exactly once: 28 rows, zero runner errors; independent audit PASS with seven of seven corruption controls detected. A separate post-formal cross-binding auditor also recomputed the representation payloads and dispatch-state identities for all 28 rows and passed without modifying frozen raw evidence.

Construction corrections retained: initial test invocation omitted the read-only launcher mount; it failed while opening `/formal_launch.sh`, then passed after the mount was added. One ambiguity-preflight summary initially reused a PID loop variable and mislabeled its initial-owner field; the source was corrected, frozen, and the final preflight reports initial PID 10 and ambiguous XRes/ready PID set `[10,13]`. Neither event invoked or repeated the formal runner.
