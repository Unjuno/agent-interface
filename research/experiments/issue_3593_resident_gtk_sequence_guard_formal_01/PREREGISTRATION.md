# Issue #3593 formal GTK allocation (preregistered)

This directory records an independent finite GTK-effect test of the sequence-guard policy. The OrbStack preflight successfully exercised GTK3, private Xvfb, a synthetic Space press/release, visible title change, and raw frame retrieval; that is environment setup evidence, not the formal result.

The frozen allocation is one invocation, zero retries, 36 fresh rows: nine exact traces × two actual candidate policies × effect on/off. Each event is stepped individually and any emitted action is delivered immediately before the next event. Source is read-only, network disabled, evidence output fresh and writable.

PASS requires the exact matrix, independent prefix replay, per-prefix visible GTK effect/title, disabled-effect controls, stale replacement rollback failure on upstream but refusal on guarded candidate, valid positive controls, key-up, frame byte/hash/dimension verification, process incarnation and reap evidence, and corruption challenges.

Image: `sha256:69bc215db0514ee1bc4f730cceb296ecef89e4418cea8d4b2fc2ca3101101e27` (`linux/arm64`).
Source main commit: `bec31389157ae5237bcc6474dfd9bf9b16d30752`.
The raw formal result does not yet exist.
