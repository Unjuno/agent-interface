# STOP — construction-01

No container started and no experiment row was produced. The host-side `docker run` command contained an image digest transcription error (`...302c2c805...`) and OrbStack rejected it as not found. The inspected local image ID is `sha256:2f17fc044b579bab302c2e8054d3a686e2cb9a83de48e70534b94cd8ebbe06a9`, which is the preregistered pin. Preserve this setup STOP; the corrected mock-only attempt uses a separate `construction-final-02/` output directory. Formal allocation remains unspent.
