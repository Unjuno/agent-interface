# Text delivery capability model v1

Offline candidate for representing text-delivery routes separately from semantic text intent. The model makes clipboard/keymap/accessibility/IME side effects explicit and fail-closed. It consumes retained evidence but does not modify shared runtime or contract files.
