# XKB core-map projection transfer

Research-only transfer test for `text_payload_preflight_v1`. It deliberately separates exact delivery from capability coverage: the same semantic text request may be directly representable on one live keymap and correctly refused on another.
